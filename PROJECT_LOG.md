# PROJECT LOG — The Sorcerer-Sword

One running log across the whole project (game, game3d uplift, book, marketing) so progress and cleanup don't require digging through a dozen scattered STATUS docs. **Newest entry on top.** Keep entries short — one paragraph, what changed + where the detail lives (link the doc/commit, don't paste it). This file replaces reading through `game3d/GAME3D_UPLIFT_PLAN.md`'s STATUS history, `PLAYTEST_FINDINGS*.md`, and `GAME_BACKLOG.md` day to day — those stay as the detailed source-of-truth per topic, this is the index.

**How to add an entry:** one block per work session/agent run, dated, 2-6 lines: what changed, what area (game / game3d / book / marketing / infra), and a pointer to the full detail if there is one. When a `## NEEDS HIRO` or `## PENDING` item gets resolved, move it out of the Pending section below into the dated log with a "RESOLVED" note.

---

## 2026-07-19 — [Fable 5] ARCH WARLOCK form-congruence batch (BUG-020) — 6 bugs + 2 follow-ups

Hiro's 6-item batch: arch warlock idle strobed (old 3-frame demonlord set yoyoed) and dash/portal/hex reverted to base-warlock art because no animation selection branched on `P.archWarlock`. Fixed as one category (form incongruence): `heroOneShot` + dash `_castSet` are now form-aware (`archwarlock_hex/portal/dashcast`, warlock fallback); hex/dash projectiles carry an `arch` flag selecting new finishers `hound_ahexhit` (TWO claw demons) / `hound_afirehit` (arch succubus). Art: Hiro's 3 NEW golden refs (`archwarlock_dash/hex`, `hound_archwarlock_hex`) sliced verbatim (new border-flood dark-bg keyer — interiors survive dark-on-dark); agent-scaled sheets kept only where no ref exists: `archwarlock_portal`, `hound_afirehit`, `demonlord_idle` 4f/`walk` 5f — ALL SCALED OUTPUT UNREVIEWED pending lab greenlight. Follow-ups: lv10 evolution road now needs ≥10 hound-kills of exp (`P.kills>=10`); SUMMON button hidden + `startChannel` hard-guarded (was resurrecting the warlock body over the lich). `archwarlock` added to all three resolver maps. Verified: smoke gate 4+20 GREEN, node --check, wiring greps, frame counts. Detail: game3d/BUGLOG.md BUG-020; new FORM-VARIANT CONGRUENCE checklist in game3d/docs/MAINTENANCE.md. xAI spend this round: 8 calls (agent batch) ≈ $0.48. SAME-DAY ADDENDA: (a) evo road wasn't triggering — `P.kills` was never incremented ANYWHERE (dmgBonus has been flat since port); killEnemy now counts, gate reduced to kills-primary `EVO_KILLS_NEEDED=5` with level-snap to 10 + progress popup; node harness on verbatim fragments ALL PASS (offer at exactly kill 5, both roads, no re-offer). (b) Hiro's `dancer_move.png` golden ref sliced: dancer_idle 8 (replaces gen 10f, stale 9/10 swept), NEW dancer_walk 8 + dancer_dance 12 (auto-created anims, ready to wire wherever she should dance). (c) `dancer` token added to ingest/gen resolver maps. (d) ART CLEANUP (Hiro request): retired warlock sets moved to `game3d/art_archive/removed_2026-07-19/` (50 files) — `warlock_summon` x8 (summons permanently retired, channel hard-guarded), `warlock_cast` x3 (dead fallback — the bare 'warlock_cast' texture it checked never existed), `warlock_walkb` x8 (zero code references), + the stale art_in/ intake copies of warlock_dashcast. Hound had NO unused sets (all 14 live: 5-row combat + 9 finisher variants + base still). Manifest rebuilt (147 sets); preload verified clean — archive folder sits outside assets/sprites so the recursive manifest scan can never resurrect it. Restore = move files back + rerun ingest. (e) FINISHER PACING round 3: every warlock/arch-warlock finisher on the hound slowed again per Hiro — firehit/portalhit/afirehit 5→3.5fps, clawhit 6→4fps, durs stretched to cover playback; claw-demon cameos (hexhit/ahexhit) exempt at 6fps; lich finishers untouched. (f) Hiro's NEW golden ref replaced `hound_portal_bonedragon.png` (BONE DRAGON ACID ASSAULT — the old sheet's hound vanished mid-sequence): sliced verbatim, hound_portalhit x12 overwritten, K remeasured 1.70→1.69. (g) Playtest fixes: BOOT_V cache-bust was DATE-granular so same-day sheet overwrites served stale from browser cache (why the new bone dragon didn't show) → now Date.now() every boot; labRestart (R) now FULLY resets the hero to a fresh lvl-1 base warlock (level/kills/roads/lich/archWarlock/demonLord/devilT/radius wiped, 'LAB RESET' popup) — it previously only respawned enemies, leaving him a lich. (h) Key-color policy: reference/gen sheet background switched GREEN→MAGENTA (FF00FF) — green screens clash with the kit's sheol-green FX; gen_sprites.py prompt + MAINTENANCE.md updated; slicers auto-detect bg so old green sheets still re-key. Arch-warlock body ref incoming from Hiro (BUG-016 stays open). (i) HIS BODY REF LANDED (`archwarlock_move.png`): archwarlock_idle 6 / walk 8 / hex 8 (replaces the previous 12f hex; stale 9-12 swept) sliced with pocket-scrub + number-zone-only text mask (the old full-band text mask was PERFORATING silver bead trim — root cause found via magenta-bg contact sheets); heroFormTex archWarlock→'archwarlock' (demonlord stand-in RETIRED), bare still + preload + TARGET_WORLD_H 1.1 wired. (j) TRANSPARENCY AUDIT (Hiro request) across all 446 warlock+hound frames: 4 frames had unkeyed opaque corners, afirehit carried chroma-green residue — 17 files scrubbed (interior pure-green pockets + edge fringe de-spill), normals rebuilt; first corner-flood attempt on archwarlock_hex_6/7 ate the body and was redone from the ref (lesson: verify fixes on MAGENTA contact sheets, now the standard check). BUG-016 arch-warlock half is DONE pending Hiro's in-game greenlight; arch grim reaper half still open. (k) LICH SLASH polish (Hiro playtest: "hex is really just a scythe slash"): the lich's light verb is now NAMED what it is — touch button + verbs journal read SLASH in lich form (button form-aware in updateLabels, resets on lab R; stale 'Q SUMMON' hint dropped) — and the scythe reach that triggers the shambler finisher went up ~45% (max(96,0.42·HERO_PX) → max(140,0.6·HERO_PX)). (l) HOUND GREENLIT + LAB TARGET → BEAST (Hiro): TEST_ENEMY default 'hound'→'beast' (?foe=hound revisits). Beast base kit already live (idle4/walk4/attack4/hurt3/death4, world-H 1.9, dynamic zoom covers it); NO beast finisher sheets exist yet — foeFinisher stays silent on ability hits until Hiro's beast hurt refs land (per pipeline: no auto-scaling from the hound without his say-so). Awaiting refs; suggested names beast_hexhit/firehit/portalhit/clawhit/ahexhit/afirehit/scythehit/arrowhit/fadehit, MAGENTA bg per the new key policy, beast visible in every frame. FIRST BEAST REF LANDED same session: `beast_hexhit.png` (8f single-row, dark stage) sliced verbatim → beast_hexhit x8 (K 1.45 from solo-beast frame 1; the wide-row hairline filter had to be dropped for single-row sheets — it was chopping the lying-down beast). NEW `FINISHER_K` per-enemy override map ('<enemy>_<kind>' → K) since kind-level K was hound-measured; crowd override reads it first. Hex on the beast now plays the claw-demon combo; auto-revives for the other kinds as their sheets land. NEW TONE WORKFLOW (Hiro): GPT guardrails were flattening his intended tone, so refs now come as GPT layout-anchors + his ORIGINAL prompt, and Grok re-renders truer to intent (his ref = composition anchor, 1-2 calls/sheet). First use: `beast_firehit` — Grok edit of his ref (laughing succubus, theatrical bend-over flame, glowing heart kiss, fiercer fire) → 8f sliced, K 1.638 in FINISHER_K, UNREVIEWED; raw sheet kept at tools/sheets/sheet_beast_firehit_grok.png. 1 call ≈ $0.06. `beast_portalhit` next via same workflow (3 calls: one 400-rejected prompt rephrase, v2 dropped the beast from frame 7 for a gore piece — violating Hiro's beast-in-every-frame rule — v3 fixed 7/8): bone dragon arm-snatch + green fire + arm-regrow, 8f sliced K 1.781, UNREVIEWED, raw at tools/sheets/sheet_beast_portalhit_grok.png. `beast_clawhit` (4 calls: Grok's OWN image moderation rejected 3 renders — entrails/disembowelment is a HARD Grok ceiling; "demonic war-beast + dark ichor + battle damage" framing passed): void-claw combo 8f, K 1.65. `beast_ahexhit` (4 calls: v2 good green-violet but frame 1 bolt-only; v3 fix refused; v4 = flat-green-stage re-render after v3's black-on-black proved unkeyable + cobble slabs — frame 1 DROPPED, ships as 7f, K 1.625; slicer gained a green-dominance term for gradient stages). All UNREVIEWED. Session spend ~13 calls ≈ $0.78. `beast_afirehit` (1 call: arch succubus green flamethrow + taunt belly dance + mocking kiss; frame 1 bolt-only again → dropped, 7f, K 1.181) + `beast_scythehit` (1 call: spectral scythe no-wielder + shambler maul + crumble-to-dust; 8f, K 1.389; corner bg-sample had to move inside the art band — black title strip fooled it). Both UNREVIEWED, raws in tools/sheets/. Grok budget topped up to $14.29 (Hiro). Beast finisher coverage: hex/fire/portal/claw/ahex/afire/scythe DONE. `beast_arrowhit` (1 call: volley + dagger assault + ash crumble, 8f K 1.438) + `beast_fadehit` (1 call: black-dragon shoulder-bite fling with whole-beast crash frame + green flame bath, 8f K 1.82) — ALL NINE beast finishers now live, all UNREVIEWED pending Hiro's lab pass. A GPT-ref authoring checklist was given to Hiro in chat (flat key bg / no caster in frame / victim every frame / no scenery / even columns).

## PENDING / NEEDS HIRO (standing open items, check before starting new work)

- **xAI/Grok art budget (Hiro, 2026-07-15): $6.84 remaining** (measured: $7.15→$6.84 across the last ~5 sheets ≈ $0.06/call). Runway ≈ 110+ calls. Corrected 2026-07-17: BUG-009 is CLOSED (see BUGLOG.md FIXED), stale reference to it as queued art removed. Still genuinely queued and now BLOCKED — both are tagged `[NEEDS-FABLE — art]` in BUGLOG.md and Fable is no longer available (see the "WORKFLOW CHANGE" entry below — dated 2026-07-07 in the log; flagging that its date looks out of sequence relative to the 2026-07-14/15 entries above it, but not correcting it without checking — may just reflect real elapsed time between sessions): **BUG-016** (arch-warlock/arch-reaper evolution sheets, ~2-3 calls) and **BUG-018** (stitch/chain/gunner feet-clipping redo, ~3 calls). Both are small, bounded Grok jobs Claude/Sonnet could run directly now (API key confirmed present) — needs Hiro's go, not started unasked. `game3d-bugfix` schedule (hourly bug-fix loop, last ran 2026-07-15) is currently disabled — confirmed via `list_scheduled_tasks`, no active resource risk.

- **[CLI] Git push — this repo:** `git status` (2026-07-03) shows no corruption, just normal unstaged/untracked changes (this repo's index looks healthy now — the 2026-06-22 corrupt-index/mass-deletion note in `GAME_BACKLOG.md` appears stale/resolved). Still needs a manual `git add`/`commit`/`push` pass whenever you're ready — automation never auto-pushes.
- **[CLI] Git push + index repair — live site repo (`Neverendingnarratives`):** separate repo, not connected in this session so I can't verify it directly. Backlog (U2) reported its OneDrive git index throwing `index.lock cannot be unlinked` / `cache entry has null sha1`. Check/repair that one yourself from a normal local session before pushing it.
- **game3d animation/summon/VFX/traversal overhaul (2026-07-03):** plan written, see `game3d/GAME3D_ANIM_COMBAT_OVERHAUL_PLAN.md`. Hand this to the Fable chat. **Do not run `gen_sprites.py`/art generation until Phase 2 ships** — the stale queue was cleared 2026-07-03 (see below), so `needed_sprites.json` is now empty; it'll only be worth running again once the auditor re-queues it against the fixed code.
- **Scheduled tasks:** all 10 are currently disabled (see below, 2026-07-03). Don't re-enable `game3d-build` / `playtest-bughunt` at their old 8/13-minute cadence — that's what burned the whole week's quota. Decided 2026-07-03: routing game3d work through a separate Fable-5 chat instead of schedules for now (schedule creation doesn't currently let you pick a model from chat — that's only available via the Scheduled-tasks page in the app UI — and self-disable-on-completion has proven unreliable in practice, so any future schedule should be treated as needing manual disable regardless of what its prompt says).
- **Visual auditor:** `game3d/tools/audit/latest.json` is stale (last run 2026-07-02 17:34, verdict PASS/0 P1 at that point). Not urgent to restart (`python game3d/tools/visual_audit.py --watch`) until active game3d-build work resumes.
- **Book 2 editorial / audiobook schedules:** `book2-editorial`, `book2-detic`, `book2-voiceprep`, `audiobook` all ran and self-disabled at various points in June, but no output for them was found in this repo (`The Sorcerer Sword ARPG`) — likely living in a different folder. Worth a separate check if you want their status.

---

## 2026-07-14 — BUG-002 fixed: stale foeSprite visible:true hygiene (game3d) [bugfix-schedule]

**game3d.** The retired duel-path foeSprite/foeShadow (BRAWLER UNIFICATION) no longer report visible:true — born `setAlpha(0).setVisible(false)`, the per-frame retired-path guard and advPitVisible(false) now set visible:false too. Cosmetic/hygiene; sim and crowd render untouched. Verified: 12-assert node harness on the verbatim edited fragments, ALL PASS + Read-tool tail check (`</html>` intact at 3804). Detail: game3d/BUGLOG.md FIXED. Queue now: only [NEEDS-FABLE] entries remain OPEN (BUG-016 arch forms, BUG-009 old-gen sheets) — nothing schedulable next run.

## 2026-07-19 — Dragon re-keyed green (scaled variant of Hiro's ref), aspect DAMAGE, summon labels hidden (game3d) [Fable 5]

**game3d.** (1) BLACK DRAGON fix: generated ONE scaled variant of Hiro's own hound_blackdragon golden ref with ONLY the background swapped to chroma green + silhouettes brightened (edit-anchored, sequence/captions preserved, 1 xAI call) — keyed cleanly (multi-blob keep for the separate dragon+hound bodies), 16 transparent frames replaced the vignette panels, K 1.742. UNREVIEWED. (2) ASPECT DAMAGE (new aspectDamage helper, level-scaled, defers correctly under active finishers): lich dash arrow buffed to moderate ((diceN d6 + bonus)×1.2), PORTAL bone-dragon assault now bites (×1.2, green numbers), FADE roast burns (×1.2). (3) SUMMON scrubbed from the UI: journal kit line → 'HEX / CLAW / WARD / DASH · demon aspects answer his strikes', verbs + touch labels Q → CLAW. Verified: fadehit contact sheet + manifest 16f + full-file node --check.

## 2026-07-19 — HIRO'S LICH GOLDEN REFS LIVE: shamblers maul, bone-archer execution, black-dragon roast (game3d) [Fable 5]

**game3d.** Hiro delivered three golden references (tools/refs: hound_shamblers 16f 3-row, hound_bonearcher 12f, hound_blackdragon 16f 4×4) — sliced VERBATIM, zero AI art, ingested under the auto-revive names so the gated triggers lit up with no new trigger code: hound_scythehit (16f, K1.0 — shambler maul→decay→rise), hound_arrowhit (12f, K1.163 — arrow volley→dual-sword execution→crumble), hound_fadehit (16f, K1.45 — dragon roast). NEW SLICING MODE for his dark-stage sheets: background-DISTANCE keying (corner-median bg, steep alpha ramp) for the shamblers/archer sheets; the blackdragon sheet's art is tonally inseparable from its stage, so it ships as VIGNETTE PANELS (full painted frames, 16px soft-faded borders — art untouched) — flagged for Hiro: if he prefers keyed transparency there, a chroma/lighter-bg re-export of that one sheet is the fix. WIRING RESTORED: lich dash → bone-arrow projectile → arrowhit on impact; fade → fadehit (dragon roast) on the nearest foe; scythe → scythehit (was already gated in). Pacing 5fps (16f=3.2s+hold / 12f=2.4s). All three UNREVIEWED pending the lab. Verified: contact sheets per set + manifest (16/12/16) + full-file node --check.

## 2026-07-19 — Lab feedback round: firehit/portalhit retuned, LICH ASPECTS REJECTED (awaiting Hiro's refs), arch-warlock stand-in body (game3d) [Fable 5]

**game3d.** (1) Succubus (firehit) + dragon (portalhit) finishers: slowed 6→5fps (2.4s play, 2.6s hold) and shrunk ~15% (k 1.96→1.65 / 2.01→1.70); per-finisher fps now supported. (2) LICH ASPECTS UNDONE per Hiro (didn't pass review — he's creating golden references himself, the pipeline working as intended): 120 frames (hound_scythehit/arrowhit/fadehit + lich_dashcast/fadecast) archived to _src/rejected_lich_aspects_2026-07-16, manifest rebuilt clean; the trigger code STAYS gated-off and auto-revives when his reference sheets are sliced under the same set names; lich dash reverted to a plain blink (no cameo, no projectile), fade cast/finisher wiring removed. (3) ARCH WARLOCK now visibly transforms on the herald pick: wears the black-green DEMONLORD body as an explicit STAND-IN until Hiro's own arch-warlock reference lands (BUG-016). Verified: manifest shows zero rejected sets / all keepers intact; full-file node --check.

## 2026-07-16 — LICH/DEVIL ASPECTS SCALED (7 sheets off the golden refs) + PERMANENT FORMS + Q=CLAW + death-anim fix (game3d) [Fable 5]

**game3d.** (1) DEATH-DISAPPEAR FIX: the field-wave clear wiped enemies[] instantly, destroying the sprite before the death anim — corpses now persist until the next wave spawns (death anim + fade play fully; deferred-kill chain intact). (2) ASPECTS SCALED UP (agent, 10 xAI calls incl. 1 reject + 2 timeouts) — per the art-pipeline skill, every sheet is a SCALED VARIANT edit-anchored to Hiro's four GOLDEN REFS (hex/fireball/portal/dash), swapping cameo + palette; ALL UNREVIEWED pending his lab greenlight: hound_scythehit (shambler swarm, K1.385) · hound_arrowhit (bone-archer volley, K1.494) · hound_fadehit (obsidian dragon, K1.912) · hound_clawhit (arch-devil gouges that bleed then knit shut, K1.697) · lich_dashcast (archer arrow cameo, frac .935) · lich_fadecast (black-dragon dive, frac .957) · archdevil_dashcast (ARCH succubus, green fire, frac .934). TRIGGERS: scythe hit→scythehit · lich dash→bone ARROW projectile→arrowhit · fade→lich_fadecast + fadehit on nearest foe · devil/warlock heavy claw→clawhit · dash cast sheet + projectile now FORM-MATCHED (warlock fireball / lich arrow / devil sheol-green fireball). (3) KIT RESTRUCTURE (Hiro): lv10 evolution transforms IMMEDIATELY + PERMANENTLY — binder road = permanent LICH on pick (no death-wait; P.lichPerm), herald road = permanent ARCH WARLOCK (flag + herald bonuses; own sheet pending BUG-016); Q = CLAW ATTACK in warlock form (devilClaw + archdevil claw sheet — the arch-demon is an aspect now, not a transformation; lich Q: 'the scythe is enough'). Known trade-off flagged: green-screen keying eats some ghost-green portal glow (survives as chartreuse); if it reads weak in the lab, those combos need fresh human refs per the pipeline. Verified: full-file node --check, all 7 sets in anims.json @12f.

## 2026-07-16 — Finisher pacing + DEFERRED DEATH (hurt sequence completes before the death anim) (game3d) [Fable 5]

**game3d.** Hiro's lab feedback: (1) sequences slowed — finishers 8→6fps (12f = 2.0s, hold 2.2s), warlock dash-cast 14→7fps (heroPlayOnce reads the live rate so its revert timing follows). (2) DEFERRED DEATH: a killing blow during (or triggering) a cameo finisher no longer skips it — killEnemy() now DEFERS while _finishT runs (foe pinned at 0hp, all kill paths covered incl. hex ticks + splash), and the deferred kill fires the moment the sequence ends → death animation, loot, kill-beat and lab-respawn all land AFTER the full hurt sequence. Dash-fireball trigger reordered (finisher applies before the kill check). Also: FIGHT LAB boot fixed earlier this session (resume-from-save was outranking the lab). Verified: full-file node --check + defer harness (fatal hit → not dead mid-sequence → dead exactly at expiry). Still UNREVIEWED per the pipeline until Hiro greenlights in the lab.

## 2026-07-16 — GOLDEN REFERENCES IMPLEMENTED: ability-aspect finishers live in the FIGHT LAB (game3d) [Fable 5]

**game3d.** Hiro delivered four golden-reference sheets (tools/refs) and they are now IN THE GAME, sliced verbatim from his art (ZERO AI generation): hound_hex_clawdemon → `hound_hexhit` (12f), hound_fireball_succubus → `hound_firehit` (12f), hound_portal_bonedragon → `hound_portalhit` (12f), warlock_dash_succubus → `warlock_dashcast` (12f). All 12 frames of a set share ONE union canvas (bottom-center) with a per-set normalizer k (canvasH/subject-height: 1.68/1.96/2.01) so the hound stays true-size while the cameo demon/dragon towers. SYSTEM: `foeFinisher(e,kind)` — plays the enemy's cameo hurt sheet ONCE (8fps, ~1.7s, foe held via stunT, facing frozen toward the attacker, render owns the sprite exclusively); PIPELINE GATE: fires only if `<enemy>_<kind>` exists in anims.json, so new enemies light up per-combo exactly when their greenlit sheet lands. TRIGGERS (exactly as specced): hex hit → hexhit · DASH-conjured fireball hit → firehit (blink now plays the warlock_dashcast cameo, then fires a src:'dash' fireball at 430ms along his facing; only THAT fireball triggers the finisher) · portal swap → portalhit on the swapped foe. Hero cameo uses figure-frac 0.898 so the warlock stays true-size under the portal/succubus overlay. VERIFIED: full-file node --check (318KB script, mount un-pinned after sandbox restart), finisher-gate harness (hound fires / gunner correctly refuses / faces attacker / 1.7s hold). STATUS PER SKILL: implemented but UNREVIEWED — needs Hiro's in-game greenlight in the lab (R to repeat: J hex · SPACE dash · K portal). NOTE: /tmp toolchain was wiped by a sandbox restart; tools/ingest_art.py now serves fully via the mount and worked directly.

## 2026-07-16 — ART-PIPELINE RESTRUCTURE: FIGHT LAB (hound-only), summons retired to aspects, skill adopted (game3d) [Fable 5]

**game3d.** Hiro added the sorcerer-sword-art-pipeline skill (human golden references first, AI scales only, mandatory reference communication, summons = ability-cameo frames NOT standalone entities, one enemy at a time). THIS SESSION = setup only, zero art: (1) roster inventoried — 14 types, all spawn sources mapped incl. mid-fight reinforcements (master hounds L749, necro skels L804, champ thralls L847). (2) FIGHT LAB shipped: boots to the old road with ONE enemy (Hiro designated HOUND), auto-respawn ~1.2s after each kill with round counter, R = instant restart, ?foe=<type> swaps the enemy, ?lab=0 restores normal play; a per-frame sweep purges ANY non-TEST_ENEMY foe from every spawn source, and lab mode freezes area transitions + story triggers (all content intact behind the flag; 1-of-each gauntlet now opt-in ?test=1). (3) SUMMONS_OFF: Q disabled with an in-fiction popup ('the demons answer other calls now') — becoming ability ASPECTS per the skill; nothing deleted. Verified: lab-loop harness (single hound, intruder purge, kill→respawn round 2, R→round 3) ALL PASS. NEXT (needs Hiro's golden references per the skill): Hex/Claw-Demon, Dash/Succubus, Portal/Dragon cast-cameo + hound hurt-finisher sheets — none exist yet.

## 2026-07-15 — Dancer polish (taunt cut, captions gone, SMALL corner card) + hero facing hysteresis (game3d) [Fable 5]

**game3d.** Hiro's four: (1) dancer TAUNT dropped from the showcase cycle (janky slice; frames stay on disk). (2) The 'IDLE/SWAY' words were the showcase caption — labels removed entirely (plus one frame with a baked label blob scrubbed + normals regenerated; all 20 idle/sway frames contact-verified clean). (3) Dancer MvC card was full-screen — hyperCutIn gained a SMALL mode: corner face card at ~30% height with speed-lines, NO scrim, NO hit-stop, the fight stays fully visible; her reactions use it. (4) "Arch warlock turns left/right at idle": P.face was re-derived every frame from the current foe's side, so a foe hovering near his x (or first-alive swaps) flip-flopped the hero — 24px hysteresis added (flip only on a decisive side); fixes all forms, archdevil included.

## 2026-07-15 — THE DANCER REACTS: Capcom face-card pop on kills she witnesses (game3d) [Fable 5]

**game3d.** Hiro dropped tools/dancer_mvc.png (a second dancer sheet, different outfit). Used the EXPRESSIONS row ONLY — six face close-ups sliced (hue-keyed for its two-tone green panels, baked frame digits trimmed off the chins), ingested as dancer_face_1-6 under npcs/; the sexualized poses row was deliberately left out of the game assets, and the idle/walk rows skipped (outfit clashes with the canonical stage sheet — face cards don't). WIRING: killEnemy — any kill landed while the dancer is visible in the background triggers her own MvC face-card cut-in (random expression + line: "ENCORE!" / "I've seen worse footwork." / "he DIPPED him." / "now THAT is choreography.", pink card, 650ms), queued 1.35s behind a boss FATALITY so the two cuts never collide. Zero xAI calls.

## 2026-07-15 — THE DANCER arrives: Hiro's sheet sliced + background showcase on the old road (game3d) [Fable 5]

**game3d.** Hiro dropped tools/dancer_ref.png — a complete hand-made 5-row green-screen sheet for the DANCER OF VARENHOLM (the mq6 hook). Sliced + ingested with ZERO xAI calls: dancer_idle 10 / sway 10 / spin 10 / wave 10 / taunt 8 (the taunt row's uneven spacing broke valley cuts — blob-run sliced instead; 48 frames, filed under npcs/ with 'dancer' added to the resolvers, world height 0.8 halfling). SHOWCASE: she dances in the BACKGROUND at the start of the old road (one step up the depth plane), cycling all five states every ~3.4s with a 'THE DANCER — <STATE>' label so each can be reviewed in place; keeps dancing through test-gauntlet fights. Render-only; ready to become the Varenholm quest NPC when Chapter 2 opens.

## 2026-07-15 — Summon fixes: fiend passes his master, shambler/archer resheeted, real bone arrows, form-true FATALITY (game3d) [Fable 5]

**game3d.** (1) CLAW FIEND "doesn't go to enemy": the warlock-ejection ring applied to SUMMONS — the fiend couldn't cross his master to reach a foe on the far side; allies now pass P freely (enemies still ejected). (2) Shambler + bone archer left-right jitter + glitchy attacks: their sets were the last OLD keyframe art (3-frame, LEFT-facing) — both resheeted modern (shambler lurching two-arm swipe; bonearcher raise/nock/FULL DRAW/loose — first attempt rejected for identical frames, retry passed), and the demon-draw flip is now art-aware + hysteresis (only re-flips on decisive horizontal facing |cos|>0.25 — kills the near-vertical-path jitter). Demon draw also picks _idle sets at rest. (3) Bone arrows get their OWN art: anim_bonearrow (carved shaft, ghost-green wisps; filed under fx/, all three resolvers updated) — hexbolt fallback keeps the bone tint. (4) MvC FATALITY portrait now uses heroFormTex() — the LICH cut shows the lich (arch devil/demon lord likewise). Spend: 4 calls (incl. 1 rejected archer).

## 2026-07-15 — HURT COVERAGE COMPLETE: all 14 enemies react to hits; BUG-009 closed (game3d) [Fable 5]

**game3d.** Hurt-animation audit found 5 gaps (hound's 1-frame hurt row + door/master/champ/skel with no modern sheets at all — the standing BUG-009). Agent batch (5 calls, 0 retries): full 5-row sheets for all five, with per-sheet fixes (champ's baked-in label text pixel-erased, skel's two-tone green grid remapped pre-key, door recounted by x-density). Stale sweeps done. FINAL TABLE: every one of the 14 enemy types now has hurt ≥3 and death ≥4 (manifest = 125 sets); the crowd renderer's flash→hurt / death-one-shot selection needed zero code changes. Also: enemy projectiles made unmissable (muzzle pop, 100px streaked bolts, glow halo, trail motes, impact pop — harness-verified sprite pick + cleanup). Spend: 5 calls.

## 2026-07-15 — PERF PASS + solo fast succubus + gunner resheet + no more invisible attacks (game3d) [Fable 5]

**game3d.** (1) PERF ("running significantly slow"): BOOT_V cache-buster was Date.now() — EVERY page load refetched ~1100 frame PNGs; now busts once per DAY (?fresh=1 forces a refetch after mid-day art drops). DOM hint/chip writes now only-on-change (per-frame textContent was thrashing layout). Hex-stack popup storm halved (damage numbers every other tick). (2) SUCCUBUS: coven summons ONE succubus (was 3; evo multipliers still scale) working 5x faster (cool 2.2→0.44 — mend or fire every ~0.44s). (3) GUNNER sheet regenerated (locked right-facing stance, smoke/coat-only idle variance, big muzzle flash, kneel-to-flat death; 4/6/5/3/4, stale hurt_4 swept). (4) INVISIBLE ATTACKS: every non-gas ground zone now renders generically (telegraph ring while tele, pulsing danger fill while live — the master's WHIPCRACK was landing invisibly); bone-archer ARROWS ride the bolt art with a pale bone tint (were faint orbs). Also this round (interrupted-task carryover): TEST GAUNTLET debug encounter (boots on the old road, 1-of-each × 14 in order, ?notest=1 or TEST_ENCOUNTER=false disables, never persists, outranks resume) + jump/slide switched to PHASE-DRIVEN frames (launch/rise/apex/fall/land · drop/hold/recover — physics picks the frame, nothing loops; harness 1→2→3→4→5 & 1→3→4). Spend: 1 call.

## 2026-07-15 — OFF-MODEL PURGE + facing root cause + blackdragon idle + pyre PROJECTILES (game3d) [Fable 5]

**game3d.** (1) OFF-MODEL PURGE (Hiro: jump/slide didn't match the ref): the moves sheet had anchored to tools/ref_warlock_idle.png = the OLD front-facing design. The anchor is now warlock_idle_1 (sheet-era = THE model; old ref kept as refs/ref_warlock_front_LEGACY.png); moves sheet REGENERATED on-model (jump 5 / slide 4, verified vs idle_1); 30 legacy files archived to _src/legacy_offmodel (old idle/cast/diffuse/normal/walk/hurt/summon stills + all 18 spine-era rig part pngs; tools/refs parts → refs/legacy_parts); preload 'warlock'/'warlock_f0' fallbacks now point at warlock_idle_1 (every fallback path shows the correct body — kills the last old-sprite sources); rig part/JSON loads gated behind ?rig=1. (2) ENEMY FACING ROOT CAUSE: regenerated sheet frames all face RIGHT (SHEET_STYLE) while legacy stills face LEFT — crowd flip is now art-aware (animDriving → flip P.x<e.x, still → P.x>e.x). (3) BLACKDRAGON idle strobe: sheet regenerated with body/head LOCKED facing right, wings-only cycle (5/5/5/5, resliced/ingested). (4) PYRE MAGE MODERNIZED: the old pit fire/ice/bolt GROUND CIRCLES are gone — it now SHOOTS elemental projectiles with dedicated sheets: NEW anim_lightbolt (forked yellow-white lightning) + anim_coldbolt (ice shard w/ frost mist), fire reuses the firebolt comet; updFoeShots picks by s.elem; lightbolt/coldbolt added to all three folder resolvers (filed under fx/). Spend: 4 calls. Interrupted-task note: Enhancement #3 log entry stands; this entry covers the follow-up round.

## 2026-07-15 — ENHANCEMENT #3: JUMP · magical RUNE-STEP double jump · SLIDE (game3d) [Fable 5]

**game3d.** Side-scroller evasion kit: W/↑ JUMP (gravity 2600, apex ~162px; height rides its own P.air axis so the depth-plane sim/collisions are untouched — hero renders lifted, shadow shrinks with altitude); W again mid-air = RUNE STEP — a flattened violet summoning circle flashes beneath his feet with downward spark kick-off (render-only conjure, ~265px total, no triple); S/↓ SLIDE (0.38s committed low dash at 440px/s, 0.95s CD, honors held direction, low lean-back fallback pose). EVASION IS TIMED SKILL, not cooldown i-frames: hurtWarlock gates — airborne >60px = 'LEAPT', sliding = 'SLID UNDER'; both suspend the warlock-ejection pass so he passes THROUGH the enemy line. ART: 1 xAI call — warlock jump(5)/slide(4) sheet (crouch-launch-apex-fall-land; low slide w/ violet sparks), sliced + ingested into the warlock/ entity folder (the whole new pipeline used end-to-end); driver act mapping air>slide>hurt>summon>walk>idle. Tutorial gained the 3.6s movement caption. NOTE: the mount now also pins ingest_art.py — ran the assembled /tmp copy. Verified: 10-assert physics harness (apex, clean landing + counter reset, rune fires on 2nd jump only, no triple, slide window/CD/direction) ALL PASS. Spend: 1 call (~$5.7 remaining).

## 2026-07-15 — Enhancement #2 AUDIT: full lifecycle trace + resume-mid-pit fix (game3d) [Fable 5]

**game3d.** Traced every encounter end-to-end in the live file (all encStart/encComplete/encDone sites grepped host-side). Found + fixed ONE defect: quitting MID-PIT and reloading restarted the ladder at fight 0, replaying completed pit encounters — advEnterPit now resumes at the FIRST UNCOMPLETED pit id ("the Pit remembers — fight N/8" banner), clamped safely at the last index. Verified: resume-after-3-cleared starts at pit-4; all-done edge clamps. Everything else checked clean: pit completes mark BOTH advance and final paths, ?arena=1 legacy writes nothing to the registry, started-not-completed correctly re-triggers, camp/caravan gates + advWave starts + advEncounterCleared completes all wired.

## 2026-07-15 — ENHANCEMENT #2: encounter registry, 2-foe cap, persistent completion + SAVE SYSTEM (game3d) [Fable 5]

**game3d.** Central `ENCOUNTERS` registry (arena.html): every encounter formally defined — id, name, area, starting foes (min 1 / MAX 2; mid-fight summons exempt), summons, trigger, completion, post-access, story reqs, follow-up. Rows: pit-1…pit-8 (GRAVEHANDS trimmed to 2 graves), camp (now gunner+necro), caravan (now chain+gunner). COMPLETION IS PERMANENT: ADV.encState persists in localStorage (karridge_enc) — completed ids never re-trigger, foes never respawn, the Pit seals after pit-8 (existing one-time rule now registry-backed). Bonus this unlocked: a real SAVE SYSTEM (karridge_save: quest/mq/gold/potions/relics/boy/level, autosave ~4s + on completions) — reloading the page RESUMES in Karridge (no tutorial) with a "the ledger remembers you" banner; `?reset=1` wipes everything for a fresh playthrough. Verified: 7-assert node harness (trigger gate, permanence across simulated reload, save round-trip, pit chain persistence, reset wipe — ALL PASS). Docs: MAINTENANCE how-to (add-an-encounter checklist) + WIKI note.

## 2026-07-15 — ENHANCEMENT #1: sprites reorganized into per-entity folders (game3d) [Fable 5]

**game3d.** assets/sprites/ restructured per Hiro's spec: `warlock/` (+ `summons/<succubus|archsuccubus|dragon|claw_demon|shambler|bone_archer>/`, `forms/<lich|archdevil|demonlord>/`), `enemies/<type>/` ×14, `npcs/`, `fx/` — 1068 files moved, zero lost (1069→1069 recursive count; one stray bg_pit_far.png copy left at root). Code keeps FLAT sprite names everywhere via three synced resolvers: spritePath() in arena.html (preload rewritten data-driven, loadAnimFrames + rig-part paths resolved), ENTITY_DIR in ingest_art.py (writes into entity folders, anims.json rebuilt RECURSIVELY — verified byte-identical 109 sets pre/post), SPRITE_ENTITY_DIR in gen_sprites.py (entity_ref/part_ref resolve folders; /tmp runnable copy patched, gunner/archdevil refs verified existing). Manifests + rig JSONs stay at sprites root; _src/ stays flat (archive). Docs: layout + keep-three-maps-in-sync rule appended to tools/README.md + docs/MAINTENANCE.md. Verified: spritePath 9/9 mapping cases, preload block node --check, manifest identity, full anims.json existence walk (0 missing frames, 0 missing stills of 38).

## 2026-07-15 — Facing hysteresis, dynamic big-monster zoom, THE CROSSROADS journey map, one-time Pit (game3d) [Fable 5]

**game3d.** Hiro's 3: (1) enemy facing gets 14px hysteresis (no wrong-way flicker when the warlock crosses a foe; flip only outside the dead zone). (2) DYNAMIC ZOOM — when anything HUGE is on stage (world-height ≥1.4: dragons/beast/door/champ, a summoned dragon, or a transformed warlock) the whole cast eases to 0.78× world scale and back to 1.0 when they leave; implemented as a WORLD-scale factor (S.zoomK on hero/crowd/demon world heights), NOT camera zoom, because camera motion fights the pinned parallax — feet stay on the painted ground line. Harness: eases 1.0→0.782 with a beast up, back to 1.000 after it dies. (3) THE CROSSROADS: the pit gate now opens a DC-style JOURNEY MAP overlay (destinations: Pit / Karridge / West Trade Road (unlocks with mq1) / Varenholm + Ashenveil Academy shown LOCKED as next-chapter hooks; E closes). THE PIT IS A ONE-TIME CONQUEST — after quest≥3 it reads CLEARED and cannot be re-entered (repeat-run code retired in place, dormant). Verified: in-file zoom block + map-logic harness (pit locks after clear, 2 unlocked dests pre-mq1... westroad unlocks at mq1); tail markers all present via host Grep.

## 2026-07-15 — CHAPTER 1: the ORIGINAL pit main quest ported into the parallax world (game3d) [Fable 5]

**game3d.** The original game's main quest chain (quests.js mq1-mq5) now plays in arena.html after THE MISSING CHAMPION closes, tracked by ADV.mq: Marlow's LISTENING ROOM (50g) → NEW AREAS west of the village (BACK ALLEYS + WEST TRADE ROAD w/ teal ley-line node, 6 art calls: 2 full-bleed far layers, 2 keyed mid strips, npc_veiled + npc_quarryboy stills) → THE WAYSTATION field fight (capture team) → free the QUARRY BOY → THE VEILED WOMAN buyer beat (night tint falls) → THE NIGHT SHIPMENT caravan fight → the DRAGON EMPEROR at the well (Ankunyx's recorded line) → CHAPTER CLOSED +200g, Varenholm hook. 16 original voice clips copied/wired (quarry boy, veiled woman, Ankunyx, narrator waystation/emperor + 9 warlock lines used beat-for-beat). NEW SYSTEM: field encounters — advWave() spawns waves in ANY area, ADV.combat lets frame() run the full sim outside the pit, advEncounterCleared() advances the beat; pit ladder guarded from field waves. VERIFIED: full-file assembly agent (host Read splice over the mount pin, 3973 lines) → node --check PASS + all chapter anchors present exactly once. WIKI.md updated with the Chapter 1 table + next-chapter roadmap (Varenholm dancer, White Writ, Black Carriage, Academy). Spend: 6 calls (~$5.8 est. remaining).

## 2026-07-15 — Docs baseline: WIKI + MAINTENANCE + CODE_MAP; arch-devil idle smoothed; brute/grave feet; folders tidy (game3d) [Fable 5]

**game3d.** Maintenance round (Hiro: "almost at a good baseline to build parallax games from"). (1) ARCH DEVIL idle regenerated as ONE smooth flame cycle (body frozen, hellfire low→rising→high→falling — no more 2-phase strobe); counts idle4/walk5/claw5/bite4. (2) FEET SCAN across all 15 sheeted types: real culprits were brute + grave (their REFERENCE stills are waist-up crops, so sheets invented clipped legs) — both regenerated with full legs + below-feet margins, stale frames swept; stitch/chain/gunner mildly flagged → BUG-018. (3) Folders tidy: tools root = code + refs + folders only; art_in = DROP_ART_HERE.md + raw/ only. (4) DOCUMENTATION BASELINE written to game3d/docs/: WIKI.md (bestiary w/ real numbers, quest walkthrough, kit tables, economy, dev flags, voice map), MAINTENANCE.md (architecture contracts, 5 end-to-end HOW-TOs, pipeline commands, all hazards, budget/schedule workflow), CODE_MAP.md (~35 grepped anchors, file=3876 lines, retired-region + wrapper-pattern tables); README links them. Doc agent verified fact-sheet vs code (4 corrections: summon spawn 160px flat, blink 2.2s CD/4s stun, contagion tick tightens to 0.4s, herald ward 7s). (5) True code modularization deliberately DEFERRED → BUG-019 (needs a dedicated session + extended regression gate; mapped, not moved). Spend: 3 calls (~$6.6 est. remaining).

## 2026-07-15 — Zoom-out, real separation floors, portal/blink distance, summon spacing, hook/pyre/necro feet (game3d) [Fable 5]

**game3d.** Hiro's 5: (1) hook/pyre/necro sheets REGENERATED with feet-in-every-frame enforcement (agent, 4 calls — pyre's first try clipped the whole death row at the sheet edge, retried clean); resliced at observed counts, ingested, _src orphans swept. (2) OVERLAP root cause: hitbox radii (11-30px) are tiny vs drawn bodies (~150-250px) — separation now uses VISUAL floors (creature↔creature 0.28×HERO_PX, creature↔warlock 0.36×HERO_PX, full X-eject off the warlock), 2 relaxation passes + arena clamp; ALL melee reaches raised to ≥ the floors (foe generic/hound bite/grave, brute shove, zombie swipe, lich scythe, devil claw/bite) so strikes land from OUTSIDE the ring — weapon range, not body contact. Harness-verified floors hold. (3) PORTAL now crosses to the OPPOSITE side of the screen (mirror across centre; furthest foe yanked into the old spot; fires even with no foe = pure escape) + BLINK 150→260px. (4) Summons spawn 150-180px out (fiend 160, dragon 180 ahead, coven fans 150) — never on top of him. (5) ZOOMED OUT: HERO_PX 0.355H→0.27H (clamp 200-420) — every actor keys off it, so the whole world scales down together. Spend: 4 calls (~$6.85 remaining est.).

## 2026-07-15 — MvC HYPER CUT-INS replace camera cinematics; HP bars off; warlock-collision fix (game3d) [Fable 5]

**game3d.** (1) Hiro: camera zoom/pan "moves the warlock upward, out of place" — correct diagnosis: camera motion slides world actors against the PINNED parallax layers, unfixable cheaply in this view. Camera cinematics RETIRED (killCam keeps spark burst + hit-stop only; camFocus = flash). REPLACED with Marvel-vs-Capcom-style HYPER CUT-INS — pure screen-space overlay (scrim dims, diagonal speed-lines streak, the character portrait SLAMS in with overshoot + white flash + title card, world hit-stops underneath, sweeps out). Wired: ARCH DEVIL / LICH / DEMON LORD transforms (1.1s full cuts), every summon completion (0.48s assist-flash: fiend/dragon/coven), boss kills (beast/champ/door → 'FATALITY' cut). Works identically in village/road/pit because nothing touches the camera. (2) All floating HP pips/bars (foes + summons) hidden by default — ?hpbars=1 restores. (3) separateActors now also ejects creatures OFF THE WARLOCK (full X-push on the creature, P never shoved; margin outside melee stop distances — harness-verified brute+hound un-stack in one frame). Earlier same-day: post-quest repeat Pit runs (+35%/run, scaling gold), immortal-coven fix (herald never-expire rule removed), green arch fire, locked arch-devil idle, kill-word variety, Bellow taunts. Budget note: $7.15 xAI remaining (~$0.07/call) — logged in PENDING.

## 2026-07-13 — Post-quest Pit runs, de-overlap, immortal-coven fix, kill-cam cinematics, green arch fire, arch-devil idle redo (game3d) [Fable 5]

**game3d.** (1) NOWHERE TO GO fixed: after the quest closes, the Pit reopens as REPEAT RUNS — every re-entry +35% enemy hp/damage, payouts scale (120+90/run gold + potion), objective points back east; Marlow's quest-complete banner announces it. (2) separateActors(): pairwise de-overlap across all living summons+foes each frame (X-biased push; harness-verified). (3) IMMORTAL COVEN root cause: the old pit "herald succubi never time out" gate — removed; only the phylactery dragon is timeless, everything else obeys the 4s clock. (4) CINEMATICS RETURN: killCam on every kill (punch-in + pan to the slain foe + spark burst + settle, layered over hit-stop/slow-mo), camFocus() implemented (real push-in for the lich rise etc.), kill-word variety, and Bellow-style TAUNTS on every slice fight intro. (5) ARCH succubi now throw SHEOL-GREEN fire (greenbolt art keyed by d.arch). (6) Arch-devil sheet regenerated with a LOCKED side-on stance (idle varies only by flame flicker/breathing — no more back-and-forward glancing); resliced/ingested 4/4/4/4. Spend: 1 call (~40 total).

## 2026-07-13 — BUG-017: the REAL size bug — per-frame sprite recreation (game3d) [Fable 5]

**game3d.** "Enemies still tiny" survived the table bump because the table was never the problem: the foe-crowd pool recreated every sprite EVERY FRAME (texture.key vs base-key check can't match mid-anim), each time scaling against the 512px base still → permanent ~0.4×. Proved by building a minimal live Phaser 3.80 harness (chromium headless) that showed the scale math correct in isolation, which left only the pool churn. Fix: pool slots track o._base; sprites persist; scale reads the live frame height. Demon draw got the same live-frame fix + the 0.85 "support rank" shrink removed. Detail: game3d/BUGLOG.md BUG-017.

## 2026-07-13 — Playtest round 3: sizes, hex rework + contagion, summon recast cycle, tutorial gate, green comet, arch-devil sheet (game3d) [Fable 5]

**game3d.** Hiro's 7 items: (1) SIZE PASS — world-height table bumped (humanoids ≈ warlock: gunner/pyre/necro 1.05, hook/stitch 1.0, hound 0.8→1.0, brute 1.45, succubus 0.8→1.05 humanoid, arch 1.1, clawfiend 1.35, lich 1.15) + crowd scale now reads the LIVE anim-frame height (never a stale base texture). (2) HEX: CD 10s→2s everywhere, STACKS for everyone (+15 tick per reapplication), and CONTAGION — a foe dying hexed passes the rot to the nearest foe at DOUBLE tick damage per jump, with a violet tracer + '×2/×4/×8' popup. (3) Stale 'ENTER THE PIT' hint cleared on pit entry. (4) Tutorial no longer skipped by the focus click/audio-unlock keypress — input ignored for the first 3s. (5) SUMMONS REWORKED: one summon per ~3s cast, cycling fiend→dragon→coven, 3s recast cooldown (heavyCD gate in doHeavy), all summons 4s life; coven cast still triggers ARCH DEVIL (now 6s base, evos extend 10/16); reaper's shamblers/archers 8s (incl. infection-risen). (6) NEW anim_greenbolt sheol comet — arch-succubus/black-dragon green fire gets its own projectile art (selected by b.col, no tint-over-ember); succubus visibly casts her MEND. (7) ARCH DEVIL got a real 4-row sheet (idle/walk/CLAW/BITE, on-model) — claw dash + bite-heal play their one-shots via heroPlayOnce. Arch-warlock/arch-reaper evolution forms queued as BUG-016 [NEEDS-FABLE]. Verified: channel-cycle harness (brute→dragon→succubus), block syntax checks, greenbolt/archdevil contact sheets reviewed, tail intact. Spend: 2 calls (~39 total).

## 2026-07-13 — BUG-001 fixed: solo HUD "— DEFEATED —" ghost hidden (game3d) [bugfix-schedule]

**game3d.** updateGGHud's no-foe fallback split: "— DEFEATED —" now only shows when a roster exists and is all dead (gauntlet-cleared state kept); with enemies.length===0 (?solo=1) the foe name blanks and the whole right fighter block gets visibility:hidden, reversibly restored when a foe/roster/town state returns. Cosmetic-only, left side + town branch untouched; moot in normal play since the HUD stays hidden per BUG-010 — visible under ?hud=1. Verified: 15-assert node harness on the verbatim sed-extracted function, ALL PASS + Read-tool tail check (`</html>` intact at 3715 lines; mount again pinned at 200473 bytes — display hazard only). Detail: game3d/BUGLOG.md FIXED. Next in queue: BUG-002 (stale foeSprite visible:true; BUG-009 stays NEEDS-FABLE).

## 2026-07-13 — BUG-007 fixed: the phylactery holds — dragon-shielded lich, LAST DRAGON FALLS (game3d) [bugfix-schedule]

**game3d.** The bone dragon is now truly the lich's phylactery (pit.js 635/1488/639-641 port): while risen, hurtWarlock returns early with a 'PHYLACTERY' popup whenever any dragon summon lives (flash/shake still read the hit, hp never moves), and when the LAST dragon falls the unsummon path fires 'THE LAST DRAGON FALLS — the lich can BLEED now' + 'MORTAL AGAIN' and he's hittable again (next death FINAL per BUG-005). The life-freeze half (dragon exempt from d.life-=dt while P.lich) turned out to be already in-file from an earlier port — verified rather than re-landed. Verified: 20-assert node harness on verbatim sed-extracted fragments, ALL PASS + Read-tool tail check (`</html>` intact at 3711 lines; mount pinned at 200473 bytes again — display hazard only). Detail: game3d/BUGLOG.md FIXED. Next in queue: BUG-001 (solo HUD "DEFEATED" ghost; BUG-009 stays NEEDS-FABLE).

## 2026-07-13 — BRAWLER UNIFICATION + road-boot tutorial + ability FX + repo cleanup (game3d) [Fable 5]

**game3d.** Hiro's 7-item batch: (1-BIG) SIM/RENDER UNIFIED — root cause of "the wolf would not die" and "invisible attacks": the duel shell rendered a theatrical foe while updFoeAI fought from sim positions; duel path + cosmetic chargers retired, every foe now renders full-size AT its sim position with hurt/attack/walk/idle/death anims, telegraph tints, HP pips; updFoeAI is the one authority (BUG-013). (2) Enemy projectiles ride animated firebolt/hexbolt sprites (+70px lift); hound bite arc visible; kill beat moved into killEnemy. (3) Game now BOOTS ON THE ROAD with a hands-off warlock tutorial (narrator intro clip + captioned hex/blink/portal/summon demo; ANY key takes control) — village unchanged to the west (BUG-014). (4) NEW FX: anim_blinkwave (hex-lightning stun discharge at the blink departure point, real 230px radius) + anim_wardaura (shield bubble while wardT>0) — 2 xAI calls, luminance-keyed (BUG-015). (5) Cleanup agent: tools/ organized into checks/ + sheets/ + README.md; art_in purged of 483 already-ingested frames — the bare-ingest resurrection hazard is GONE (art_in now only DROP_ART_HERE.md + raw/). Verified: syncFoeCrowd node --check, scripted tutorial-timeline run, FX contact sheets reviewed, tail intact. Spend: 2 calls this round (~37 total against the $9.86).

## 2026-07-13 — BUG-006 COMPLETE (3/3): lich clock landed, archers verified, kit closed out (game3d) [bugfix-schedule]

**game3d.** The lich kit is done. This run landed THE LICH CLOCK in arena.html's frame() (pit.js:2279 port, before the living summon ladder): while risen, 6s summonZombies · 8s summonArchers · 12s exitLich (back to the living); lichlord extension skipped (evolutions unwired). Discovery: piece 2 (summonArchers + archer AI + arrow branch + draw wiring) was ALREADY in the file from a prior run that never logged it, and the K=FADE reroute was already in doParry — both verified this run instead of re-ported. Judgment (deferred from piece 1): the clock passes NO force flag, so ?solo=1 stays claw-fiend-only per the standing decision (only the phylactery dragon bypasses); the 12s resurrection still fires in solo. Verified: 21-assert node harness on verbatim sed-extracted fragments, ALL PASS + Read-tool tail check (`</html>` intact at 3595 lines; mount again served it truncated at 2755 — display hazard only, not "repaired"). Detail: game3d/BUGLOG.md FIXED. Next in queue: BUG-007 (phylactery).

## 2026-07-09 — Playtest round 2: HUD off, 12 sheets redone expressive, summons ungated + de-clung (game3d) [Fable 5]

**game3d.** Hiro's four fixes: (1) GG duel HUD hidden by default (?hud=1 restores) — it overlapped the journal. (2) Twelve sheets regenerated from scratch for expressiveness (agent, 17 xAI calls): clawfiend/bonedragon/blackdragon/succubus/archsuccubus/gunner/hook/necro/stitch/brute/beast/lich — every frame a distinct silhouette, dragons/succubi idle = full hover wingbeat cycles, full-body margins (brute's half-body idles took 3 attempts), stale frames swept, anims.json verified. (3) All summon rungs live at any level (lvl 3/5 gates removed; solo stays fiend-only). (4) Coven de-clung: succubus no longer orbits the warlock — holds her spawn point with no enemies (ranged mend unchanged), advances to a firing band when foes exist; matches the fiend/dragon/shambler/archer spec. Verified: extracted-updDemons node harness 4/4 + BUGLOG entries BUG-010/011/012. Cumulative xAI spend this stretch ≈ 35 calls of the $9.86.

## 2026-07-09 — DC ONE-SHOT: "The Pit of Karridge" vertical slice shipped (game3d) [Fable 5]

**game3d.** Hiro's bold ask executed in one pass: arena.html now boots into a full Dragon's-Crown-style slice by DEFAULT (`?arena=1` = legacy 20-fight gauntlet, `?solo=1`/`?rig=1` unchanged). VILLAGE HUB (Karridge: chapel/smithy/inn NPC stills, THE WELL, 4-layer parallax — painted far skyline, keyed building row, 1:1 scrolling ground, near prop plane IN FRONT of the actors) → walkable OLD ROAD (dead trees/gibbet parallax) → THE PIT (8-fight authored ladder, 10 enemy types, BEAST OF KARRIDGE boss) → win → return → quest close. QUEST reconstructed from the ORIGINAL pit game's recorded voice clips (8 mp3s copied to game3d/assets/voice, played by manifest id + banner subtitles; city/arena music ported, ducks under voice): Marlow's missing champion DREN — "men who run, run IN boots"; reward relic DREN'S BOOTS (+15% stride) + gold/potion loot (E drinks in the pit; Marlow sells at 25g with the five-silver gag when short). ART (agent, 18 xAI calls, ~0 retries): 9 enemy sheets w/ idle/walk/attack/HURT/DEATH rows (hound hook chain pyre grave stitch necro brute beast), 3-row lich sheet, 5 village/road bg layers, 3 NPCs. FIXES rolled in: foe hurt/idle anim selection, slower telegraphs + attack cadence (Hiro), heroPlayOnce reverts into the form idle SET (kills the old-sprite flash), bone-dragon GAS cloud visible again (soft sheol draw, no pit-era circle). VERIFIED: full quest-loop node walkthrough (18 asserts, ALL PASS) + syntax check on the block + host-side tail confirm (</html> @3542). NOTE: the shell mount still pins arena.html at 200473 bytes — the ADV block is beyond the pin; use the Read tool.

## 2026-07-08 — BUG-006 progress (1/3): lich SHAMBLERS ported — summonZombies + zombie AI + infection (game3d) [bugfix-schedule]

**game3d.** First piece of the lich kit landed in arena.html: summonZombies() (3 shamblers, hp 25+4·kills, ORIGINAL 24s life, base numbers — evo multipliers skipped per entry), zombie branch in updDemons (hold/return to spawn with no enemies, shamble-seek, infecting d8+4 swipe, _atkPulse sync), infectT decay + killEnemy infection-rise ("IT RISES" — infected foes come back as shamblers), and draw wiring (shambler flipbooks, BASE_R 13, ground lane). Judgment noted: summonZombies(force) mirrors the SOLO gate; the later channel-clock run decides force. Verified: 18-assert node harness ALL PASS + draw-ternary fragment check + Read-tool tail check (`</html>` intact at 3257 lines; mount again served it truncated at ~2783 — display hazard only). BUG-006 stays OPEN; next piece: bone archers. Detail: game3d/BUGLOG.md progress note.

## 2026-07-08 — BUG-005 fixed: warlock death→LICH rise ported, hp-floor removed (game3d) [bugfix-schedule]

**game3d.** hurtWarlock() in arena.html no longer floors hp at 1 — a lethal hit now runs the original pit.js:1506-1515 pipeline: 3s kneel (paralyzed + rooted, 'THE PACT HOLDS' on any verb), phylactery bone dragon summoned (bypasses the ?solo=1 gate via a force flag), then enterLich() from the new frame-loop countdown. Lich death is FINAL: 'SLAIN' + the current fight restarts after 2s (no lose() flow in the shell yet). Pit-faithful note: dying again DURING the kneel is also final, as in the original. Verified: 11-assert node harness + verbatim-fragment syntax check + Read-tool tail check (`</html>` intact at 3217 lines; mount again served it truncated — display hazard only). Detail: game3d/BUGLOG.md FIXED. Next in queue: BUG-006 (lich kit, split across runs).

## 2026-07-08 — BUG-004 fixed: summon lifespans set to ~5s (game3d) [bugfix-schedule]

**game3d.** Per Hiro's spec, all summon life values in arena.html summonDemons() set to 5 (were brute 18 / dragon 15 / succubus 14) and the demon draw block's lifeFrac divisor simplified to d.life/5 so the life ring/fade reads correctly. Verified via standalone node harness (spawn/half-life fractions correct for all 3 types) + Read-tool tail check (`</html>` intact at line 3184; shell mount again served the file truncated — display hazard only). Detail: game3d/BUGLOG.md FIXED. Next in queue: BUG-001 (solo DEFEATED HUD).

## 2026-07-08 — BUG-003 fixed: summons freed from the assist lane, sim-driven movement (game3d) [bugfix-schedule]

**game3d.** Removed the SIDEON ASSIST-LANE/MIN_D render override in arena.html's demon draw block — summons now render at their sim x (depth rule, per-type vertical motion, and _atkPulse-synced attack anim kept); summonDemons() records homeX/homeY and updDemons() makes brute+dragon hold/return to the spawn point with no enemies and seek+attack the nearest on their own otherwise. Verified via standalone node harness (7/7 pass) + Read-tool tail check (`</html>` intact; shell mount still serves the file truncated — display hazard only). Detail: game3d/BUGLOG.md FIXED section. BUG-004 (5s lifespan) is next in queue.

## 2026-07-08 — Warlock-focus batch verified complete by scheduled finisher [schedule]

**game3d.** All completion checks PASSED on first run: anims.json parses with all 19 summon sets at exact counts (clawfiend 5/5/5, bonedragon 6/6/6/6, blackdragon 5/5/5/5, succubus 5/6/6/6, archsuccubus 5/5/5/5); all 5 sheet PNGs in game3d/tools/ open cleanly with PIL (1248×832 each); arena.html contains both "const SOLO = !" and "PIT-VFX CLEANUP" markers. Syntax check skipped per the baked-in caveat: the shell mount shows arena.html tail-truncated (~line 2803 of 3164), but the Read tool confirms the real file is intact and ends with `</html>` — the known OneDrive/mount display hazard, nothing to fix. No changes made. Disabling this task (`game3d-warlock-focus-finisher`) now — per the standing note that self-disable is unreliable, Hiro should still manually confirm it's off in the Scheduled sidebar.

## 2026-07-17 — THE DANCER OF VARENHOLM (mq6) implemented; road showcase retired (game3d) [Claude/Sonnet]

**game3d.** Ported the next Chapter 1 epilogue beat from the original `game/` (quests.js `mq6-the-dancer`/`varenholm` block) into game3d's `ADV` state machine. New `varenholm` area (coach-only, unlocks on the map at mq>=6); on first arrival plays the coach/performance narrative once (real recorded VO, copied over from `game/assets/voice/` — `05343e18`, `25c2d96e`, `5148e2e8`, `d414f8f8`, `91ed3cf8`, `97b2c046`, `eb7f25f8`, `f2cedd04`, none of these existed in game3d's voice folder before), then Cookie (the Dancer) is a real, findable, idle-animated NPC (using her existing `dancer_idle` anim set — no static `npc_cookie.png` exists, so she's built off her real idle frame instead, matched by name not texture key) who hands off the SALTCELLAR job, which chains into a 2-foe fight, then an automatic 4-foe cult ambush, then mq=7 and a scripted return to Karridge. Retired the old render-only "dancer showcase" (background cameo on the first road, cycling anim states with no interaction) and repointed its "she reacts to a kill" easter egg at her own Varenholm fights instead. **Two deliberate substitutions, flagged for Hiro:** (1) no "wight" enemy type exists in the roster — substituted `grave`+`skel` for the saltcellar fight; (2) Varenholm has no background art yet — reusing the village art as a placeholder (`bg_village_*`), and the whole town is one location rather than a separate canal sub-area, to keep this pass scoped. Real Varenholm art (Crown Quarter / Civic Auditorium look) is a clean follow-up if wanted — small, bounded Grok spend (2-3 images), not started without asking first. Verified by direct re-read of every edited block (full `node --check` still blocked by the bash mount truncation on this file) — all edits follow existing patterns exactly (ENCOUNTERS registry, `advWave`/`advSay`/`encDone` usage, NPC struct shape) so risk is low, but this hasn't been run in a live browser yet since this environment has no headless browser installed. Next: Hiro should playtest this in the real game before anything further builds on top of it.

## 2026-07-17 — Debug encounter disabled at boot; Chapter 1 alley softlock investigated (game3d) [Claude/Sonnet]

**game3d.** First direct implementation pass (no Fable). (1) `TEST_ENCOUNTER` (the 1-of-each-foe debug gauntlet that booted every session, `arena.html` ~3602) flipped from default-on to default-off — real players now boot into Chapter 1, not the review gauntlet; `?test=1` brings it back for foe-review sessions. Verified by direct re-read of the edited line (full `node --check` not possible — this repo's bash mount still truncates `arena.html` well before this line, a known issue; the edit itself mirrors the existing `?rig=1`-style flag pattern). (2) Investigated Hiro's report of getting stuck after THE VEILED WOMAN (the alley NPC, Chapter 1 mq3→4, "THE BUYER" beat): traced the full mq3→4 (`npc_veiled` handler) and mq4→5 (`caravan` encounter trigger, gated on `ADV.mode==='westroad' && wx<=980`) code paths — both read as logically correct in isolation; no confirmed bug found yet. One real requirement worth surfacing: after talking to the Veiled Woman (who's in the `alleys` area), the player must physically walk back WEST through alleys into `westroad` to reach the caravan trigger zone — easy to read as "stuck" if not obvious. Also nearly mis-flagged a false alarm: an earlier read of `advSetArea`/`advMap` appeared to show literal `\` where `//` comments should be; re-read via the Read tool confirmed it was a one-off display glitch, not real file corruption — logging this as a reminder that even the Read tool isn't 100% infallible on this repo, re-verify before treating a suspicious read as fact. Next: need Hiro's exact description of what "stuck" looks like (can't move? dialogue won't close? screen stays dark? no encounter ever spawns no matter how far west?) before guessing at a fix.

## 2026-07-07 — WORKFLOW CHANGE: Fable no longer available, Claude/Sonnet now implements directly [Claude/Sonnet]

**game3d + process.** Hiro's subscription no longer includes Fable access. From here on, this session does the actual implementation (Edit/Write/bash on `game3d/` directly) instead of drafting prompts for Fable to execute — the relay step is gone. Checked before proceeding: `game3d/tools/xai_key.txt` exists on disk, so Grok generation is runnable directly if needed. No headless browser (Playwright/Puppeteer) installed yet in this environment — Fable's screenshot-based in-engine verification isn't available until that's set up; verification for now relies on direct file/code inspection (grep, node --check, opening images) same as the trust-but-verify checks already used on Fable's work all session. Built and saved a project skill (`fable-oversight`) capturing the verification/scope-discipline lessons from this session, per Hiro's request — still applicable now even without a separate agent to relay to, since the discipline (verify before claiming done, audit before spending, confirm exact paths, ask on real forks) applies to direct work too. Continuing to log every change here with signature, one scoped step at a time, same as before.

## 2026-07-07 — Enemy pause lifted for one thing: Stitch facing bug -> full sprite QA sweep (game3d) [Claude/Sonnet, not Fable]

**game3d.** Hiro reported Stitch faces the wrong way sometimes, suspects it's the small sprite sheet — same class of problem the Warlock had pre-Phase-2 (most enemies share the low frame counts: stitch's 5 sets are all 3-4 frames). Rather than let a full "QA every enemy, redo as necessary" sweep run unchecked (real Grok $ across ~14+ enemy types), confirmed with Hiro directly: yes, lift the enemy pause for this specific audit+fix pass (does not reopen Phase 3/4 summon-AI/VFX work). Sent Fable an audit-first prompt: diagnose exact cause per enemy (facing-logic/art-convention mismatch vs genuinely-too-few frames) before spending anything on regeneration. Detail: plan doc exception note under `## SCOPE NARROWED 2026-07-07`.

## 2026-07-07 — Scope narrowed: enemies + summons disabled, Warlock-only focus (game3d) [Claude/Sonnet, not Fable]

**game3d.** Hiro flagged that a lot of systems didn't carry over cleanly from the original top-down `game/` port and wants a cleanup pass. Rather than chase that directly, scope narrows first: all enemy spawning and summon casting get disabled behind a reversible dev flag so work + playtesting focus entirely on the Warlock (his sheet-flipbook locomotion, missing attack/hurt/summon-cast rows, any Pit-carryover bugs specific to him). Not a deletion — Phase 3/4's summon-facing work resumes once the Warlock is in good shape. Detail: plan doc `## SCOPE NARROWED 2026-07-07`. Next: Fable disables enemies/summons, inventories what didn't carry over (report, don't blind-fix), continues Warlock polish.

## 2026-07-06 — Decision confirmed: no 3D pipeline, staying 2D + fully agentic (game3d) [Claude/Sonnet, not Fable]

**game3d.** Hiro asked about Tencent Hunyuan 3D (image-to-3D + auto-rig + AI motion, API-callable) and Cascadeur (AI-assisted 3D keyframe animation, real Python API) as possible strategy changes after the 2B.5 recheck found real rig defects. Both ruled out for the same reason, confirmed directly by Hiro: the game is 2D, styled like Dragon's Crown Pro, not 3D — either tool would mean rendering a 3D mesh through a custom stylized shader pipeline to fake a 2D painterly look (a multi-year investment class of problem, see Guilty Gear Xrd), not a plug-in fix. Hiro also confirmed he has no interest in personally learning 3D animation as a means to this end. Locked: stay on the programmatic cutout rig, 100% Grok + code, ceiling accepted as "good 2D cutout animation," not fighting-game-tier. Detail: plan doc `## DECISION CONFIRMED 2026-07-06`. Back to the two live unblocks: save `ref_warlock_greenlit.png`, then Fable's joint-overlap/pivot fix.

## 2026-07-09 — Enemies + all summons back ON by default; solo now opt-in (game3d) [Fable 5]

**game3d.** Per Hiro: SOLO default flipped back — plain URL runs the full gauntlet with every summon rung live (dragon/coven/devil included); `?solo=1` keeps the warlock playground (claw fiend only there). The game3d-bugfix schedule's standing-decisions block updated to match so it never "fixes" the default back. Verified: flag logic tested, plain URL → SOLO false.

## 2026-07-09 — Summon behavior re-specced by Hiro; queued as BUG-003/004 (game3d) [Fable 5]

**game3d.** Hiro's playtest verdict on the claw fiend: summons must NOT ride an assist lane pinned to the warlock — new spec: hold their SPAWN POINT when no enemies exist, seek + attack enemies on their own when they do, and live only ~5s each. This supersedes the Phase-3 "assist lane + synced dart" design (distance/depth rules survive; the lane positioning goes). Queued as BUG-003 (movement/AI, with code anchors) and BUG-004 (5s lifespan + display divisors) at the top of game3d/BUGLOG.md for the game3d-bugfix schedule.

## 2026-07-09 — Flat hex, claw fiend un-gated for playtest, BUGLOG + bugfix schedule live (game3d) [Fable 5]

**game3d.** Three per Hiro: (1) SIDEON hex now flies FLAT down the lane (direction = facing sign, vy=0 — the mouse angle had it flying "in 3D" up/down the depth plane) at his mid-section (BOLT_LIFT 72→105); top-down fallback keeps the free angle. (2) CLAW FIEND un-gated in solo for playtesting: Q-channel works again, rung 1 summons the fiend (now wearing its new sheet sets: idle5/walk5/attack5); rungs 2/3 (dragon/coven/enterDevil) remain solo-gated with a 'SOLO: FIEND ONLY' popup. All edit logic verified in standalone node harnesses. (3) Bug-fixing loop stood up: `game3d/BUGLOG.md` (OPEN/FIXED queue, rules in the header, seeded with 2 known cosmetic solo-HUD bugs) + scheduled task `game3d-bugfix` every 10 min — fixes ONE topmost bug per run with minimal-change/verify/document discipline, exits instantly when the queue is empty, retags ambiguous items [NEEDS HIRO]. Standing caution: default-model schedule (pick Fable via the Scheduled-tasks page if desired) and check it's actually off if the queue stays empty long-term.

## 2026-07-09 — Bolt round 2: bigger + higher hex, dedicated FIREBALL sheet for succubus/dragon bolts (game3d) [Fable 5]

**game3d.** Per Hiro's screenshot: BOLT_LIFT 58→72 and anim-bolt size 55→92px+ (max(92, r·17)) — the swirl was the sheet art all along, just dot-sized (and with no foe to aim at, the bolt follows the mouse angle, which was pointed at the floor). Also generated `tools/sheet_firebolt.png` autonomously (6-frame blazing comet-tail fireball on black), sliced+ingested as the `firebolt` set: kind 'fire' bolts (succubus/dragon fireballs) now play their OWN comet anim, hex keeps the violet swirl, `b.col` tints handle arch/sheol green, arrows stay on the glow orb. Wiring validated standalone under node (per-kind anim selection, tint rules, lift, fallbacks). Scope note: gun bullets / mage spells / breath cones deferred per Hiro — same methodology, apply when those systems are back on.

## 2026-07-09 — Hex bolt: floor-skating fixed (BOLT_LIFT) + animated sheet projectile (game3d) [Fable 5]

**game3d.** Hiro spotted the hex "ball rolling across the floor" — root cause is a true pit remnant: the top-down sim flies projectiles at the caster's ground-plane y, which in side-on view IS the floor. Fixed render-only with a BOLT_LIFT (58px, matching the cast origin) applied at all three draw sites: bolt sprite, fxTrail motes, fireBurst impact bloom; sim/hit detection untouched. He also wanted a real bolt animation — generated `tools/sheet_hexbolt.png` autonomously (one Grok call, 12 frames of swirling violet arcana on black), luminance-key sliced the top row → `hexbolt` 6-frame set ingested → hex/fire bolts now play the swirling sheet anim (ADD blend, velocity-oriented, ember tint for fire) with the glow orb kept as arrow/fallback. Verified: all 5 BOLT_LIFT sites confirmed on host; the new draw block validated standalone under node (anim path, orb path, lift, reaping). Vibe check: cast a hex — the bolt should fly at staff height as an animated violet swirl.

## 2026-07-08 — Finisher schedule created per Hiro (10-min watchdog, self-disabling) [Fable 5]

**Infra.** Per Hiro's instruction, created scheduled task `game3d-warlock-focus-finisher` (every 10 min): verifies the warlock-focus batch (19 summon anim sets at exact counts, 5 sheets readable, SOLO default + VFX-cleanup markers in arena.html, syntax check with the mount-truncation caveat baked in), logs to PROJECT_LOG.md, fixes only narrow failures with existing tools, and DISABLES ITSELF once all checks pass (or after two unfixable failures, with a NEEDS HIRO note). Cautions per the 2026-07-04 post-mortem: 10-min cadence is what burned the quota before — this one is self-terminating and cheap per run, but self-disable has historically been unreliable, so MANUALLY CHECK it's off (Scheduled sidebar) when back; it also runs on the app-default model, not Fable (model choice is only settable in the Scheduled-tasks page UI).

## 2026-07-08 — Autonomous art loop: solo default + pit-VFX purge + 5 summon sheets generated/ingested without Hiro (game3d) [Fable 5]

**game3d.** Big one. SOLO now default (?solo=0 restores gauntlet; verified headless, zero errors). Old top-down VFX draws removed per Hiro (dot fireballs → painterly glow-orb bolts for all kinds; green-circle breath + gas-circle draws gone; sim untouched). And the key unlock: api.x.ai is reachable from THIS sandbox (the "local-only" constraint was the old scheduled sandbox) — new `--sheets` mode generated complete labeled sheets for all 5 summons in one Grok call each, anchored to their approved sprites; auto-sliced (102 frames), stale frames purged (delete permission granted), ingested; anims.json verified for all 19 new sets. Summon art is banked but invisible (solo default) — breath/fireball/mend wiring is future summon-phase work; old *_attack sets remain as the engine hook until then. Sheets archived in tools/. Detail: plan STATUS.

## 2026-07-08 — Hurt row in; warlock sheet kit complete (game3d) [Fable 5]

**game3d.** Hiro's hurt sheet (6 labeled frames) grid-sliced, spot-checked, ingested (3-keypose set → 6 green-lit frames; figure fraction fine, no shrink override). Wired as a one-shot on the flash edge — the flash window is shorter than the arc, so looping would have cut it mid-stagger; ordered before the summon-release branch so a hit visibly interrupts the channel. node --check PASS + driver-order verified. Warlock sheet kit now complete: idle/walk/walkb/hex/portal/phased-summon/hurt. Next: Phase 4 (VFX) or summon-systems resume, on Hiro's go.

## 2026-07-08 — SOLO playtest flag + port-gap inventory (game3d) [Fable 5]

**game3d.** Hiro scope change executed: `?solo=1` dev flag gates ALL enemy spawning + summon casting (3 choke points, reversible, nothing deleted) — verified with node --check, runtime probes (enemies/demons = 0, Q no-op) and a headless empty-arena screenshot; zero errors. Then a read-only port-gap inventory (game/ → game3d): headlines are the unported lich death-pipeline + lich raises, Demon Lord/arch-succubus M8 deferrals, hex slow/contagion, channel interruption (now implementable), per-form HP, inert evo stat scaling, missing meta layer (potions/pause/options/music/autopilot/share), HUD gaps (cooldowns/boss bar/stats), leftover boot TEST stand-ins (forced lvl 5), the 1-real-foe wave architecture, and a stale parity checklist contradicting the live code. Full detail in the plan STATUS + chat. No fixes applied per instructions. Next: warlock hurt/attack rows (hurt wires zero-code once art lands; old hurt art is style-mismatched).

## 2026-07-07 — Summon shrink fixed; cutout rig officially retired by Hiro (game3d) [Fable 5]

**game3d.** The summoning shrink was the world-height contract fitting the eruption-tall canvases into the character's box (figure = 0.703 of canvas vs 0.923 idle, measured) — hero scale now applies a figureFrac override (0.761) for summon frames so the body stays constant while effects tower past. node --check PASS. **Decision recorded: Hiro is not going back to the rig — sheet flipbooks ARE the art direction.** Rig code/data stay dormant behind ?rig=1 as salvage (future tech-debt cleanup, no prompt spend now); 2C permanently closed. Next: Phase 4 (VFX) on his go.

## 2026-07-07 — Phased summon per Hiro's spec: intro 1-5, hold-pulse 5-6, release 6-7-8 (game3d) [Fable 5]

**game3d.** Summon anim redone from Hiro's 20-frame summoning sheet exactly as he specced (too fast + funny looping before): grid-sliced frames 1-8 (the sheet's detached effect fragments made blob-slicing unusable — used the sheet's own column pitch; labels stripped), ingested over the old set. Engine: three phase anims (intro once @9fps, 5-6 pulse @4fps while the channel holds, 6-7-8 outro once on release via the castTimer-owned heroPlayOnce; pulse self-resumes after interruptions). node --check PASS, wiring grep-verified. Rest of the sheet intentionally unused per Hiro. Next: Phase 4 (VFX) on his go.

## 2026-07-07 — Abilities sheet in: summon/hex/portal anims live (game3d) [Fable 5]

**game3d.** Hiro's second sheet (summon/hex/portal, 8 frames each) sliced, spot-checked and ingested — summon upgraded 3→8 frames (already wired to the channel beat), hex + portal are NEW sets wired via a new `heroOneShot()` (one-shot playback timed to the set's real length, castPose ownership contract, rig mode unaffected). HEX cast now plays the sheet anim instead of the old still; PORTAL plays a blink beat on the doParry swap. node --check PASS, wiring verified. Next: Phase 4 (VFX) on Hiro's go; hurt/attack rows + cast-wide sheets open on the art side.

## 2026-07-07 — ✅ Warlock animation APPROVED by Hiro ("much better!") — sheet flipbook is the way (game3d) [Fable 5]

**game3d.** 2B.5 gate cleared: Hiro approved the in-game Warlock running on the green-lit SHEET flipbook (his own approved art, one-generation consistency, sliced via `tools/slice_sheet.py`, played by the Phase-1/2 machinery). Decision record in the plan STATUS: sheets supersede per-frame generation AND the old "too rigid" rejection; the cutout rig stays behind `?rig=1` as the attack-anim candidate; 2C (rig polish) deferred as superseded. Remaining art gaps: warlock attack/hurt/summon action rows; cast-wide sheets for summons/enemies (deferred, needs Hiro's go). Next per plan order: Phase 4 (VFX overhaul), then Phase 5 (traversal). Awaiting go.

## 2026-07-07 — "Sheet not visible" fixed: flipbook now default, rig opt-in, cache-buster added (game3d) [Fable 5]

**game3d.** Hiro couldn't see the sheet in game — root-caused to two stacked causes: the rig was still the default driver (sheet only ever showed on `?rig=0`), and in-place frame overwrites (same filenames) meant the browser cache served the OLD walk frames anyway. Fixed in `arena.html`: sheet flipbook is the default, rig via `?rig=1`, and a boot-time `?v=` cache-buster on anims.json + rig JSONs + every anim frame load. node --check PASS. Vibe check: plain URL (one Ctrl+F5) = sheet; `?rig=1` = rig.

## 2026-07-07 — Locomotion sheet sliced + ingested; rig-vs-flipbook A/B ready (game3d) [Fable 5]

**game3d.** Hiro saved the green-lit locomotion sheet; ran `slice_sheet.py` + ingest from the sandbox: 21 registered frames in (idle x5 fwd-loop @5fps, walk x8 fwd-loop @14fps, walkb x8 stored for later — faces left as drawn). Frames spot-checked (right-facing, clean key, on-model), anims.json + per-set canvas registration verified. The existing Phase-1/2 machinery plays them with zero new code. The 2B.5 decision is now a clean A/B on his own art: default URL = cutout rig, `?rig=0` = sheet flipbook; hybrid (sheet locomotion + rig attack) is a small driver change if he wants both. Detail: plan STATUS.

## 2026-07-06 — Strategy scan (open-source 2D animation tools) + sheet slicer built (game3d) [Fable 5]

**game3d.** Hiro asked what tools we're not using (incl. Chinese/GitHub) AND produced a green-screen locomotion SHEET of the green-lit warlock (idle x5, walk x8, walk-back x8) — one generation, perfect cross-frame consistency, which is exactly the property per-frame generation could never give us. Scan results (detail in chat): ToonCrafter (Tencent ARC, anime in-betweening, ~10GB VRAM forks), pose-driven single-image animators (MusePose/MimicMotion; Ant Group's Animate-X targets game/non-human characters), Wan 2.2 I2V/Animate (Apache 2.0, 5B runs on ~8GB via ComfyUI) — all local/free, all scriptable; GUI riggers (Spine/Live2D) stay ruled out. Built `tools/slice_sheet.py` (green-key → row clustering → label filtering → bottom-center registered frames → art_in + prints the ingest command; logic verified on a synthetic sheet). Waiting on Hiro to save the sheet as `tools/sheet_warlock_locomotion.png` and run slice+ingest; then walk/idle can ride the single-source flipbook (`?rig=0` compares today; per-action rig/flipbook split is a small driver change if he wants both). No 2C/Phase 4/summons.

## 2026-07-06 — Recheck #2 screenshots compared: double-belt + staff nudge fixed, awaiting verdict (game3d) [Fable 5]

**game3d.** Hiro's in-game shot of the green-lit rig vs his target: visibly close now. Fixed the two remaining mechanical deltas — robe waist band tucked under the torso belt (was reading as a double belt), staff orb raised/tightened to his back (top just above the head, shaft to the ground). Validator clean, landmarks re-checked numerically. The darker violet cast vs the target is arena ambient lighting, not a rig defect. Awaiting his explicit 2B.5 verdict before anything else moves. No 2C/Phase 4/summons.

## 2026-07-06 — GREEN-LIT part batch integrated (manifest v5), ready for recheck #2 (game3d) [Fable 5]

**game3d.** The green-lit reference finally landed and generated (console: `ref=ref_warlock_greenlit.png` on robe/torso/head/staff). Style leap confirmed by eye — the parts now carry Hiro's approved look (hair chains, knotwork collar, exact rune band); all face right; forearm still a single arm; the new head brings its own collar that double-covers the neck seam. Pivots manifest v5 re-measured for the new dims; validator clean; landmarks check (hem at ground, collar at neck, staff ground→above-head). Sandbox composite still blocked (bridge truncates 2 PNGs; host files intact). Next: Hiro's in-game 2B.5 recheck #2 — this batch is his own art. No 2C/Phase 4/summons.

## 2026-07-06 — Second regen batch integrated (manifest v4); greenlit ref still unsaved (game3d) [Fable 5]

**game3d.** Hiro reran the 4-part regen before saving the green-lit reference (console: `ref=warlock_idle.png`; `tools/ref_warlock_greenlit.png` confirmed absent) — so another old-ref batch, overwriting the last one with new dims. Silver lining: it's the cleanest batch yet; all four content-checked by eye and re-measured into pivots manifest v4 (torso belt sits low at 0.75 of its frame now; robe/head/staff re-pivoted). Validator clean; numeric assembly landmarks check out (hem at ground, collar at neck, staff ground→above-head). Logged a standing caution: no more `--force --parts` runs until the greenlit PNG actually exists — each one burns credits on a random old-ref variant and invalidates the manifest measurements. Next: recheck #2 on the current batch; greenlit rerun only if still short. Detail: plan STATUS.

## 2026-07-06 — Part regen batch integrated, manifest v3, awaiting recheck #2 (game3d) [Fable 5]

**game3d.** Hiro's 5-part regen (robe/torso/head/staff/staff-forearm) integrated: robe faces right (flipX mirror removed), forearm is a single clean arm (pivot moved to its new top-center elbow), torso gained a seam-hiding below-belt overlap, head/staff much cleaner — pivots manifest v3 with new sizes/pivots/scales for all five. Caught from the console output that the GREEN-LIT reference wasn't used (`ref=warlock_idle.png` — the image-save step got pasted into cmd, so the file never existed); documented the corrected procedure in the plan STATUS for if recheck #2 still falls short. Validator clean; numeric assembly landmarks check out; sandbox composite blocked this round (bridge truncates 2 of the new PNGs — host files verified intact by eye, Hiro's in-game check unaffected). Next: 2B.5 recheck #2.

## 2026-07-06 — 2B.5 recheck #1 failed → robe motion damped, staff-behind composition, green-lit ref pipeline (game3d) [Fable 5]

**game3d.** Hiro rejected recheck #1 (lower body still turning wrong / not following torso) and supplied a green-lit full-body edit as the quality target. Three changes: (1) robe follow-lag + waist-pivot sway damped hard in all three timing templates — the lag was counter-rotating the skirt against the torso, reading as "not following"; (2) staff re-composed to match his edit: behind the whole body, crystal peeking beside the head (was floating in front); (3) `gen_sprites.py` per-part ref override — once his edit is saved as `tools/ref_warlock_greenlit.png`, robe/torso/head/staff regenerate FROM that one coherent figure (single-source parts kill the glued-on drift). Verified: validator clean, in-engine build OK zero errors, composite refreshed (`tools/audit/rig_vs_ref.jpg`). **Blocked on Hiro's local step:** save the green-lit PNG + run the 5-part regen/ingest (commands in the plan STATUS) — the remaining quality gap is art-bound. Then his 2B.5 recheck #2. No 2C/Phase 4/summons.

## 2026-07-06 — 2B.5 fail fixed: measured per-part scale + robe facing (game3d) [Fable 5]

**game3d.** Hiro's 2B.5 verdict (proportions off / limbs glued / lower body backwards) root-caused and fixed. (1) Per-part scale now lives in the pivot manifest (`worldH` fraction of figure height + measured `alphaH` px, applied in buildWarlockRig; validator enforces both) — derived from the approved reference's actual alpha-row landmarks (shoulders 0.19, belt 0.40 of figure height), which also exposed the bind's hips sitting 70px too low; joint positions re-derived. (2) robe_lower confirmed generated facing LEFT → mirrored via a manifest `flipX` (pivot x mirrored too) until its regen (prompt hardened to "toes pointing right"); regen queued with arm_staff_fore. Iterated 4 screenshot rounds in the sandbox harness: also re-anchored the staff to the reference's shaft line/height (worldH 1.10, in front of the head) and sized the head up (0.40). Verified: node --check, validator clean on final data, in-engine build OK with zero page errors, and a side-by-side composite of the assembled rig vs the reference (`game3d/tools/audit/rig_vs_ref.jpg`). Next: Hiro re-runs the in-game 2B.5 check; 2C/Phase 4/summons untouched.

## 2026-07-05 — Overhaul 2B.4 complete: cutout-rig playback live, Warlock only (game3d) [Fable 5]

**game3d.** The rig now MOVES: `arena.html` gains eased playback (`rigEase`/`rigEval`/`rigPlay`/`rigUpdate`) driving the 2B.3 hierarchy from `warlock_rig.json` — walk/idle picked from the sim, HEX cast plays the one-shot attack with its impact freeze, robe lags the torso, scaled-dt clock so hit-stop freezes the rig, hurt-flash tint on all parts. Flag-gated: `?rig=0`, a failed build, or a transformation form all fall back to the Phase-2 flipbook untouched; summons/enemies not touched. Verified: node --check PASS; template math dry-run (exact freeze plateau, loop continuity every channel, no pose snapping); headless in-engine run with numeric pose samples + screenshots showing the assembled part-warlock mid-stride and in attack anticipation, zero page errors incl. the rig=0 fallback boot. Bind proportions are eyeballed-rough by design — that's 2B.5's call. Detail: plan STATUS block. Next: Hiro's in-game 2B.5 vibe check (the pivot's real test); 2C stays gated on a clear yes.

## 2026-07-05 — Overhaul 2B.3 complete: rig hierarchy + timing templates, built inert (game3d) [Fable 5]

**game3d.** `assets/sprites/warlock_rig.json` (16-node bind tree + idle/walk/attack keyframe templates with all four plan quality techniques: draw-order joint overlap, robe followTorso lag, per-track eases, attack impact-freeze plateau) + `arena.html` gains part/JSON preloads, a pure `rigValidate()`, and `buildWarlockRig()` — container tree with sprite origins = the 2B.2 pivots, built HIDDEN and undriven (playback is 2B.4; today's build renders identically). Verified: node --check PASS; validator 0 errors/0 warnings on the real data under node + catches 3 injected faults; headless in-engine boot logs "[RIG] build OK: 16 nodes, 9 sprites, 3 actions" with zero page errors. Detail: plan STATUS block. Next: 2B.4 playback (Warlock only, flag-gated), then Hiro's 2B.5 in-game vibe check.

## 2026-07-05 — Overhaul 2B.2 complete: Warlock rig pivot manifest (game3d) [Fable 5]

**game3d.** `assets/sprites/warlock_rig_pivots.json` written: 9/9 parts, normalized [0-1] pivot per part (= Phaser setOrigin), joint labels, measured against the real ingested image dims. Verified via a crosshair-overlay contact sheet, one visual adjustment pass (waist + both shoulders), re-checked. `arm_staff_fore` pivot flagged provisional until its content regen lands. Detail: plan STATUS block. Next: 2B.3 (hierarchy + timing templates in arena.html).

## 2026-07-05 — 2B.1 part transparency fixed locally, keyer root cause found, zero Grok spend (game3d) [Fable 5]

**game3d.** The 4 broken 2B.1 parts are fixed without regeneration. Root cause split (verified on raw pixels): robe_lower/tome came back on desaturated sage-green (70/110/75) that sat exactly ON the old key thresholds → 0% keyed silently; the two forearms had real green bgs but artwork covered the top-left corner — the only spot the keyer sampled. Permanent fixes in `gen_sprites.py`: 4-corner bg sampling, relaxed green thresholds, 1px erode + edge-band despill + feather (fringe metric 10-19% → ≤1.9%), a loud LOW-KEY warning when a cutout keys <10% transparent, a `--rekey` mode (reprocess from raw/, no API), and hardened green-screen/framing prompt language. All 4 re-keyed from raws, alpha-channel-verified, landed in art_in/ + re-ingested to assets/sprites (sha + host-side decode verified; anims.json intact). Lighting consistency across parts checked by eye on a contact sheet: PASS. One CONTENT defect survives keying: `arm_staff_fore` contains two limbs — the single justified regen, command handed to Hiro (hardened prompt). Detail: plan STATUS block. Next: 2B.2 pivot manifest on the 8 good parts.

## 2026-07-04 — Overhaul Phase 2B.1: cutout-rig part prompts ready, awaiting local generation (game3d) [Fable 5]

**game3d.** First step of the cutout-rig pivot (Phase 2B, Warlock pilot). Defined the fixed 9-part inventory (head / torso / robe_lower / staff-side upper+forearm / staff / off-side upper+forearm / tome — staff its own part, hands merged into forearms, robe one piece) and added a `--parts` mode to `gen_sprites.py`: 9 one-time edit-mode generations from the approved SIDE-ON idle, with the light direction pinned byte-identically in every prompt (key upper-left, teal rim right/tome side, violet ambient — confirmed against the actual approved PNG by eye first, per the plan's generation-time lighting requirement) plus the joint-overlap seam rule baked into each prompt. Verified via py_compile + dry-run (9 rows, identical light block, filters, crop path). Generation is the known LOCAL-only step — command handed to Hiro in chat (`python gen_sprites.py --parts`, then `ingest_art.py`). Acceptance closes after the by-eye lighting-consistency check across the landed PNGs. Detail: plan STATUS block. Next: 2B.2 (pivot manifest).

## 2026-07-04 — Phase 3 fix verified with real screenshot: scale-aware summon clearance (game3d) [Fable 5]

**game3d.** The reopened Phase 3 issue is fixed and screenshot-verified. `arena.html`: summon clearance is now scale-aware — `MIN_D = min(480, heroW/2 + thisSummon'sWidth/2 + 24)` off the live `displayWidth` (per-type estimate on first frames), so the 2.0x dragon gets ~410px+ clearance vs ~200px for a succubus; plus an on-screen floor (`sumW*0.10`, applied to BOTH the rest slot and the at-rest exclusion push-out — the first pass had the push-out shoving the dragon back off-screen, caught numerically) so big summons stay ≥60% visible and the depth rule takes over as the readability guarantee at the left wall. **Verified with an actual rendered screenshot** (per the fix brief), not just `node --check`: stood up headless Chromium+playwright inside the sandbox, served a reconstructed `game3d` copy, force-summoned dragon+fiend+coven — warlock silhouette fully clear; measured dragon w=710px at clearance 409px, depth 754 vs hero 756 (behind); fiend grounded, dragon airborne. Screenshot saved to `game3d/tools/audit/phase3_fix_verify.png`. Caveat: the sandbox's software GL can't finish registering keyframe sets (loader stalls), so flipbooks showed stills — fine for a distance/depth/motion test (same sprite sizes), but the Phase-2 stride vibe-check still needs eyes on the PC where the sets demonstrably load. Next: Phase 4 (VFX: breath cone from the mouth, painterly trailed projectile).

## 2026-07-04 — Phase 2 art generated + ingest skip-if-exists bug found and fixed (game3d)

**game3d.** Hiro ran the scoped 94-image generation locally (`gen_sprites.py --force --anim`, warlock walk/idle + 5 summons + door wave), then `ingest_art.py` with no args. Found: the no-args path silently skips any frame filename that already exists in `assets/sprites/` — so 13 sets ended up a Frankenstein mix of old attack-posed frames 1-3 + new correctly-posed frames 4+, and `warlock_idle` (both its frames named `_1`/`_2`, both pre-existing) got **zero** of the new content, with a stale leftover `idle_3` keeping the manifest at 3 frames. Fixed: deleted the stale `idle_3` file set, then force-re-ingested the 41 stale frame-1-3 filenames by passing them explicitly (bypasses the skip check). First attempt from the Claude-side sandbox hit the known mount-truncation hazard mid-batch (verified zero partial/corrupt writes before stopping); Hiro ran the same command locally and it completed clean. Verified via `anims.json` (`warlock_idle` now correctly 2, not 3) and by directly viewing the actual PNGs rather than trusting file timestamps — this mount doesn't reliably update mtimes even on real content changes (same quirk seen on Phase 1/2's code edits). Detail: `game3d/GAME3D_ANIM_COMBAT_OVERHAUL_PLAN.md` STATUS block. Next: Fable does the real Phase 2 acceptance vibe-check now that the art is genuinely fixed, then Phase 3.

## 2026-07-04 — Full quality roadmap approved: Phase 2B + 2C locked (game3d)

**game3d.** Hiro asked directly whether Phase 2B (programmatic cutout rig) actually gets to Dragon's-Crown-pro quality or is another half-measure — answered honestly: it fixes the real structural defect (AI-generation drift/discreteness) but rigid-part rotation isn't full mesh deformation, and final quality depends on Fable's authored timing curves, not guaranteed on the first pass. Walked through the realistic quality roadmap; Hiro approved all of it. Added to the plan: a generation-time lighting-consistency requirement in 2B.1 (pin light direction/rim-light across all separately-generated parts, or rotation reveals mismatched lighting — cheap to fix now, expensive later), and a new **Phase 2C — Motion Polish** section (motion arcs instead of straight lerps, bigger anticipation + follow-through overshoot, more independently-lagging secondary motion beyond the robe) gated to start only after 2B.5's in-game vibe check is a clear yes — don't polish a rig that might still need structural rework. 2C also carries an explicit honest ceiling note: even done well, this is "genuinely good cutout animation," not literally identical to Vanillaware's manual mesh-deform rigging (which Hiro has ruled out as a route) — an acceptable outcome unless Hiro decides otherwise after seeing it move. Extending the part-hierarchy to summons/enemies stays explicitly deferred until the Warlock pilot passes.

## 2026-07-04 — Animation pipeline PIVOT: flipbook → programmatic cutout rig (game3d)

**game3d.** Hiro shared research (`sorcerer-sword-animation-pipeline.md`) correctly identifying that Dragon's Crown/Vanillaware actually animates via 2D skeletal/mesh-deform rigging over single pieces of art, not per-frame regeneration — and that independent Grok calls per keyframe (Phase 2's approach) will always drift frame-to-frame, capping how smooth it can ever get. Investigated feasibility: true DragonBones/Spine rigging requires manual GUI bone-placement + mesh-weighting — neither Fable nor Claude can do that, and Hiro ruled out any path requiring work outside the Claude/Fable agentic workflow. Considered and rejected (Hiro's call): single-shot multi-frame sprite-sheet generation — still a fixed discrete-frame ceiling, and critically, it's the same "regenerate a whole sheet from scratch every time" waste pattern that's been burning Grok $ all along, not a fix for it. **Locked decision:** programmatic cutout animation — Grok generates each Warlock body part ONCE (reusable forever), Fable builds a part-hierarchy in Phaser code and animates it via hand-authored timing curves (tweened rotation/position, no regeneration ever needed for a new action or a feel tweak). This is the actual fix for "why do we keep regenerating art over and over" — parts are permanent assets; only genuinely new parts ever cost another Grok call. Piloting on the Warlock ONLY before any templatization to summons/enemies. This week's 94-image Phase-2 batch is not wasted but does not ship as-is — repurposed as visual/prompt reference for the new segmented-part art and the timing curves. Added as **Phase 2B** in `game3d/GAME3D_ANIM_COMBAT_OVERHAUL_PLAN.md`, ordered before Phase 4 (it's the single biggest open question in the project right now). Old Phase 2 status/history kept in the doc, marked superseded not deleted.

## 2026-07-04 — Phase 3 REOPENED: dragon still visually clings despite the fix (game3d)

**game3d.** Hiro's in-game screenshot (Fight 4/20, dragon active) showed the dragon's wing/tail still draping over the warlock — the depth rule renders it behind him (working as coded), but "behind and still covering half the frame" still reads as clinging. Root cause: `MIN_D=78` is a flat clearance applied to every summon type equally, but the dragon renders at 2.0x world height (vs 0.8-0.9x for succubi) — its actual wingspan far exceeds a 78px anchor clearance. Same screenshot confirmed animations read "a little smoother" (partial Phase 1/2 win) and showed the exact old-top-down-game fireball/acid-breath leftovers Phase 4 already targets (ground-skating red dot fireballs, small green circle instead of a directional breath cone). Sent Fable an exact fix brief: scale the minimum distance to each summon's own rendered size (off `SPRITE_TARGET_H` or display width) instead of a flat constant, re-verify with an actual screenshot (not just `node --check`) before calling Phase 3 done, then proceed to Phase 4 with the two concrete VFX targets above. Detail: `game3d/GAME3D_ANIM_COMBAT_OVERHAUL_PLAN.md` STATUS block.

## 2026-07-04 — Overhaul Phase 3 complete: summon distance/depth/per-type motion (game3d) [Fable 5]

**game3d.** Phase 3 shipped, render-only in `arena.html`'s SIDEON demon block. (a) Min-distance rule: assist rest slots now sit a full body clear of the warlock (78px + 34px spacing) with an exclusion clamp at rest; the synced melee dart may still cross his lane. (b) Depth rule: any summon sprite overlapping the warlock's silhouette renders BEHIND him (depth < P.y), never in front; the shared aura/hp-bar graphics layer is capped below him too. (c) Per-type traversal motion applied to the lane position itself (aura/hp-bar ride along): dragon FLIES at gy-64 with a slow heavy bob + velocity-driven banking tilt; succubus HOVERS at gy-26 with a faster/lighter float + side-to-side drift while attacking; claw fiend is grounded on the line with no float (run = dart + walk flipbook). Old shared "restless" sbob/sine wobble removed in SIDEON. No sim/AI/damage changes. Verified: `node --check` PASS (reconstructed copy — mount still tail-truncates; host intact via Read). On-screen acceptance (silhouette readable with 2+ summons, dragon flying, succubus hovering, fiend running) needs an eyes-on/auditor pass on the PC. Next: Phase 4 (VFX overhaul).

## 2026-07-04 — Overhaul Phase 2 complete: keyframe source fixed (game3d) [Fable 5]

**game3d.** Phase 2 of the overhaul plan shipped, code-side. `gen_sprites.py`: KEYPOSE vocabulary split by ACTION TYPE (walk = true 8-frame loopable stride with contact/down/passing/up × two mirrored halves; idle = 2 subtle breathing poses; attacks = 6 frames anticipation→contact→follow-through), policy via `frames_for()`, new `--anim <ent>_<act>` flag to generate keyframe sets directly without the auditor queue, `needs_rows()` floors stale queue entries to policy counts. `visual_audit.py`: coverage targets updated from flat ">=3 keyposes" to the same per-action policy so requeues reflect the new template. `arena.html` `loadAnimFrames()`: sets with >=4 frames now loop FORWARD at 14fps (no yoyo snap-back); legacy <=3-frame sets and 2-3-frame idles keep the yoyo. Verified: py_compile + behavior dry-runs PASS (prompt vocab, frame counts, queue flooring, coverage policy), `node --check` PASS. **Generation itself must run on Hiro's PC** (sandbox 403s api.x.ai) — scoped command handed over in chat (warlock walk/idle + 5 summons + door wave, ~94 images; NOT the full 56-set roster). Acceptance vibe-check (visible stride, no snap-back) possible only after that art lands + ingests. Next: Phase 3 (summon distance/depth/per-type motion).

## 2026-07-04 — Overhaul Phase 1 complete: double-motion stack killed (game3d) [Fable 5]

**game3d.** Phase 1 of `game3d/GAME3D_ANIM_COMBAT_OVERHAUL_PLAN.md` shipped: added `animDriving()` to `arena.html` and gated ALL procedural bob/lean/squash-stretch (hero locomotion block, `foeMotion()`, the crowd-wing shuffle rock, and the summon hover/lunge deformation) to stand down whenever a real ingested keyframe cycle (`anim_*`) is playing on that sprite — one motion driver per actor, procedural kept only as the fallback for still-texture actors. Step dust kept in both modes (ground-contact cue, not sprite deformation). Verified: `node --check` on the extracted script PASS. Note: the bash-mount tail-truncation hazard is back on this mount (mount copy ends mid-file; host file verified intact via Read at 2795 lines) — syntax check done on a reconstructed copy. Next: Phase 2 (KEYPOSE vocabulary + frame-count fix in `gen_sprites.py`).

**Infra.** Diagnosed why the week was a blackout: `game3d-build` (every 8 min) + `playtest-bughunt` (every 13 min) plus 6 more hourly schedules ran continuously and blew the weekly Claude quota by Sunday; every fire after that just returned "hit your weekly limit" and did nothing until reset (2026-07-03, 6am Chicago). All 10 scheduled tasks are now disabled. Also: `game3d/` is local-only by design (never touches the live site), so even the real progress made before the quota died was never going to be visible without opening `arena.html` directly.

**game3d.** Vibe-checked the current build against Hiro's feedback: janky double-motion animation (3-keyframe yoyo + procedural bob/lean stacked on the same sprite — confirmed in code at `arena.html`:~1435 and ~1752), summons rendering on top of / in front of the warlock with no distance or depth rule, attack VFX (fireball/acid breath) still at the original top-down game's tiny scale, and no walk-beat between gauntlet waves (instant pop, `arena.html`:~1319). Wrote `game3d/GAME3D_ANIM_COMBAT_OVERHAUL_PLAN.md` — 5 ordered phases (kill double-motion → fix keyframe source/frame count → summon distance+depth+per-type AI → VFX rescale/restyle → walk-to-next-wave beat). Decision: stick with the existing free Grok keyframe pipeline (`gen_sprites.py`), improve prompts + frame count, rather than integrate a paid third-party walk-cycle tool. Plan will be handed to a separate Fable-5 chat to execute (not a schedule) so cost stays deliberate/one-prompt-at-a-time.

**Scheduling discussion.** Explored a same-day 11am-1pm/every-30-min schedule as a hypothetical — confirmed that's possible via a date-pinned cron, but decided against using any schedule for the game3d work right now. Confirmed two real constraints for future scheduling decisions: (1) picking a specific model like Fable 5 for a scheduled task is only exposed in the app's Scheduled-tasks page UI, not from within a chat; (2) self-disable-on-completion instructions in a schedule's prompt have not been reliable in practice (matches the game-audit schedule's own history of re-firing because a prior disable "did not persist") — treat every schedule as needing a manual check/disable.

**Cleanup executed.** Removed all confirmed-cruft files: every `.bak`/`.bak-*` copy in `game/` (7 of `pit.js` alone, plus one-offs on `companionAI.js`, `music.js`, `questnav.js`, `touchstick.js`, `voice.js`, `main.js`, `ArenaScene.js`, `CityScene.js` ×2, `WorldScene.js`, `quests.js` ×2, `build_voice_manifest.js` ×2, `gen_wiki.js`, `smoke_test.js`, `EVOLUTION_UI_FIX.md`, `GAME_TASKS_TODO.md`, `index.html` ×2) and one in `marketing/`; the empty `game/New folder/`; orphaned scratch scripts `_evo_qa_tmp.js`, `evo_qa.js`, `qa_out.txt`, `tests/_tmp_gauntlet120.js`, `tools/_mount_probe.txt`, `tools/_ovr_test.js`, `tools/_probe_tmp.js` (verified none of these were referenced elsewhere in the repo first); and 11 `*.stale-narrator.mp3` files in `assets/voice/`. **Kept** `game/tests/demonlord_qa.js` and `game/tools/transition_probe.js` — both are actually referenced in `PLAYTEST_FINDINGS.md`/`GAME_BATCH_DEMONLORD.md`, so despite matching the "probe/qa" naming pattern they're real, not cruft. Also cleared `game3d/tools/audit/needed_sprites.json` to `[]` — it only held the stale 3-keypose requests from before this plan; it'll refill correctly once the auditor runs again against the fixed pipeline. Note: repo required a one-time delete-permission grant this session (OneDrive-style "Operation not permitted" on the first `rm` pass, same hazard documented throughout `GAME3D_UPLIFT_PLAN.md`) — resolved via the folder-delete permission prompt, no repo issue.

---

## 2026-07-02 — HIRO FEEDBACK #8 landed (game3d)

Banded backdrop stage (crowd wall no longer hidden behind the floor layer, pillars moved behind actors), full keyframe animation pipeline shipped (56 entity/action sets × 3 keyframes, `anims.json` + `loadAnimFrames()`), procedural stride/lean/squash-stretch locomotion added as a stopgap ahead of real art. Visual auditor hit **PASS — 0 P1** for the first time. Detail: `game3d/PLAYTEST_FINDINGS_VISUAL.md` run-log (L). *(This is also the run that introduced the double-motion-stack problem flagged 2026-07-03 above — the procedural motion was meant to be temporary "ahead of the art," but the art landed on top of it instead of replacing it.)*

## 2026-06-28 — Camera direction conflict resolved; split build reverted; forced top-4 order set

Two contradictory camera decisions (an old Guilty-Gear 1v1 gauntlet framing vs. the actual Dragon's-Crown-brawler call) were both marked "locked" in the plan on the same day — resolved in favor of the brawler (waves + summons-as-allies + sideways-scrolling follow-cam), 1v1 framing retired. The `game3d-anim` parallel schedule was disabled (produced nothing — needed a code seam that didn't exist yet) and `game3d-build` went back to owning the whole uplift solo. Forced order set: audit telemetry hook → remove dev-text overlay → crowd wall visible → pillars to midground + scrolling camera, before any more feel-polish. Repo also moved off OneDrive this day, ending a long-running bash-mount tail-truncation problem that had forced every prior run to verify edits via the Read tool instead of `node --check`.

## 2026-06-26 to 2026-06-27 — 2.5D uplift bootstrapped (`game3d/`)

Pivoted from the abandoned low-poly 3D/GLB spike to a 2D Phaser side-on approach (anime/painterly, Dragon's Crown-referenced). `GAME3D_UPLIFT_PLAN.md` + `GAME3D_PARITY_CHECKLIST.md` created (scope: Arena + Warlock, 1:1 parity with the original 2D combat, no new mechanics). Sprite scale-normalization fix landed (warlock was rendering huge, dragon tiny). Arch Devil transformation lifecycle ported. Full side-on sprite roster (12-14 fighters) ingested.

## 2026-06-21 to 2026-06-26 — Live game (`game/`) backlog closed out clean

`game-audit` schedule did a full read-only sweep, consolidated scattered TODOs into `GAME_BACKLOG.md`, and after several re-verify passes (schedule kept re-firing due to OneDrive timestamp ordering issues) confirmed the backlog was complete and self-consistent — no new open code items, schedule self-disabled. Evolution kits (Seraph wrath/aegis/judgement/bulwark, Druid bleed, Warlock lv20 roads) verified wired via `quick-fix` schedule, gate GREEN, self-disabled. Ronin 4-attack combo + independent parry windows, mobile zoom default fix, de-level/evo-panel re-offer bug all shipped and regression-covered.

## 2026-06-26 — Growth tracking started

`GROWTH_LOG.md` created — Royal Road baseline (10 followers, 5,473 views, 65 avg views/ch, 12 comments) with targets (+40%/mo followers, 1,000-follower sales inflection, 100 audiobook sales to greenlight Book 2).

---
*Earlier history (pre-2026-06-20) not backfilled — see individual doc files (`CINEMATIC_FIXES_TODO.md`, `EMBER_REMOVAL_PLAN.md`, `MOBILE_FIXES_TODO.md`, etc.) and `git log` if needed.*
