# GAME3D PARITY CHECKLIST — Arena + Warlock (2.5D anime uplift)

## STATUS
2026-06-28 (02:32) — **THE HOUND lunge-pounce — hound per-type verb MISSING → PRESENT (6th & final mob signature verb; champ/beast bosses already done).** The hound had been running the GENERIC melee brain (approach → `!` tele → arc swing). Ported pit.js:1668 into a dedicated `hound` branch in `updFoeAI` (`arena.html`, right after the `necro` branch, before the `champ` block, `continue`s past the shared brain like every other per-type verb). Faithful: it CHASES tight (advance while `dToP > reach+18`), and on a 0.42s crouch wind-up (`!` popup, `_tele` lights the sprite) it POUNCES — sets `lungeT=.28`, `lungeA=ang(e,P)` and dashes at **340 spd** down the locked lane, BITING **once** on contact (`!e.bit` gate, `rnd(4,7)×dmgScale`, small shake), then `cool=rnd(1.2,2)`. The "read the crouch, sidestep the leap" read — and with the master's RELEASE feeding fresh hounds, the pack rakes the warlock with relentless overlapping lunges from every angle (DC swarm pressure, not stand-and-trade). Verified: isolated `node` harness (`outputs/hound_harness.js`) ran the EXACT branch — `node` exec = SYNTAX OK + 6/6 behavioral asserts PASS (chases into range & attacks, executes a lunge, bites once, dmg=13 = 4-7×2 scale ∈ [8,14], `!` telegraph popup fired, shake≥3 on bite). File tail re-read WHOLE via Read tool (`</script>` L2538, `</body>` L2539, `</html>` L2540 — 2540 real lines = prior ~2519 + the 21-line branch; bash served the documented truncated ~1088-line stale tail again — `wc`/`node --check`/diff-subagent over the FUSE mount unusable). Live `game/` untouched; game3d NOT published. **Per-type foe AI now PRESENT for ALL mob/boss verbs: grave riposte · door guard-break · pyre zone-cast · master pack-release · gunner aim-line lock · necro raise · HOUND lunge · champ thrall-feed · beast two-phase enrage+charge.** The per-type-AI parity gap is CLOSED. NEXT (next DC-feel gap, no longer per-type AI): promote the render-only crowd chargers to REAL sim foes (hittable mid-charge) so waves are N authoritative foes, not 1 sim foe + N cosmetic pressers.
2026-06-28 (02:25) — **MULTI-FOE CROWD PRESSURE — 1 charger → TWO CONCURRENT (brawler-migration step 1, priority-2 FEEL gain).** The cosmetic crowd-charger (the 22:0x single rotating presser) was generalised from five singletons (`_crowdCharger`/`_chargeT0`/`_chargeStruck`/`_chargeAimX/Y`) into a SLOT ARRAY `this._chargers` of `{foe,t0,struck,aimX,aimY}` with `_maxChargers=2`, all inside `syncFoeCrowd` (`arena.html`, 7 surgical edits, no new method, single-copy arc now slot-sourced). Now up to TWO back-row foes break formation AT ONCE — each commits its own lunge-lock, paints its OWN ground lane oval (the 03:0x decal), and strikes its own committed window once; the spawn cadence was tightened to `now+700+rand·1000` (0.7–1.7s, deliberately UNDER the 1.4s arc) so a 2nd presser actually OVERLAPS the 1st instead of waiting for it to finish. Still RENDER-ONLY — both chips route through `hurtWarlock` (i-frame-safe, HP floored at 1), so two simultaneous pressers can never KO/softlock; each fully telegraphed + sidestep-dodgeable. Verified: `outputs/slotcheck.js` `node --check` SYNTAX OK + 5/5 randomized behavioral runs (cap≤2, reached 2 concurrent, ≥2 distinct foes struck, per-slot committed aim, strike-once-then-free); grep = ZERO remaining old-singleton refs; edited region balanced + file tail WHOLE via Read tool (`</script>` L2516 → `</html>` L2518, ~2518 lines; bash served the documented truncated ~1089-line stale tail again — trusted Read + the harness). Marked: the multi-foe-pressure row 1-charger → 2-concurrent. Still PARTIAL vs full DC (1 authoritative sim foe + N render-pressure chargers, not N sim foes — the true brawler refactor remains). ZOMBIES/BONE-ARCHERS + DEMON-LORD coven ×3 + arch-succubus FUSE still MISSING. LOOK unchanged ≈88% (a FEEL gain). Harvested → `game3d/blocks/multi_charger_slots.md`. Live `game/` untouched; game3d NOT published. NEXT: raise `_maxChargers` toward 3 + scale to wave size, then promote a render-charger to a REAL sim foe (hittable mid-charge).
2026-06-27 (PARITY+BENCHMARK pass) — **ATMOSPHERIC PARTICLE LAYER (rising embers + raking god-rays + drifting dust) — MISSING → PRESENT (closes the prior #1 LOOK gap "living crowd + ambient motion", ambient half).** Verified the new render-only atmosphere layer in `arena.html`: `buildAtmosphere()` (L2090, called from `create()` L1472 right after `applyCameraGrade`), `_initMote()` (L2117), `stepAtmosphere(now,dt)` (L2128, called from `update()` L2211 right after `syncFoeCrowd`). Three layers behind the actors (depth -50/-49, over the painted backdrop at -100/-99, under every depth≥0 actor/FX): (1) 4 ADD-blend GOD-RAY shafts (warm 0xffe6b0, angled 18°, scale 0.95×7.6, each breathing out of phase via `sin(t*0.6+ph)`); (2) 30 rising warm EMBERS (orange/gold, brisk upward `_vy` -18..-48 px/s, horizontal `sin` sway, fade in off the floor / out toward the rim, recycled at the top); (3) ~8 slower cooler DUST specks (every 4th mote, 0x9ab0d0). Self-managed `glow`-texture pool, guarded (`!textures.exists('glow')`→return), driven on REAL delta (`rdt`) so it stays alive through hit-stop. Sets `this._embers=true` → flips the `__AUDIT__` embers flag TRUE (the auditor was scoring it FAIL; audit hook L2218-2228 now reports embers live). Writes NO sim/combat/targeting state. ALSO confirmed (doc-lag reconcile): the warlock now POSE-SWAPS by STATE — `warlock_idle/walk/cast/hurt/summon` are all preloaded (L1331-1348, summon = Hiro drop 2026-06-28) and selected each frame by `P.flash`/`P.channel`/`P.moving` (L2176), no longer a single idle pose. Spot-checked kit/summons/evo/transform pit.js line refs vs the live pit.js — all still match (hexBolt:565, portal:581, summonDemons:597, devilDur:789, enterDevil:792, enterDemonLord:806, devilClaw:903, devilStrike:917, blink:951, enterLich:970, lichSlash:998, summonZombies:1014, summonArchers:1022, EVOLUTIONS:84). ZOMBIES/BONE-ARCHERS rows still correctly MISSING (summonZombies/summonArchers not ported to arena.html); arch-succubus FUSE/Sheol + DEMON LORD coven still MISSING/PARTIAL (see those rows). Updated rows: Title-screen-backdrop (embers/god-rays/dust now PRESENT), CAST pose + Caster-look (walk/hurt/summon poses now PRESENT). No code edited (read-only pass); only this checklist touched. DC-LOOK est. ~85% → ~88%. Live `game/` untouched; game3d NOT published.
2026-06-27 (later) — **THE HOUND MASTER PACK-RELEASE — master per-type verb MISSING → PRESENT (5th signature verb).** Ported pit.js:1678 into a dedicated `master` branch in `updFoeAI` (`arena.html`, right after the pyre block, before the generic `if(e.attacking)`, `continue`s past the shared brain like grave/door/pyre). THE HOUND MASTER holds a handler's standoff (`dToP>reach+170` → advance at 0.8 spd) and on a 1.5s cool either (a) RELEASES a fresh PAIR of hounds — `mkFoe({type:'hound',...,hp:66*dmgScale})` ×2, spriteKey/name/dmgScale set so they render via the crowd pool + fight via the generic hound AI, capped at 6 alive, 60% weighted, `RELEASE ×2` popup — or (b) cracks a TELEGRAPHED WHIP zone at the warlock's feet (`{type:'whipcrack',r:44,tele:.6,dmg:8*dmgScale}`, `WHIP` popup) when within 200. Added the whipcrack resolution to `updZones` (faithful to pit.js:2339: tele-only, detonates ONCE for `dmg` if the warlock is still inside `r` when the crack lands, pushes a `ring:true` lash swing + shake/vib, then splices — routed through `hurtWarlock` so roll/ward/fade i-frames negate it) and a tan TELEGRAPH-ring render in `zoneGfx` (fill closes in as the tele runs out → leave the circle). The spawns turn a single wave into a self-replenishing DC swarm — the verb that keeps the brawl crowded instead of bleeding to the boss. Verified: isolated `node` behavior harness PASSED all asserts (hounds spawn + cap at 6, hp=66×1.5=99 scaled, whip fires once the pack is capped, whip lands 12 dmg + consumes + pushes the lash ring when the warlock stands on it, and MISSES for 0 when he's stepped off the circle). All 3 edited regions re-read brace-balanced via the Read tool on FRESH disk (bash served the documented truncated ~1076-line stale tail again — `wc`/`node --check`/diff-subagent over the FUSE mount unusable); file WHOLE (`</script>` L2211, `</html>` L2213, 2213 real lines). Live `game/` untouched; game3d NOT published. Remaining per-type verbs: GUNNER aim-line lock (sidestep-the-laser), HOUND-pack flank, NECRO raise, CHAMP/BEAST boss phases (pit.js:1686-1805). NEXT: GUNNER aim-line lock.
2026-06-27 (PARITY+BENCHMARK pass) — **CAMERA POST-FX GRADE / BLOOM / VIGNETTE / painterly ColorMatrix — MISSING → PRESENT (FEEDBACK #6 B-2).** Verified the new `applyCameraGrade()` method (`arena.html` L1281, called at the end of `create()` L1271) adds a MAIN-CAMERA post-pipeline composited after the whole scene + Light2D sprites: (1) a hand-composed 4x5 custom `addColorMatrix()` (~1.16 saturation, ~1.08 contrast around 0.5, warm tint R×1.04/B×0.94, ~−0.04 offsets to deepen shadows), (2) `addBloom(0xffe6c0,1,1,1.05,0.58,6)` for the dreamy magic-lit halo, (3) `addVignette(0.5,0.5,0.72,0.42)` to frame the brawl + deepen the pit rim. Correctly WebGL-GUARDED (`!cam.postFX || renderer.type!==WEBGL` early-return) + try/caught (warns + skips, never breaks loadability on a Canvas fallback), sets `this._graded`. PURELY VISUAL — touches no sim/combat/targeting/render path. Marked PRESENT: the Title-screen-backdrop row's grade clause. Spot-checked the kit/summons/evo/transform rows against pit.js — every source line ref still matches (hexBolt:565, portal:581, summonDemons:597, enterDevil:792, enterDemonLord:806, devilClaw:903, devilStrike:917, blink:951, enterLich:970, lichSlash:998, summonZombies:1014, summonArchers:1022, EVOLUTIONS:84); ZOMBIES/BONE-ARCHERS rows correctly still MISSING (not ported to arena.html). No code edited (read-only parity+benchmark run); only this checklist touched. DC-LOOK est. ~80% → ~85%. Live `game/` untouched; game3d NOT published.
2026-06-27 (PARITY+BENCHMARK pass) — **TOUCH/MOBILE CONTROLS — MISSING → PRESENT (long-requested, Hiro asked 3x).** Verified the on-screen Dragon's-Crown touch UI added this run is wired through the EXISTING input globals with no new combat path: a LEFT virtual stick (`#stickBase`/`#stickNub`, `arena.html` ~L2038) writes `stick.{dx,dy,on}` (read each frame in movement at L1114), and a RIGHT diamond verb cluster (HEX/SUM/DASH/WARD, ~L2064) calls the global verbs `doSlash`/`doHeavy`+`heavyRelease`/`doRoll`/`doParry` (SUM mirrors the Q hold-to-channel→release-to-summon feel; HEX/SUM also resolve an open evo pick). Both shown on coarse/touch pointers or `?touch=1` (`#touch.on`). Marked PRESENT: the TouchStick row, the on-screen touch-buttons row, and the Parry/Roll rows (their last TODO — an on-screen button — is closed by `bWard`→`doParry`, `bDodge`→`doRoll`). No code edited (read-only parity+benchmark run); only this checklist touched. Live `game/` untouched; game3d NOT published.
2026-06-27 (later) — **THE WALL GUARD-BREAK — door per-type defense MISSING → PRESENT (2nd signature verb).** Ported pit.js hitEnemy:1187-1195 into the shared `foeGuard()` funnel in `arena.html`. Changed `foeGuard`'s contract from a boolean to a DAMAGE MULTIPLIER (0 = fully negated / grave parry, 0.25 = chipped off a guard / door CLANG, 1 = clean hit) so one funnel handles both negation and scaling. THE DOOR now blocks its FRONT: a LIGHT strike landing inside its ±0.9-rad facing arc CLANGs for 25% chip (`CLANG — flank or HEAVY` popup, small shake); a HEAVY strike BREAKS the guard for 2s (`GUARD BROKEN`, big shake+vib) AND still lands full damage; the break decays in `updEnemies` and pops `GUARD UP` when it reforms. Because `e.face` tracks the warlock each frame, the only ways past are HEAVY or FLANKING — stepping up/down the depth plane so the hit lands off the door's facing arc (a real use for the shallow 2.5D plane). Wired the new multiplier through all 3 warlock-damage sites: the HEX/fire/arrow bolt impact in `updFireballs` (parry → bolt negated; door → direct hit chipped, AoE/DoT unchanged), `devilStrike` (CLAW/BITE), and `lichSlash` (SCYTHE is always heavy → door guard always BREAKS, grave still parries the whole hurl+stun+dmg). Verified: an isolated `node --check` + behavior harness on the exact edited blocks PASSED all asserts (door light→0.25, heavy→1 & brokenT set, while-broken→1, grave→0, normal→1, flank-from-above→1 bypasses front); all 5 edited regions re-read brace-balanced via the Read tool on fresh disk (bash served the documented truncated 1080-line tail again), file WHOLE (`new Phaser.Game` L1989, `</html>` L1997, 1998 real lines). Live `game/` untouched; game3d NOT published. Remaining per-type verbs: PYRE 3-spell zone channel + mage-shield, MASTER pack-RELEASE, GUNNER aim-line lock, HOUND-pack flank, NECRO raise, CHAMP/BEAST boss phases (pit.js:1620-1805). NEXT: PYRE 3-spell channel + mage-shield.
2026-06-27 (23:01) — **GENERIC FOE AI — FOE BEHAVIOR MISSING → PARTIAL (foes now MOVE + FIGHT).** The roster sprites were INERT statues: `updEnemies()` only ticked status/DoTs, nothing chased/telegraphed/struck the warlock, and the only damage path to him (`foeShots`) was never spawned — so melee foes were harmless. This was the real #1 brawler gap (bigger than the per-type nuance the last STATUS named). Added `updFoeAI(dt)` to `arena.html` (faithful port of pit.js `updEnemyVs:1580` helpers chase/beginAttack/telegraphThenHit) + wired into the sim chain at L1000 (status DoTs FIRST, then the brain). Every foe now: (1) 2D-CHASES on the shallow plane (x AND y — up/down depth + left/right), sprite face flips horizontally to the warlock; (2) MELEE: approach → `!` telegraph (`A.wind`) → melee arc swing → `hurtWarlock` gated to a 1.3-rad front arc; (3) RANGED (gunner/pyre): hold a standoff, `AIM` telegraph, fire a `foeShots` bolt down the lane (pyre = burning); (4) STITCHER: mends the most-hurt ally (pink tracer), hangs back instead of fighting; (5) per-type FEEL via a `FOE_AI` table — door/chain/brute/champ/beast hit HEAVY + screen-shake, hounds lunge FAST, chain does a 2π ring sweep; stunned foes (lich scythe / dragon gas) freeze; damage ×dmgScale. Render-only fields (`_mv`/`walkP`) feed the pose-swap. Verified via Read tool on FRESH disk (bash served a TRUNCATED 1067-line copy ending mid-`load.image('gunner'` — the documented FUSE stale-tail; `wc`/`node --check`/diff-subagent all unusable): `updFoeAI` opens its `function` + closes `}` at L552 brace-balanced, the chain call at L1000, input handlers + Phaser boot tail WHOLE past L1140. Live `game/` untouched; game3d NOT published. The per-type SIGNATURE verbs remain the gap (door guard-break-on-heavy, grave RIPOSTE stance, pyre 3-spell zone channel + mage-shield, master pack-RELEASE, gunner aim-line, champ/beast boss phases — pit.js:1602-1805). NEXT: port the GRAVE COUNT riposte stance (pit.js:1655) — parry-and-counter, the most distinctive per-type verb — then the DOOR guard-break + PYRE 3-spell channel.
2026-06-27 (22:34) — **PAINTED PIT BACKDROP (Dragon's-Crown atmosphere) — EMPTY STAGE → PRESENT (FEEDBACK #6 part A).** Hiro dropped 3 new 1280×720 backdrop PNGs (`art_in/` 22:26): `bg_pit_far` (amphitheater walls + watching CROWD + braziers + god-rays, baked lighting), `bg_pit_floor` (bloodstained sand), `bg_pit_fg` (alpha-keyed pillars/chains framing the edges). These are BACKDROPS not actors → they SKIP `ingest_art.py` (no 512-cap/normal-map/scale-normalize); copied → `assets/bg/`, sources archived → `art_in/raw/`. Wired into `arena.html` as 3 PARALLAX LAYERS replacing the old flat-fill backdrop + magenta-ellipse ring: new `const BG` + 3 `load.image` preloads; `buildSideOn()` now adds far `setDepth(-100)` / floor `setDepth(-99)` (both behind every depth≥0 actor + FX graphic) / fg `setDepth(9000)` (over everyone), each cover-scaled `max(W/1280,H/720)` + `scrollFactor(0)`; the create() procedural floor is hidden via a new `this.floorGfx` ref; the old fills remain as a guarded ELSE fallback so the build never breaks if art is missing. `syncSideOn()` adds per-frame parallax (far −6%, fg −16% of the warlock's centre-offset). Render-only — no sim/combat touch. Verified via Read tool (bash served the truncated mount tail AGAIN — `wc`/grep missed the edits; a node-check/diff subagent gets garbage): all 4 edits re-read brace/paren-balanced on fresh disk, downstream tails intact. Live `game/` untouched; game3d NOT published. DC LOOK gap est. ~55%→~75%. NEXT: FEEDBACK #6 part B — FLICKERING brazier Light2D point-lights at the painted brazier spots so torchlight plays across the fighters, then bloom+vignette+warm ColorMatrix grade (Phaser 3.60 FX), god-ray drift, rising embers, crowd sway/roar/kill-puff.

2026-06-27 (22:1x) — **GLADIATOR ROSTER ART — STAND-INS → REAL SPRITES (the biggest LOOK gap, now closed).** Hiro dropped 14 on-model side-on sprites (`art_in/` 21:5x), one per pit.js `FIGHTS[]` enemy `type`: door/hook/chain/pyre/gunner/grave/stitch/brute/master/hound/necro/champ/beast + skel. Ingested all 14 (`tools/ingest_art.py` — added their TARGET_WORLD_H from the original hitbox radii: hound 0.8 … door 1.5 … beast 1.9; ran a clean copy from the non-OneDrive outputs dir because the FUSE mount served a truncated script to bash) → `assets/sprites/<name>.png`+`_n.png` (capped 512 + auto-Sobel normal), sources archived to `_src/`, originals moved to `art_in/raw/`. Wired into `arena.html`: 14 `load.image` preloads + 14 `SPRITE_TARGET_H` entries + a `FOE_TYPES` set & `foeTexFor(type,ranged)` helper; the 3 `spawnFight` `spriteKey` assignments (main foes, post-fight-3 stitch HEALER, wildcard) now resolve to the foe's REAL type sprite instead of the shambler/bonearcher stand-in. `buildSideOn`/`syncSideOn`/`syncFoeCrowd` already key off `spriteKey`, guard `textures.exists`, and scale by `SPRITE_TARGET_H[key]`, so each new sprite renders at its correct pit.js stature (THE DOOR towers, hounds knee-high, BELLOW'S beast hulks) with a safe fallback. Verified via Read tool on fresh disk (bash served the truncated arena tail again — `node --check` unusable): all edits intact, all 14 diffuse+normal+_src present, file whole (`Phaser.Game` L1719, `</html>` L1727, 1727 real lines). Live `game/` untouched; game3d NOT published. NEXT: per-type foe BEHAVIOR (door guards, gunner/pyre shoot, grave ripostes) so the roster fights like pit.js, not a shared approach→lunge — the art gap is closed, the AI gap remains.
2026-06-27 (22:0x) — **CROWD FOES ACTING (not just leaning) — PARTIAL → improved.** Added LIGHT crowd AI on top of the 21:5x restless-lean: in `syncFoeCrowd` (`arena.html`) a scene-level picker (`this._crowdCharger`/`_chargeT0`/`_nextCharge`, ~L1156) occasionally elects ONE waiting foe to break formation; that foe runs a windup→lunge→strike→recover arc (`t=(now−_chargeT0)/1400`, ~L1192) — rears back ~W·0.03, eases IN to just right of the player (`hitX=P.x+48`), pops a per-type colored 6-spark `glow` burst + tiny `S.shake` on the strike beat (once, gated by `_chargeStruck`), then eases back to its slot; auto-clears at `t≥1` (can never stick off-slot) and re-paces `_nextCharge=now+2600+rnd·2800`. Charger telegraphs a hot yellow tint + draws over the formation (`depth fy+40`). RENDER-ONLY — no sim/HP/damage written (the duel sim stays authoritative; this is a cosmetic DC pressure read). Verified: isolated `node --check` SYNTAX OK + a 1500-sample sweep of the arc — 0 NaN, deepest approach = hitX, returns to baseX, bounded (PASS). Both edits re-read balanced via the Read tool (bash still serves the truncated mount tail). Live `game/` untouched; game3d NOT published. NEXT: gen dedicated gladiator side-on sprites (paid gen-sprites, Hiro-present) to replace shambler/bonearcher stand-ins — the biggest LOOK gap; deeper: rotate the charger so different wing foes take turns + occasional real damage on the player.
2026-06-27 (21:4x) — **MULTI-FOE SIDE-ON CROWD — MISSING → PRESENT.** Every ALIVE foe that isn't the current duel foe now renders as a real side-on sprite (was: only `currentFoe` got a sprite, the rest were invisible — `enemyGfx` skips all in SIDEON). New scene method `syncFoeCrowd(now)` in `arena.html` (L1144, right after `syncSideOn`) + call in `update()` (L1429, just after `syncSideOn`). It keeps a `this._foeCrowd` Map of `{spr,shadow}` keyed off the enemy OBJECT: each frame it culls sprites whose foe `e.dead || e===cur || enemies.indexOf(e)<0` (death, wave-swap, AND promotion to the current duel foe so no ghost duplicate of `foeSprite`), then lays out the remaining WAITING foes in a 2-row back depth band — `x=min(W·0.96, W·(0.80+0.05·col+row·0.025))`, `y=gy−H·(0.07+row·0.075+0.012·col)`, scale `0.82−row·0.10−0.02·col` so deeper foes stand higher/smaller, `setDepth(fy)` y-sorts them, a hazy `0x8f8fa8` "waiting" tint (overridden by white hurt-flash / yellow `_tele` / magenta `hexT`). Each gets a thin red HP pip via a dedicated `this._crowdGfx` layer (depth 8000). Sprites pooled+reused (texture-key change rebuilds), shadow each. Render-only — reads `e.x/flash/_tele/hexT/hp`, never writes sim. Verified: isolated `node --check` SYNTAX OK; a behavioral mock (4-foe wave) confirmed pool=3 waiting, cull-on-kill, **cull-on-promotion (fixed a ghost-duplicate bug: was 2, now 1)**, wave-swap cull, no sprite leak (2 GO/foe). Edits re-read balanced via Read tool; file whole (`Phaser.Game` L1655–1657, `</html>` L1660, 1660 lines — bash served the truncated tail again, the documented OneDrive hazard). Live `game/` untouched; game3d NOT published. NEXT: gen dedicated gladiator side-on sprites (door/hook/chain/pyre/gunner/grave/stitch/brute/master/hound/necro/champ/beast) to replace the shambler/bonearcher stand-ins; then crowd foes acting (approach/attack) not just waiting.
2026-06-27 (21:2x) — **M4 — pit.js FIGHTS[] GAUNTLET ROSTER + WAVE LOOP — MISSING → PRESENT.** Replaced the 2 hardcoded placeholder dummies (`Shambler`/`Bone Archer`) with the real 20-fight ladder. New `FIGHTS[]` data table in `arena.html` (L165–255, above `GAUNTLET_N`) ports pit.js:1389 1:1 — every fight's NAME + TAUNT + foe composition (THE DOOR → TWIN HOOKS → THE CHAIN → … → SIEGE WORKS 4-up → THE FORMER CHAMPION → BELLOW'S SECRET beast+boss), HP/spd/r/col preserved. New `typeLabel()`, `mkFoe()` (seeds foe-AI + DoT fields), `spawnFight(idx)` (pit.js spawnFight:1456: `i=1+idx*0.30` stat scale, `dmgScale=1+idx*0.16`, post-fight-3 deck-stack = stitch HEALER + random wildcard + DOUBLE all HP). `GAUNTLET_N=FIGHTS.length` (20, was stub 21). create() (~L1000) now `spawnFight(S.fight)` (after `scene=this` L953, before buildSideOn L1036). `buildSideOn`/`syncSideOn` foe sprite keys off `f.spriteKey||foeKeyFor(name)`; spawnFight sets `spriteKey` = STAND-IN (ranged→`bonearcher`, melee→`shambler`) until gladiator art is gen'd. `killEnemy` no longer advances per-kill; a wave-cleared check in `frame()` (after the upd* block, ~L800) rolls to the next wave when `enemies.every(dead)` (or sets `gauntletDone` + banner on the last). Verified: isolated `node --check` SYNTAX OK + a clear-every-wave smoke rolls 0→19 & sets done, post-fight-3 stack correct (4 foes, HP doubled: door 180×1.9×2=684); all 4 edits re-read balanced via Read tool, file whole (`Phaser.Game` L1605–1608, `</html>` L1611, 1611 real lines — bash served truncated 1089, the documented OneDrive hazard). Live `game/` untouched. NEXT: render ALL alive foes as side-on sprites (a per-foe pool keyed off `spriteKey`, y-sorted in the depth band) so a multi-foe wave looks like a DC crowd — currently only `currentFoe` gets the big sprite, the rest are enemyGfx circles — THEN gen dedicated gladiator sprites.
2026-06-27 (17:55) — **#5 SUMMONS RENDER AS SIDE-ON ASSISTS — now PARTIAL.** The warlock's summons no longer scatter in a top-down ring; in SIDEON they form an ASSIST LANE at his LEFT, facing RIGHT, darting forward to strike the foe. Done purely in the demon DRAW loop of `arena.html` (sim/targeting/HEX/DoT untouched → combat identical): the per-demon body is wrapped in a render-only position override (L1148–1155) — each living summon takes an `_aSlot` (packed each frame in `demons[]` order) and its RENDER `d.x/d.y` moves to `P.x−52−slot·30` (left of+behind the hero), ground line `gy`, dragons hovering 64px up, a `slot%2` 2-row stagger; a `pow(sin(now/430+slot·1.9),6)` term keeps each assist RESTING in lane then briefly DARTING +78px right (reads as harrying the foe). Originals saved in `_oxs/_oys` and RESTORED at the end of the body (L1214) so the override is render-only (next frame the sim approach/swing/breath/fireball vs `enemies[]` uses true positions). Because the override wraps the WHOLE body, the existing code lays body/aura/HP-bar AND the lit sprite out side-on together (no disjoint floating bars) at front-plane scale (lane y≈gy ⇒ dty≈1 ⇒ dsc≈1.0). Sprite block (L1208): SIDEON forces `setFlipX(false)` (assists face the foe) + `×0.85` scale (secondary to the hero; dragons still tower ~357px). Reconciled a doc lag: priority-4's hit-stop/slow-mo + `finisherBurst()` were ALREADY in the code (mtime 16:40) though the 17:24 status listed them as next — verified present (`finisherWhirl` sets `S.hitPause=0.12`+`S.slow=0.45`+calls `finisherBurst()`; `update()` ~L1085 freezes then 35%-slows the sim), so this run took priority-5. Verified via Read tool (bash/`node --check` unusable — stale truncated mount tail): 3 edits balanced + symmetric, override opens L1149/restore L1214 inside one `for(const d of demons)` body, flip/scale L1208; file whole — `class Arena` L699, `Phaser.Game` L1278, `</html>` L1286. Live `game/` untouched. NEXT: sync each assist's DART to its actual sim attack event (lunge lands when damage lands) + a small side-on HP-bar under each lane sprite; THEN priority-6: port abilities/transformations to the side-on frame (warlock cast/hurt swaps, devil/demon-lord/lich form art).
2026-06-27 (17:24) — **#4 FINISHER BEAT + CAMERA WHIRL between fights — now PARTIAL.** New scene method `finisherWhirl()` (~L927, after `syncSideOn`), wired into `syncSideOn`'s new-foe branch (L886) so it fires the instant `currentFoe().name` changes (= the prior foe was SLAIN; the first foe is seeded in `buildSideOn` so frame-1 never false-triggers). On a kill: white `flash(0xffffff,0.16)` + `S.shake=13` (routed via `update()`'s `cameras.main.shake`) + a `banner('DOWN!','next challenger steps up')`, then a camera PUNCH — `cam.zoomTo(1.14,170)` + `cam.pan(W·0.66,H·0.5,200)` whipping toward the right entry side — that SELF-RESETS after 520ms (`zoomTo(1.0,360)`+`pan(W·0.5,H·0.5,360)`), so an interrupted beat can never leave the camera stuck zoomed/off-centre; all guarded on `this.cameras.main`. The existing lineup-shift + collapse + step-in now play UNDER the whirl. Reconciled a doc lag: the duel loop + foe AI (priority-3) were already in the code from the 16:38 edit but the plan's 16:2x status still listed them as pending — verified present via Read tool (helpers foeDmg:152/hurtWarlock:156/currentFoe:170/foeKeyFor:174), so this run advanced to priority-4. Verified via Read tool (bash `cp` again served the stale truncated tail — `wc` 1144 vs the real **1248**; node --check unusable): `finisherWhirl` opens L927/closes L940, call at L886, file whole (`Arena` closes L1238, `Phaser.Game` L1240–1245, `</html>` L1248). Live `game/` untouched. NEXT: polish into a true GG finish — brief HIT-STOP/slow-mo (`S.slow`/`hitPause`) + a finisher pose/particle burst on the dying foe — THEN priority-5: warlock SUMMONS as side-on ASSISTS at his LEFT striking the current foe.
2026-06-27 (17:0x) — **DUEL LOOP increment 3 — gauntlet counter now ADVANCES on a kill (still PARTIAL).** Closed the last hole in the side-on loop: a foe dying did not advance `S.fight`, so the GG HUD center "FIGHT n / N" sat frozen at 1/21. Made the single kill funnel `killEnemy(e,word,col)` (~L203) do `S.fight=min(GAUNTLET_N-1,S.fight+1)` after `gainLevel()` (and take an optional death-popup word/colour); `currentFoe()` then returns the next alive foe, which `syncSideOn` collapses-and-steps-in. Routed the one off-funnel death — the hex-DoT "ROTTED" kill in `updEnemies` (~L354) was `e.dead=true` inline — through `killEnemy(e,'ROTTED','#b070f0')` so a rot-death levels + advances identically. All kills now share one path. Verified via Read tool (bash served the stale truncated tail again — `wc` 1144 vs the real **1227**; node --check unusable). Both edits brace-balanced on fresh disk; file whole: `Arena` closes L1216, `Phaser.Game` boots L1218–1223, `</html>` L1226. Live `game/` untouched. NEXT: FINISHER + camera-WHIRL on a slain foe before the next steps in (priority-4); then the real FIGHTS[] roster port (M4) to replace the 2 placeholder dummies.
2026-06-27 (16:5x) — **DUEL LOOP increment 2 — foe SIDE-ON AI; HP now trades BOTH ways (still PARTIAL).** The current foe is no longer passive: new scene method `foeAI(now,f)` (called from `syncSideOn`'s live branch) runs a 4-state side-on machine on `currentFoe()` — **approach** (walk LEFT ~72px/s to a melee spacing `P.x+88`) → **windup** (0.34s telegraph: yellow tint via `f._tele`, small lean back) → **lunge** (0.20s fast dash to ~`P.x+40`; on crossing `P.x+62` calls `hurtWarlock()` once) → **recover** (back-pedal to home x, then `aiCd` rnd 0.6–1.4s). New free fns: `foeDmg(f)` (~6–12, scales w/ hitbox r) + `hurtWarlock(dmg)` — honors **WARD** (`P.wardT`>0 ⇒ negated, "WARD" popup) and **roll/blink i-frames** (`rollT`/`fadeT` ⇒ "DODGE"), else chips `P.hp` (FLOORED at 1 — no KO/respawn yet), sets `P.flash` (red hero tint, added to the form-tint block + `flash` added to the per-frame decay loop), shake + red popup. `_foeStepping` guard freezes AI during the collapse/step-in swap; `this.dt` exposed from `update()`; foe stun (`stunT`) pauses the AI. Tint priority now white(hurt)>yellow(windup)>magenta(hex)>clear. This also makes the **PORTAL WARD** mitigation real (was TODO: "needs enemy attacks first"). Verified via Read tool (bash served the usual stale truncated tail — 1143 vs the real **1220** lines; node --check unusable). File whole: `foeAI`/`syncSideOn` complete + brace-balanced between complete methods, `Arena` closes L1209, `Phaser.Game` boots L1211–1216, `</html>` L1219. Live `game/` untouched. NEXT: camera-WHIRL + finisher beat on the slain foe before the next steps in (priority-4).

2026-06-27 — **Milestone 0 (look spike) VALIDATED.** `game3d/spike.html` + `assets/spike/*` prove the core 2.5D trick: a flat anime warlock lit by Phaser **Light2D** with a baked **normal map** (cool key + green sheol + warm torch fills, contact shadow, additive staff glow) over an **angled 3/4 perspective pit floor**. This makes 2 foundation rows **PARTIAL** (lit caster sprite; angled floor). Everything else is still **MISSING** — `arena.html` (the playable combat scene shell with move + the 4 verbs + a `pit.js`-shaped combat-state object) has **not been built yet**. The RETIRED low-poly true-3D scaffold (`index.html`, `pit.html`, `city.html`, `champions.js`) is being abandoned — ignore it. Source of truth = `game/src/` (original shipped 2D game); all code refs are to `game/src/` unless noted. Block recipe: `game3d/blocks/lit_sprite_2_5d.md`.

2026-06-27 (later) — **REAL SUMMON/TRANSFORMATION ART INGESTED.** Hiro ran `tools/gen_sprites.py` (xAI Grok Imagine, on his PC) and dropped 5 on-model anime sprites: `clawfiend`, `bonedragon`, `blackdragon`, `succubus`, `archsuccubus`. All keyed/trimmed, given auto-Sobel normal maps, capped to ≤512px, moved to `assets/sprites/` (+`_n`), sources archived in `assets/sprites/_src/`. Wired into `arena.html`: preload (array form binds each normal map) + a `demonSprites` Map that lazily makes a Light2D-lit Sprite per summon, swaps bone↔black / succubus↔arch by form flag, and reaps on unsummon; graphics circles kept only as a load-fail fallback. The claw fiend / bone dragon / coven now render as Hiro's real lit creatures.

2026-06-27 (05:4x) — **FULL FIGHTER ROSTER INGESTED — 12/12.** Hiro re-dropped the COMPLETE set into `art_in/`; this run ingested the 5 remaining: `archdevil`, `demonlord`, `lich`, `warlock_hurt`, `warlock_walk` (same pipeline, now banked as reusable `tools/ingest_art.py`: cap ≤512 → auto-Sobel normal → `assets/sprites/<n>.png`+`_n` → archive `_src`). Every fighter (warlock idle/walk/cast/hurt · lich/archdevil/demonlord · clawfiend/bone+black dragon/succubus/archsuccubus) now has a diffuse+normal pair. Preload extended in `arena.html`; **seeded `SPRITE_TARGET_H` world-scale table** (per-type target heights) for the #1 scale-fix bug — table + formula in place, **NOT yet applied** in the draw path. NEXT: apply `displayScale=(HERO_PX*SPRITE_TARGET_H[key])/tex.height` in the hero/demon render + swap hero texture by form/state.

2026-06-27 (15:3x) — **SIDE-ON ROSTER RE-INGESTED (14 fighters) + 2 NEW FOES + warlock render fix.** Per Hiro's BUILD-PRIORITY-OVERRIDE, ingested the full SIDE-ON sprite set Hiro re-dropped into `art_in/` (these REPLACE the old front-facing art), incl. **2 brand-new pit challengers**: `shambler` (zombie brute, targetH 1.15) + `bonearcher` (skeletal archer, 1.05). Pipeline run inline (the mount silently dropped `ingest_art.py`'s stdout+writes when cwd was on the OneDrive FUSE mount, so processed to local tmp → copied each file in → **md5-verified all 28 diffuse/normal files match**). Wired into `arena.html`: `SPRITE_TARGET_H` gained `shambler`/`bonearcher`; preload gained both (array-form normal-mapped). **FIXED the warlock hero render:** the new side-on `warlock_idle.png` is 336×512, but the hero loaded as a hardcoded 256×384 *spritesheet* → `sheetOK` failed → it silently fell back to the OLD front-facing `warlock_diffuse`. Changed it to a plain normal-mapped `this.load.image('warlock',...)` and simplified the create() hero block to use it directly (single still). NEXT: still the #1 SCALE-FIX — apply `displayScale=(HERO_PX*SPRITE_TARGET_H[key])/tex.height` in the draw path (update() line ~793 + demonSprites make), then the side-on 1v1 GG stage flip.

2026-06-27 (15:5x) — **#1 SPRITE SCALE-FIX APPLIED — now PRESENT.** The `SPRITE_TARGET_H` world table is finally wired into the draw path of `arena.html`. Added `HERO_PX=210` + `BASE_R{brute24,dragon18,succubus10}`. HERO: `displayScale = sc·(HERO_PX·SPRITE_TARGET_H[formKey])/hero.height` (was a raw `formScale` that never divided by texture height → huge warlock). DEMONS: `dWorldH = HERO_PX·SPRITE_TARGET_H[type]·(d.r/BASE_R)` × perspective (was `d.r·5.4|4.4|3.8` → tiny dragon); the `d.r/BASE_R` factor keeps the binder/herald per-summon swell. Front-plane result: warlock ≈210px, clawfiend ≈252, dragons ≈420 (tower), succubus ≈168, archsuccubus ≈189. Verified via Read tool (bash served the usual stale truncated tail). NEXT: side-on 1v1 GG stage flip (warlock left/faces-right, one foe right/faces-left, lineup in back, two-fighter HUD).

2026-06-27 (16:2x) — **SIDE-ON 1v1 GG STAGE — STATIC SHELL now PARTIAL.** `arena.html` flips to the eye-level side-on duel presentation behind `const SIDEON=true` (+`SIDEON_GROUND_FR=0.84`). New `buildSideOn()` (called from create()): a depth-2 backdrop that covers the old angled floor — dark back wall + pit floor + the PIT RING drawn side-on as a flattened **magenta ellipse** (0xff3aa0 rim); a **frozen BACKGROUND lineup** of 8 upcoming foes (lich/demonlord/archdevil/clawfiend/bonedragon/succubus/archsuccubus/bonearcher) as small hazy Light2D sprites; the **CURRENT opponent** as a real side-on sprite (shambler/bonearcher) anchored x=W·0.72, scaled by the same `SPRITE_TARGET_H` contract, + shadow + red rim light; the **warlock pinned LEFT** (x=W·0.30, P.face=0 → faces right). Enemy-circle draw guarded `if(e.dead||SIDEON)`. STATIC tableau — no duel logic yet. Verified via Read tool (bash served the stale truncated tail again; file whole to L1093, `Phaser.Game` boots). NEXT: bind the duel loop to `pit.js FIGHTS[]` (foe HP→GG right bar, simple approach/attack, lineup advance on defeat), THEN camera-whirl + finisher transition.

2026-06-27 (16:39) — **DUEL LOOP increment 1 — foe sprite BOUND to the gauntlet (still PARTIAL).** The side-on `foeSprite` is no longer decorative: new `syncSideOn(now)` (scene method, called from `update()` behind `if(SIDEON)`) binds it to `currentFoe()`. New free fn `foeKeyFor(name)` maps a foe NAME → side-on sprite key (archer→bonearcher, black/bone-dragon, claw→clawfiend, arch-succ/succ, arch-devil/demon-lord/lich; default shambler). When the active foe CHANGES (prior one `killEnemy`'d via the live HEX/summon DoT path), the old sprite COLLAPSES (alpha→0 200ms) → next foe STEPS IN from x=W·0.86→0.72 (alpha+walk 360ms) → the FRONT background-lineup sprite is shifted off + faded (back row shrinks = "next steps forward"). Per-frame it tints the live foe (white on `flash`, violet on `hexT`). Gauntlet-cleared ⇒ last foe fades. NO foe AI/approach/attack yet (HP only trades one way). Verified via Read tool (bash/`node --check` unusable — stale truncated mount tail; file whole, `Phaser.Game` boots L1150–1155). NEXT: simple foe side-on AI (walk-in → wind-up → lunge that calls the player-damage path, + facing flip) so HP trades both ways, THEN camera-whirl + finisher.

Legend: PRESENT / PARTIAL / MISSING (vs. the new 2.5D `game3d/` target).

---

## Controls
| Feature | Status | Original ref |
|---|---|---|
| Keyboard move WASD/Arrows | MISSING | `scenes/ArenaScene.js` keydown handler; `combat/pit.js:1173,2303` reads `keys['w/a/s/d']` |
| Attack: J / left-click / `bSlash` btn | MISSING | `ArenaScene.js` (`j`, `pointerAttack`, `bindBtn('bSlash')`); `pit.js doSlash()` :307 |
| Heavy/SUMMON: Q (hold→press) / `bHeavy` (down+up) | MISSING | `ArenaScene.js` (`q`+`heavyRelease`); `pit.js doHeavy()` :1154, `heavyRelease()`:1166 |
| Parry/PORTAL: K / `bParry` btn | PRESENT | `ArenaScene.js` (`k`); `pit.js doParry()`:351 — `k` bound to `doParry` in `arena.html` firing the real PORTAL swap; the on-screen WARD touch button (`bWard`, ~L2068) now also calls `doParry()` |
| Roll/BLINK: Space / right-click / `bRoll` btn | PRESENT | `ArenaScene.js` (Space, rightButton); `pit.js doRoll()`:1168 — Space + right-click bound to `doRoll`→`blink` (gated on rollCD/rollT/channel/fade/paralyze); the on-screen DASH touch button (`bDodge`, ~L2067) now also calls `doRoll()` |
| Facing/aim toward nearest real foe + mouse aim | MISSING | `pit.js autoFace()`:306, `nearestRealFoe()`:301, `pointerAttack`/`pointerMove` api:3414 |
| Mobile analog TouchStick (left half) | PRESENT | `core/touchstick.js`; `ArenaScene.js TouchStick.attach`; `pit.js stick` :2303 — DONE in `arena.html` (~L2038): a left virtual stick (`#stickBase`/`#stickNub`) writes the global `stick.{dx,dy,on}` (the SAME field movement reads each frame, L1114) via Pointer Events; shown on coarse/touch pointers or `?touch=1`. ZERO new combat path — feeds the existing move plumbing |
| On-screen touch buttons (slash/heavy/parry/roll) | PRESENT | `ArenaScene.js bindBtn(...)` — DONE in `arena.html` (~L2064): a right-side diamond verb cluster (HEX/SUM/DASH/WARD) calls the global verbs `doSlash`/`doHeavy`+`heavyRelease` (SUM mirrors Q hold-to-channel/release-to-summon)/`doRoll`/`doParry`; HEX/SUM also resolve an open evo pick. Shown on coarse/touch pointers or `?touch=1` |
| Per-form button RE-LABELING | PARTIAL | `pit.js updateLabels()`:394 / `setBtnLabel`:393 — `updateLabels()` ported in `arena.html`: shows the active FORM (WARLOCK/ARCH DEVIL/DEMON LORD/LICH) in the `#verbs` HUD, called on every form transition. TODO: real SCYTHE/SUMMON↔CLAW/BITE labels once the devil verb bodies (devilClaw/devilStrike) reroute the verbs |
| Pause (Esc) | MISSING | `core/settings.js PauseUI`:keydown Escape |

## Base Abilities (Warlock)
| Feature | Status | Original ref |
|---|---|---|
| HEX bolt — slows + rots (DoT); 10s CD, herald 3s + STACKS | PARTIAL | `pit.js hexBolt()`:565; herald CD :567; stack :766 — DONE in `arena.html`: real `fireballs[]` bolt (vx/vy 420, r5, kind:'hex'), 10s CD (herald 3s), faces nearest foe, hit→HEXED (10s/15dmg/.5s tick) via `updEnemies`, HEX FIEND STACK +15. TODO: the SLOW on hexed foes (pit.js:1554 dt×0.6 — needs enemy movement/AI first), CONTAGION jump-on-death (:1290), particle trail parity |
| SUMMON channel (PRESS→auto-completes; ladder 3s/4s/6s) | PRESENT | `startChannel()`:573, `releaseChannel()`:577 (no cancel), tick :2285 — DONE in `arena.html`: the real LADDER is now ported into `frame()` — Q press auto-channels; rungs fire on their own at t1/t2/t3 (3s/4s/6s; DEMON LORD .5/1/3s) gated by level + "one alive" (rung1 CLAW FIEND, rung2 BONE DRAGON lv3+, rung3 COVEN lv5+→devil), then `P.channel=null`. `releaseChannel()` is now the faithful pit.js NO-OP. TODO: channel INTERRUPTION on hit/paralyze/silence (needs enemy attacks); coven rung body (succubus) still to port |
| PORTAL — swap places w/ FURTHEST foe, stun it, WARD 3s (herald 7s) | PARTIAL | `portal()`:581 — DONE in `arena.html`: faithful port on `doParry` — filters living foes (no-op if none), 3s CD, `P.ft.rolls++`, sorts FURTHEST-first & swaps positions, `clampArena` both (refactored to take an entity), `e.stunT=0.6`, `P.wardT`=3 (herald 7), WARDED/PORTAL/SWAPPED popups, shake+vibrate+flash. Visuals: ward ring on caster, stun marker on foe, `S.shake`→camera shake. TODO: the WARD actually mitigating incoming damage (needs enemy attacks/AI first); herald 7s verified once evo gating lands |
| BLINK (roll) — teleport 150 back, wide 4s stun, 2.2s CD | PRESENT | `blink()`:951 — DONE in `arena.html`: faithful port — 2.2s CD + 0.25s roll lockout + `P.ft.rolls++`, wide stun loop (any living foe <230px of the DEPARTURE point → `stunT=max(.,4)` + flash + STUNNED popup), teleport 150 back along `P.face`, `clampArena`, `fxBlink` leaf-burst at BOTH ends, `S.shake`→camera shake + vibrate + flash. 4 of 4 verbs now real (HEX/SUMMON/PORTAL/BLINK). TODO: stun fully exercised vs real foe AI once gauntlet roster lands |
| Caster look — robe/staff/book (dark elf, silver hair) | PRESENT | `pit.js drawFighter()` warlock branch ~:2438+; per uplift plan — DONE in `arena.html`: Hiro's **NEW high-res anime dark-elf warlock IDLE still** (`assets/sprites/warlock_idle.png` + `_n.png`, 256×384, silver hair / violet staff+gem / teal-runed black robe / glowing tome) loads as a 1-cell normal-mapped sheet (`WARLOCK_FRAMES=1`, frameConfig 256×384), **lit live by Light2D** (key tracks the warlock); single-frame `warlock_f0` fallback retained. Replaced the earlier lower-res 6-frame sheet. Book + staff clearly read in the still. **NEW: idle/walk/cast/hurt/SUMMON pose stills all present + state-swapped** (no longer a lone idle). Minor nits: left-facing `setFlipX` does NOT flip the normal-map X (lighting slightly off facing left); each pose is a single still (no animated frame cycles) |
| CAST pose / cast anim on HEX | PARTIAL | per uplift plan — DONE in `arena.html`: Hiro's **CAST still** (`assets/sprites/warlock_cast.png` + `_n.png`, 256×384 — staff raised, purple arcane lightning, glowing eyes; chroma-keyed from the green-screen `_src`) is loaded via the array form (normal map bound) and flashed for **0.35s on every HEX** via a guarded `castPose()` method called from `fxHexCast`, then reverts. **NEW: the warlock now POSE-SWAPS by STATE** — `warlock_idle/walk/cast/hurt/summon` are ALL preloaded (L1331-1348; summon = Hiro drop 2026-06-28) and selected each frame (L2176: `P.flash`→hurt, `P.channel`→summon, `P.moving`→walk, else idle), with a WALK BOUNCE + idle BREATHING bob (L2192). TODO: real multi-FRAME cycles (each state is still a single still — no animated frames); tie cast-pose timing to the actual cast/recover windows |
| Potions (health/etc.) | MISSING | `pit.js usePotion()`:53 |

## Summons (+ AI)
| Feature | Status | Original ref |
|---|---|---|
| CLAW FIEND (brute) — melee aggro-soak | PARTIAL | `summonDemons('brute')`:606; AI in `updDemons` :668 — DONE in `arena.html`: real `summonDemons('brute')` pushes a claw fiend into `demons[]` (hp `30+kills*5`, life 18, r24); `updDemons(dt)` brute AI chases nearest foe @95px/s, on contact 1.4s-CD swing → big shove (+70px) + token dmg (herald 2 else 1, ×dmgMul) + `killEnemy`. Smoke-tested (summon→chase→shove drops dummy HP). **ART (2026-06-27): now rendered with Hiro's real lit `clawfiend` sprite** (xAI-gen, on-model; Light2D + auto-Sobel normal map; graphics circle kept only as fallback). TODO: verify size/hp vs live numbers once gauntlet roster lands |
| BONE DRAGON — poison/acid breath = paralytic gas cone | PRESENT | `summonDemons('dragon')`:615; breath AI :682; black-dragon fireball :692 — DONE in `arena.html`: `summonDemons('dragon')` pushes a hovering dragon (`hp 44+kills*6`, ×1.35 herald / ×1.7 binder, life 15, r18); `updDemons` dragon branch = hover + approach/retreat band (>160 close, <100 back off) + 3.2s breath → green cone FX (`fxBreath`) + a lingering `zones[]` gas cloud (r110, 4s); `updZones` gas tick (.5s) refreshes light 2s stun + a hex-strength ACID DoT (`acidT`/`acidDmg 15`) ticked in `updEnemies` (pit.js:1546). **ART (2026-06-27): now rendered with Hiro's real lit `bonedragon` sprite** (Light2D + normal map; ground-glow/shadow/hp-bar kept). The BLACK DRAGON form has its own real `blackdragon` sprite ready — the render auto-swaps `bonedragon`↔`blackdragon` on `d.black`. TODO: black-dragon exploding fireball is coded (`d.black` branch) but only fires under the BINDER evo (not reachable until evolutions land) |
| Lingering GAS zone (paralytic + acid) + `zones[]` sim | PRESENT | `zones[]`:292; gas push :691; gas tick :2364; render :2834 — DONE: `zones[]` array, `updZones(dt)` gas branch (stun + acid refresh), floor render (green puff). Other zone types (fire/venom/frost/bolt/etc.) are enemy-side, deferred to the gauntlet roster (M4) |
| SUCCUBI (coven of 3) — fireball + fire DoT, can heal warlock | PRESENT | `summonDemons('succubus')`:621; succubus AI/fire :728 — DONE in `arena.html`: `summonDemons('succubus')` pushes `3*_cntMul` succubi on an orbit (`slot`, `hp 10+kills*2`, life 14, `cool rnd(.5,2)`); `updDemons` succubus branch orbits the warlock (`slot+=dt*.8`, lerp to a 58px ring), then every 2.2s **MENDs** him (+6, pink `tracers[]` beam) if `P.hp<maxHP()` else hurls a `kind:'fire'` fireball (r6, aoe45, `*1.6` dmg) at nearest foe. Added `maxHP()`/`formHP()`, `applyFire()`, `feedSuccubi()`, `tracers[]`+`updTracers`. **ART (2026-06-27): now rendered with Hiro's real lit `succubus` sprite** (Light2D + normal map; pink aura/hp-bar/mend-beam kept). The ARCH SUCCUBUS form has its own real `archsuccubus` sprite ready — render auto-swaps `succubus`↔`archsuccubus` on a future `d.arch` flag. TODO: arch-succubus fuse/burst + GREEN Sheol variant (herald/Demon-Lord, M8); the `fire`-flag BURN DoT only fires under the herald evo (coded, gated) |
| ZOMBIES (lich raise, 6s) — INFECTION DoT, infected rise | MISSING | `summonZombies()`:1014; zombie AI :645 |
| BONE ARCHERS (lich raise, 8s) — bone-shaft arrows | MISSING | `summonArchers()`:1022; arrow `updFireballs` :761 |
| Demon cap 12 (oldest dissolves) | PRESENT | `summonDemons` while-loop :599 — ported: `while(demons.length>=12){demons.shift()...}` in `arena.html summonDemons()` |
| Demon hurt/death + phylactery freeze | PARTIAL | `hurtDemon`:629, `updDemons` :632 — DONE: `hurtDemon(d,dmg)` + life/hp death (`UNSUMMONED` FX + splice) + REENTRANCY GUARD + phylactery/herald no-timeout conditions wired in `updDemons`. TODO: lich "last dragon falls→mortal again" banner; phylactery actually exercised once lich/dragon land |
| Fireballs/breath/sheol projectile sim | PARTIAL | `updFireballs()`:744 — `updFireballs(dt)` ported in `arena.html` for HEX + **ARROW** (direct, no splash) + **FIRE/blast** branches (direct dmg + `applyFire` if flagged + `feedSuccubi` + **AoE splash** at half dmg, pit.js:773-784). TODO: SLASH (piercing line) branch + the SHEOL-green `applySheol` spread-on-kill (herald/arch territory, M8) |
| Summons as SIDE-ON ASSISTS (GG framing, priority-5) | PRESENT | `arena.html update()` demon loop ≈L1144 — DONE: each living summon's `d.x/d.y` is overridden RENDER-ONLY into an ASSIST LANE at the warlock's LEFT (`P.x-52-slot*30`), real lit sprite overlaid (`clawfiend`/`bone|blackdragon`/`succubus|archsuccubus`) scaled by `SPRITE_TARGET_H × d.r/BASE_R`, `setFlipX(false)` (assists face RIGHT toward the foe), sim pos RESTORED at loop end. **POLISH (2026-06-27 18:08):** the lunge now interpolates ACROSS to the foe (`d.x=restX+lunge*(foeX-restX)`, was a fixed +78px that never left the left third) + a once-per-dart STRIKE SPARK (`d._lungeHot`) pops a 6-spark type-tinted `glow` burst on the foe. **SYNC (2026-06-27 18:40):** the dart is no longer a free `sin` clock — `updDemons` sets `d._atkPulse=0.30` at each OFFENSIVE attack (brute shove L550, dragon breath L565, black-dragon fireball L570, succubus fire-bolt L586; MEND branch excluded), decayed at L541, and the render lunge is `sin((1-d._atkPulse/0.30)*PI)` (0→1→0, peaks ~0.15s after the hit, rests at 0 between) with a `reach` factor (melee 1.0 crosses, ranged 0.55 surges partway). The strike-spark now lands WITH the damage; combat math 1:1 unchanged. TODO: per-lane HP-bar tidy (small bar UNDER each lane sprite, gate the 3 top `fillRect` pairs with `if(!SIDEON)`) |

## Evolutions (lv10 + lv20 panel + gating)
| Feature | Status | Original ref |
|---|---|---|
| EVOLUTIONS data table (warlock branches) | PRESENT | `pit.js EVOLUTIONS`:84 (warlock :103) — DONE in `arena.html`: faithful `EVOLUTIONS.warlock` (lv10 DREADBINDER/HEX FIEND, lv20 LICH SOVEREIGN/ARCHFIEND ASCENDANT w/ `from` gates) + `UNLOCKS_WARLOCK` (lv3/5/8 banners) copied 1:1 (names/focus/desc/kit) |
| lv10 choice: DREADBINDER (binder, DEX) vs HEX FIEND (herald, ATK) | PRESENT | `EVOLUTIONS.warlock[10]`:104; `maybeOfferEvo()`:159 — DONE: `maybeOfferEvo()` offers the 2-card lv10 road at `lvl()>=10`; picking writes `P.evo10` which lights up the dormant `_heraldSummon`/`_binder`/hex-stack/succubus-fire kit |
| lv20 choice gated by lv10: LICH SOVEREIGN (lichlord) / ARCHFIEND ASCENDANT | PRESENT | `EVOLUTIONS.warlock[20]`:113; gate :171 — DONE: `maybeOfferEvo()` filters `E[20]` by `b.from===P.evo10` at `lvl()>=20` (binder→lichlord, herald→archfiend), writes `P.evo20` |
| Evolution PANEL — frozen-scene card UI, click/keys 1-2 | PRESENT | `drawEvoPanel()`:3359, `evoCardRects()`:201, `evoClick`:220, `evoTick`:225 — DONE: Phaser-native panel (`buildEvoUI`/`updateEvoUI`/`clearEvoUI`, depth 200000) drawn from `evoCardRects()` so on-screen rects == hit-test; scrim + name/FOCUS/desc/GRANTS cards; `frame()` returns early while `P.evoPick` (combat FROZEN); keys 1/2 + tap (`evoClick`) resolve; live countdown in the hint. TODO: pixel-match the original card typography |
| AUTO/headless auto-default to road 1 (deadlock-proof) | PRESENT | `evoIsAuto()`:191, `evoTick`:229 — DONE: `evoIsAuto()` (window.QuestNav.mode>=1 / Autopilot.on / headless) → `pickEvo(0)` immediately; MANUAL gets a 30-unit failsafe countdown then auto-picks road 0. Cannot softlock |
| Persist chosen evo (GameState mirror, no re-offer) | PRESENT | `pickEvo()`:178; snapshot persist api:setPlayerSnapshot — DONE: `pickEvo()` mirrors to `window.GameState.player.evo10/evo20` when present (try/guarded); `maybeOfferEvo()` early-returns once the slot is set, so it never re-offers |
| Evo focus stat bonus | PARTIAL | `evoStatBonus()`:269, `EVO_FOCUS_BONUS` — DONE: `evoStatBonus(k)` + `EVO_FOCUS_BONUS=6` ported 1:1. INERT today (faithful: `stat()`/CON-DEX-ATK scaling isn't ported yet — `dmgBonus`=6+kills, `diceN`=lvl), wired to apply the moment stat scaling lands |
| BINDER kit: ×2 horde, ~1.45× size, ×3 dmg | PARTIAL | `summonDemons` `_binder` :603 — code ported & dormant in `arena.html summonDemons` (`_binder`/`_bMul 3`/`_bR 1.45`/`_cntMul`); NOW REACHABLE: picking DREADBINDER at lv10 sets `P.evo10='binder'` → the swell + BLACK DRAGON exploding fireball go live. TODO: verify the ×2 succubus count + lich-extra-dragon vs live numbers |
| HERALD kit: hex 3s+stack, +35% dragon/claw, sheol fire, succubi healed by fire | PARTIAL | :567,601,734-766 — code ported & dormant (`P.evo10==='herald'` branches: hex CD 3s + STACK in `updFireballs`, `_heraldSummon 1.35`, succubus `fire`-flag burn `applyFire`, `feedSuccubi` heal, ward 7s, no-timeout succubi). NOW REACHABLE via HEX FIEND at lv10. TODO: arch-succubus GREEN Sheol spread-on-kill = M8 |
| Unlock banners (lv3 bone dragon / lv5 succubi / lv8 arch devil) | PRESENT | `UNLOCKS.warlock`:75, `gainLevel()`:140 — DONE: `gainLevel()` (called on `killEnemy`, +1.5/kill cap 20) fires the `UNLOCKS_WARLOCK` banner + popup + vibrate on each integer level-up; DEBUG **L** key climbs levels until the gauntlet's real kill-growth lands (M4) |

## Transformations
| Feature | Status | Original ref |
|---|---|---|
| LICH / reaper (on death w/ bone dragon flying) — scythe, FADE, unkillable-while-dragon, 3-stage channel→resurrect | PARTIAL | `enterLich()`:970, `lichSlash()`:998, `fade()`:991, `resurrectWarlock()`:980, `lichPerish()`:986; binder bigger reaper :971 — **NEW 19:2x: the SCYTHE verb reroute + a minimal LICH form are in** (`arena.html`): `lichSlash()` ported side-on (L437) — green 96px arc on `currentFoe()`, half-dice ×0.8 dmg (DREADBINDER ×2), a 5s STUN + 220px knockback hurl, kill→`killEnemy(f,'REAPED')` so the gauntlet advances; `doSlash`→`if(P.lich){lichSlash();return;}` (L378) so the reaper stops casting plain HEX, mirroring the devil reroute. Minimal `enterLich()`/`exitLich()` (L529/540, faithful to pit.js:970/981 banners+FX, body-tex swap via `syncBodyTex`) + DEBUG **B** key force-toggle make it vibe-checkable; `updateLabels` shows `J SCYTHE / Q SUMMON`. **NEW 20:4x: K→FADE reroute is in** — `fade()` (L550, faithful to pit.js:991) sets `P.fadeT` 5s (10s on the binder/lich road) + `parryCD=9`; `doParry`→`if(P.lich){fade();return;}` (L718) BEFORE the portal foe-filter so FADE fires even with no foe present (pit.js:357). `hurtWarlock()` already negates damage while `P.fadeT>0` (L159) and `lichSlash`/`doRoll` already refuse mid-fade, so untargetability + "only the summons answer" come for free; label now shows `J SCYTHE / Q SUMMON / K FADE`. TODO: the death→rise→phylactery 3-stage pipeline + unkillable-while-dragon, lichPerish, resurrect |
| BLACK DRAGON (binder) — black body + green underglow, exploding fireball + acid | PARTIAL (art ready) | `summonDemons` `_binder` dragon :616; `d.black` fireball :692 — **ART (2026-06-27): real `blackdragon` sprite ingested + wired** (render swaps bone→black on `d.black`). Logic still gated behind the BINDER evo (not yet reachable). |
| ARCH DEVIL (herald) — devil form ~21s, CLAW/BITE, taunt cinematic + VO | PARTIAL | `enterDevil()`:792, `devilDur()`:789, `devilClaw()`:903, `devilStrike()`:917, outro cine `archDevilOutro()`:825, `drawArchCine()`:889 — `enterDevil/devilDur/exitDevil/archDevilOutro` ported into `arena.html` (banners+timer+herald→DEMON LORD taunt path); **NEW 18:2x: the warlock now visibly SWAPS to the side-on `archdevil` SPRITE** for the duration (not just a red tint+scale): `heroFormTex()` returns the form texture key, the render loop re-asserts `this.hero.setTexture(ft)` each frame (guarded `!_castTimer` so the cast pose isn't clobbered) + `castPose` reverts to the form body, so casting a HEX mid-devil no longer snaps back to the warlock. Same path covers LICH/DEMON LORD bodies (L83/86). **NEW 18:54: a TRANSFORM BEAT** — `formMorphBurst(ft)` (arena.html L969) pops a form-tinted shock ring + rising spark column + body flash + small shake on each genuine form change (gated by `_lastForm`, seeded so the first paint never bursts), so the morph reads as a fighting-game transformation instead of an instant texture pop. **NEW 19:1x: the DEVIL VERBS REROUTE is in** — `devilClaw()`/`devilStrike(mult,heals)` ported (arena.html L394-432); `doSlash`→CLAW (roll-dash + 2.0× carve) and `doHeavy`→BITE (1.0×, ascends a lane succubus else carves) while `P.devilT>0`, kills routed through `killEnemy` so the gauntlet counter advances; `updateLabels` shows live `J CLAW / Q BITE`. TODO: the dash-DEVOUR-own-summons priority, the taunt cinematic art, real per-form HP (L89) |
| DEMON LORD (herald terminal in-fight; resets next fight) — black+green, ×3 demons, −3s summon, auto-arch succubi | MISSING | `enterDemonLord()`:806; `_cntMul` demonLord×3 :605 |
| ARCH SUCCUBUS — black+green, GREEN Sheol burst (no ally harm, survives, no timeout) | PARTIAL (art ready) | arch logic `updDemons` :700-723; herald survive :721; spawn :626,932 — **ART (2026-06-27): real `archsuccubus` sprite ingested + wired** (render swaps succubus→arch on a future `d.arch` flag). Burst/Sheol logic = M8. |
| SUCCUBI fed by fire — immune + max-HP up (the BITE ascends one) | PARTIAL | `devilStrike` heal/ascend :930; fire-heal in succubus AI — **NEW 19:1x: the BITE-ASCEND is in** (`devilStrike(1.0,true)` flips a lane succubus to `arch`/r15/archT7 + banner). TODO: the fire-immune/heal + max-HP-up in the succubus AI itself |
| Per-form HP modifier (devil 1.1 / lich 0.8) | MISSING | `formHP()`:66, `maxHP()`:73 |

## Systems (AUTO / music / volume / options / HUD / pause / result)
| Feature | Status | Original ref |
|---|---|---|
| AUTO toggle F10 / button — OFF→FIGHT→FULL | MISSING | `ArenaScene.js cycleAuto`; `core/questnav.js cycleMode()`:16 |
| Autopilot bot (drives warlock kit incl. lich plan) | MISSING | `core/autopilot.js frame()` — warlock branch |
| AUTO board-linger + auto buy-out + auto victory-leave | MISSING | `autopilot.js` board:; `ArenaScene.js update()` autoLeave/vicAuto |
| MUSIC — per-char pit track (warlock=dungeon), title/fight switching | MISSING | `core/music.js MusicMan.play()`; `ArenaScene.js update()` pitTrack |
| Music MUTE ♪ toggle + persisted (`ss-arpg-muted`) | MISSING | `music.js toggleMute()` + `_syncBtn` |
| VOLUME sliders (music + voice), persisted | MISSING | `core/settings.js show()` slide(setMusicVol/setVoiceVol) |
| OPTIONS / Settings panel (⚙) + credits + NEW GAME wipe | MISSING | `core/settings.js SettingsUI` |
| HUD: HP bar | MISSING | `pit.js` UI.hpbar :2418; `ArenaScene.js ui.hpbar` |
| HUD: cooldown overlays (roll/slash/heavy/parry, per form) | MISSING | `pit.js` UI.cds :2421; `ArenaScene.js ui.cds` |
| HUD: level / kills / dice readout | MISSING | `pit.js` UI.stats :150,2419; `ArenaScene.js ui.stats` |
| HUD: nickname/name | MISSING | `pit.js` UI.name; `computeNickname()`:2004 |
| Banners (showBanner) + flash + popups + screen shake + vibrate | MISSING | `showBanner()`:1383, `flashFx`:1382, `popup`, `vib`:32 |
| Boss bar (fights ≥18) | MISSING | `pit.js` UI.bossbar :2420; `ArenaScene.js ui.bossbar` |
| Onboarding hints overlay | MISSING | `core/settings.js OnboardUI` |
| RESULT / shareable card (victory/death/credits PNG, Web Share) | MISSING | `core/settings.js ShareUI.make()` |
| Build stamp / stale-cache self-heal | MISSING | `main.js` :8 version, :15-31 build.txt self-heal |

## Transformations (M8)
| Feature | Status | Original ref |
|---|---|---|
| ARCH DEVIL — enter/exit + duration (15 / herald 21 / archfiend 31) | PARTIAL | `enterDevil()`:792, `exitDevil()`:800, `devilDur()`:789 — DONE in `arena.html`: faithful `devilDur()`, `enterDevil()` (lv<8 gate, `P.devilT`, `P.r=24`, flash/shake/vib/leafBurst/banner/updateLabels), `exitDevil()`. The `devilT` timer ticks down in `frame()` → `archDevilOutro()` on expiry. Coven rung calls it (non-Demon-Lord). Visual: hero scales to `P.r` + red tint. TODO: CLAW/BITE verb bodies (`devilStrike`:917/`devilClaw`:903) + per-form HP ×1.1 in combat |
| ARCH DEVIL outro cinematic (taunt → cast-down) + VO | PARTIAL | `archDevilOutro()`:830 — DONE: HERALD road fully ported (taunt banner + VO via VoiceMan-if-present → `enterDemonLord()` after 3s; per-fight `archCineFight` guard; `archLastTaunt` no-repeat). NON-HERALD: faithful taunt/seraph/cast-down BANNERS + VO, then revert. TODO M8: the seraphim DESCENT visual (`drawArchCine`:881) + the guaranteed-Lich pipeline |
| DEMON LORD — terminal black+green form | PARTIAL | `enterDemonLord()`:807 — DONE: ported 1:1 (terminal flags reset, `P.r=27`, green banner/popup/FX, `updateLabels`); reached via the herald outro or the Demon-Lord summon ladder. Visual: bigger silhouette + green tint. TODO: the .5/1/3s summon-ladder timings already exist; verify the black+green coven (arch succubi) once that art/logic lands |
| LICH / reaper — death-while-dragon-flies form | MISSING | `enterLich()`:970 (scythe, FADE, 3-stage resurrect, unkillable-while-dragon) — binder road |
| ARCH SUCCUBUS burst + GREEN Sheol fire | MISSING | `updDemons`:700-723; devil `devilStrike` arch-fuse :925 |

## Scene / Flow
| Feature | Status | Original ref |
|---|---|---|
| Intro / character demo (automated showcase + bio + VO) | MISSING | `startIntro()`:2175, `DEMOS.warlock`:~2120, `BIOS.warlock`:2089, `demoReset()`:2158 |
| Title screen + torchlit backdrop (embers/fog/torch glow) | PARTIAL | `ArenaScene.js makeTitleBackdrop()` / `updateTitleBackdrop()` — arena.html now has the **painted DC 3-layer parallax pit (bg_far/floor/fg)** + **4 FLICKERING brazier Light2D torches** that play live warm light across the fighters & track the fg parallax (`buildSideOn` `_brazLights`, flicker in `syncSideOn`) + the **camera POST-FX GRADE** (`applyCameraGrade()` L1281: warm custom ColorMatrix ~1.16 sat/~1.08 contrast/warm tint + `addBloom(0xffe6c0,1,1,1.05,0.58,6)` + `addVignette(0.5,0.5,0.72,0.42)`, WebGL-guarded + try/caught, PRESENT — FEEDBACK #6 B-2) + the **ATMOSPHERIC PARTICLE LAYER** (`buildAtmosphere`/`_initMote`/`stepAtmosphere`, arena.html L2090-2140: 4 raking god-ray shafts + ~30 rising warm embers + cool drifting dust, render-only behind the actors, sets `_embers` PRESENT); MISSING: crowd LIFE (sway/roar/brightness pulse — the painted crowd is still static), title UI/screen |
| Gauntlet ladder — 21 fights (THE DOOR … BELLOW'S SECRET) | MISSING | `pit.js FIGHTS`:1389; `spawnFight()`:1455 (scaling :1456) |
| Fight board (foe taunt/record/odds/stat table/buy-out) | MISSING | `toBoard()`:1951; `ArenaScene.js ui.screen('board')` |
| startFight / win / lose flow + death hints + quotes | MISSING | `startFight()`:1965, `winFight()`:2043, `lose()`:2054 |
| Victory screen + purse payout + city handoff | MISSING | `winFight` victory :2048; `ArenaScene.js handoffToCity()` |
| Bellow buy-out (leave after fight 3, +150 copper) | MISSING | `toBoard` canLeave :1961; `ArenaScene.js leaveArena()` |
| Level/kill growth, stat scaling, dice | MISSING | `gainLevel()`:140, `stat()`:52, `diceN()`:289, `maxHP()`:73 |
| Save/continue + zone routing | MISSING | `core/save.js`; `ArenaScene.js continueBtn` |
| Enemy AI roster (door/hook/chain/pyre/gunner/grave/stitch/master/hound/necro/champ/beast) | MISSING | `pit.js mkEnemy` + enemy update :1597+; `FIGHTS` spawn fns |
| Mobile + desktop layouts (DPR 0.55 chunky buffer, IS_PHONE) | MISSING | `ArenaScene.js create()` DPR; `OnboardUI` IS_PHONE branch |

---

## NEXT GAP TO CLOSE
**Port the ARCH DEVIL VERB BODIES — `devilClaw()`:903 + `devilStrike()`:917 — so the form actually fights.**
The TRANSFORMATION LIFECYCLE is now IN (this run): `devilDur()`/`enterDevil()`/`exitDevil()`/`enterDemonLord()`/
`archDevilOutro()` ported 1:1 into `arena.html`, the `devilT` timer ticks down in `frame()` → outro on expiry,
`updateLabels()` shows the active FORM in the HUD, and the hero scales + tints by form (devil red / lord green).
The HERALD outro → DEMON LORD path is fully self-contained and complete. DEBUG **V** key vibe-checks the devil
(press again → outro). What's still stubbed/missing:
- **devilClaw/devilStrike** (`pit.js`:903/:917): while `P.devilT>0`||`P.demonLord`, route `doSlash`→`devilClaw`
  (roll-dash to target + 2.0× carve) and `doHeavy`→`devilStrike` heavy BITE, with the arch-succubus FUSE branch
  (`d.arch`/`archT`) and the SCYTHE/SUMMON↔CLAW/BITE button RE-LABELS. This makes the form *playable*, not just
  cosmetic — the natural next piece (the lifecycle that calls it is now live).
- **LICH/reaper** (`enterLich()`:970 — on death while a bone dragon flies: scythe, FADE, 3-stage resurrect,
  unkillable-while-dragon) on the binder road, plus the seraphim DESCENT visual (`drawArchCine`:881) that the
  NON-HERALD outro currently fakes with banners-only.
- **ARCH SUCCUBUS** burst / GREEN-Sheol logic (`updDemons`:700-723) — real sprite already staged + auto-swapped
  on `d.arch`; needs the fuse/burst/Sheol-fire behavior.
- Per-form **HP ×1.1** in actual combat (needs enemy damage / the gauntlet roster).
The other big remaining systems (independent of M8): the HUD (HP/cooldowns/level-kills), AUTO toggle +
autopilot, music/volume/options, and the 21-fight gauntlet roster (M4) that replaces the 2 practice dummies.

---
### (previous gap — DONE this run)
**Port `summonDemons('brute')` (SUMMON, pit.js:606).** Done this run — `demons[]` + `swings[]` arrays,
cap-12 dissolve, count/size/dmg evo mults, `releaseChannel`→claw fiend, `hurtDemon`, `updDemons` w/ reentrancy
guard + brute chase/shove AI, `killEnemy` stub, `banner()` FX, demon+swing render (aura/hp bar/life rim/y-sort).
Syntax-checked + smoke-tested (summon→chase→shove drops a dummy's HP; swings expire).
(Prior run: ported `portal()` PORTAL onto `doParry` — swap-furthest-foe + 0.6s stun + WARD + 3s CD + visuals.)
(Prior run: ported the real `hexBolt()`/`updFireballs` HEX pipeline onto `doSlash`.) Original promote-the-spike notes:
1. **A single combat-state object** mirroring `pit.js`'s player `P` and shared `S` (positions, hp, facing, cooldowns, form).
2. **Move** (WASD/Arrows → `keys`, plus a TouchStick stub) feeding `P.x/P.y`.
3. **The 4 verb entry points** named exactly as the original — `doSlash()`, `doHeavy()`+`heavyRelease()`, `doParry()`, `doRoll()` — bound to J / Q(hold→release) / K / Space, each producing visible FX over the lit warlock.
4. **A `frame(now)` tick** as the per-frame driver.

This is highest-value because EVERY downstream system (HUD bars, cooldown overlays, AUTO autopilot, abilities, summons, transformations, evolution panel) reads/writes that same state shape and calls those same four verbs. Matching the `pit.js` API surface (`combat.P`, `combat.S`, `doSlash/doParry/doHeavy/doRoll/heavyRelease`, `keys`, `stick`, `frame(now)`) now lets the autopilot, HUD, and ability ports drop in 1:1 against `game/src/` without re-plumbing. Target a vibe-check build: lit warlock on the angled pit floor + the 4 verbs producing FX, before adding summons/evolutions.
