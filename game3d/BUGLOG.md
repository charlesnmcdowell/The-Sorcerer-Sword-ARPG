# game3d BUGLOG — the bug-fixing loop's queue

**How this works (Hiro + Claude + the `game3d-bugfix` schedule):**
- Anyone adds bugs under **OPEN** — one line each: `- [BUG-NNN] description (reported YYYY-MM-DD by who)`. Screenshots can be referenced by describing them; keep entries concrete ("X happens when Y, should Z").
- The `game3d-bugfix` scheduled task (every 10 min) fixes **ONE open bug per run**, topmost first, then MOVES the entry to **FIXED** with a dated note of what changed and how it was verified, and appends a short entry to the repo-root PROJECT_LOG.md tagged `[bugfix-schedule]`.
- Bugs too big or ambiguous for a small autonomous fix get retagged `[NEEDS HIRO]` in place and skipped.
- Ground rules for the fixer: EDIT game3d/ only — but game/src/combat/pit.js may be READ (never written) as the porting reference when a bug entry cites it; one bug per run; entries tagged [NEEDS-FABLE] are skipped (they need the interactive session, e.g. art generation); a bug marked "split across runs" may stay OPEN with an appended progress note until its last piece lands; minimal fixes, no redesigns; respect standing decisions (SOLO default on, sheet-flipbook art direction, claw-fiend-only summons, rig retired behind ?rig=1); no art generation unless the bug entry explicitly asks for it; verify with node --check on the extracted inline script — and if the shell mount serves arena.html TAIL-TRUNCATED (known hazard), verify the edited region standalone + confirm via the Read tool that the file still ends with `</html>` instead of "fixing" phantom corruption.

## OPEN

- [BUG-018] [NEEDS-FABLE — art] Mild bottom-clipping remains on stitch, chain, gunner sheets (feet-scan coverage .05-.36 on some frames; brute + grave were the bad ones and were regenerated 2026-07-15). Same 5-row regen with below-feet-margin language when budget allows (~3 calls). (reported 2026-07-15 by feet-scan)
- [BUG-019] [NEEDS HIRO decision] Code modularization: arena.html is ~3876 lines single-file with retired-but-dormant regions (duel foeAI/foeMotion, cutout rig). It is now fully mapped (docs/CODE_MAP.md) and the tail-wrapper pattern documented, but a true split into modules mid-playtest is high-risk with no test coverage beyond the node harnesses. Recommend doing it as a dedicated session with the regression gate extended first. (reported 2026-07-15 by Fable)

- [BUG-016] [NEEDS-FABLE — art + evolution wiring] ARCH WARLOCK + ARCH GRIM REAPER forms (Hiro spec 2026-07-13): on the evolution path that grants it, instead of the arch-devil transform he becomes an ARCH WARLOCK — keeps every warlock verb, hex hits harder, and his coven cast summons ARCH SUCCUBI; needs its own sheet (idle/walk/hex rows) + heroFormTex branch + summonDemons arch flag on that road. The reaper road upgrades to an ARCH GRIM REAPER: BLACK dragon phylactery instead of bone (d.black already exists for binder), keeps shamblers + bone archers (shambler infection already spreads — verify vs "defeated foes touched by shamblers rise as zombies"), reaper summons expire at 8s (DONE 2026-07-13). Art: archwarlock sheet + archreaper sheet when Fable is in session. (reported 2026-07-13 by Hiro) — PROGRESS 2026-07-19 (BUG-020 batch): archwarlock ability kit landed (hex/portal/dashcast + demonlord idle/walk body sets, black/toxic-green styling); heroFormTex still maps archWarlock→demonlord textures for the base body. Remaining: Hiro greenlight of the new body/ability sets (or his own body ref), + the ARCH GRIM REAPER road. — PROGRESS 2026-07-19 later: Hiro's OWN body ref `archwarlock_move.png` sliced (idle 6/walk 8/hex 8), heroFormTex now returns 'archwarlock' (demonlord stand-in RETIRED). Arch-warlock half DONE pending in-game greenlight; ARCH GRIM REAPER half still open.

## FIXED

- [BUG-020] ARCH WARLOCK form-incongruence batch (reported 2026-07-19 by Hiro, 6 items + 2 follow-ups). ROOT CAUSE (fishbone: code-logic + agent-process): every form-gated animation selection (`heroOneShot`, dash `_castSet`, finisher kinds) branched on `P.lich`/`P.devilT`/`P.demonLord` but NOT `P.archWarlock`, so the arch warlock wore base-warlock ability art; his stand-in body used the old 3-frame demonlord sets (<4 frames = yoyo playback = the back-and-forward idle strobe). FIXES: (1) idle strobe — demonlord_idle regenerated as a 4-frame locked-stance set (+5-frame walk), forward-loop; (2) dash/portal/hex — `heroOneShot` and `_castSet` are archWarlock-aware (`archwarlock_hex/portal/dashcast`, warlock fallback); (3) arch-succubus dash — `archwarlock_dashcast` (Hiro's golden ref `archwarlock_dash.png`) + `hound_afirehit` finisher (SCALED variant, UNREVIEWED); (4) arch hex — `archwarlock_hex` cast + `hound_ahexhit` TWO-claw-demon finisher (both Hiro golden refs, sliced verbatim via border-flood dark-bg keyer); (5) arch portal — `archwarlock_portal` cast (SCALED off demonlord still, UNREVIEWED); bolts/fireballs carry `arch:` flag → `ahexhit`/`afirehit` kinds in FINISHERS (K 1.277 golden / 2.06 scaled×0.84 shrink). Follow-ups same day: evolution road now opens on the KILL LEDGER — Hiro's 10-kill playtest exposed that `P.kills` was NEVER incremented anywhere (dmgBonus()/summon-hp scaling read a counter stuck at 0); killEnemy now increments it, the gate is kills-PRIMARY at `EVO_KILLS_NEEDED=5` (an lvl>=10 AND-condition would re-block: 5 kills only reaches lvl 8.5), level snaps to 10 when the road opens, and a 'THE ROAD OPENS IN N' popup shows progress. Verified by node harness on verbatim extracted fragments: offer at exactly kill 5, herald→archWarlock, binder→lichPerm, no re-offer — ALL PASS. Also same day: Hiro's dancer golden ref `dancer_move.png` sliced (idle 8 replacing the generated 10f — stale 9/10 swept — + NEW walk 8 / dance 12; COOKIE's anim_dancer_idle picks up the ref art automatically); SUMMON button hidden (`#bSummon display:none`, road-2 evo picks still work via card tap) + `startChannel` hard-guarded behind SUMMONS_OFF (it was resurrecting the warlock body over the lich via the warlock_summon sheet). All three resolver maps gained `archwarlock → warlock/forms/archwarlock/`. Verified: node smoke gate ALL GREEN (4+20), node --check on extracted inline script, wiring greps, 30+24 frames on disk. Cross-form ability art now has a standing checklist in docs/MAINTENANCE.md (FORM-VARIANT CONGRUENCE).

- [BUG-009] Four legacy types (door, master, champ, skel) on old-gen 3-frame sets. FIXED 2026-07-15 by Fable (agent, closed as part of the hurt-coverage pass): full 5-row modern sheets (idle/walk/attack/hurt/death, locked right-facing, feet margins) + hound's full redo (its hurt row was 1 frame). ALL 14 enemy types now carry hurt ≥3 and death ≥4 in anims.json (125 sets); stale door walk5-8/attack5-6 swept. The crowd renderer's existing hurt-on-flash / death-one-shot selection lights these up with zero code changes.

- [BUG-002] Solo mode: `scene.foeSprite` reported visible:true with no foe bound — stale invisible sprite lingered at its home position. FIXED 2026-07-14 by schedule: since the 1v1 duel render path is RETIRED (BRAWLER UNIFICATION), foeSprite/foeShadow are now born hidden (`setAlpha(0).setVisible(false)` at creation), the per-frame retired-path guard sets visible:false as well as alpha 0, and advPitVisible(false) does the same. Cosmetic/hygiene only — no sim or crowd-render behavior touched; the dead retired code past the `return` untouched. Verified: standalone node harness on the verbatim edited fragments — 12 asserts ALL PASS (born hidden, frame guard clears stale visible:true, null-safe with no sprite, advPitVisible both directions); Read-tool confirm arena.html intact (ends `</html>` at line 3804). (reported 2026-07-08 by Fable)

- [BUG-017] Enemies (and summons) rendered ~40% size despite correct world-height targets. ROOT CAUSE found by live Phaser harness elimination: the crowd pool's recreate check compared spr.texture.key to the base key — mid-animation the frame's texture key never matches, so every foe sprite was DESTROYED + RECREATED EVERY FRAME at the 512px base still and scaled against that height. FIXED 2026-07-13 by Fable: pool slots track their intended base key (o._base) so sprites persist and scale off the live anim frame; demon draw got the same live-frame fix plus removal of the 0.85 "support rank" shrink — summons stand at true world height. (Table values from earlier today kept: humanoids ≈ warlock, hound 1.0, succubus humanoid 1.05.) Verified: standalone Phaser 3.80 harness proved the scale math correct in isolation (315px vs 300px hero), pinpointing the recreate loop; syntax-checked rewritten block. (reported 2026-07-13 by Hiro)

- [BUG-001] Solo mode: duel HUD top-right showed "— DEFEATED —" + an empty foe HP bar when no foe exists. FIXED 2026-07-13 by schedule: updateGGHud's fallback branch split — "— DEFEATED —" only when enemies.length>0 (all dead, gauntlet-cleared state kept); with an empty roster the name blanks and the whole right fighter block (#gghud .side.r) gets visibility:hidden, reversibly restored whenever a foe/roster/ADV-town state returns. Town branch and left (warlock) side untouched; HUD stays display:none by default per BUG-010 — this only shows under ?hud=1. Verified: standalone node harness on the sed-extracted VERBATIM updateGGHud — 15 asserts ALL PASS (solo blank+hidden, live foe name/bar/visible, cleared→DEFEATED kept, town→KARRIDGE visible, toggle back to hidden, warlock side intact). Read-tool confirm arena.html intact (3715 lines, ends </html>; shell mount again served it tail-truncated at 2739 lines — not "repaired"). (reported 2026-07-08 by Fable)

- [BUG-013] "The wolf would not die" + invisible enemy attacks — SIM/RENDER DESYNC. FIXED 2026-07-09 by Fable (BRAWLER UNIFICATION): the 1v1 duel render path (scene.foeAI/foeMotion moving a theatrical foeSprite) and the cosmetic crowd formation/chargers are RETIRED — every foe now renders FULL SIZE at its SIM position in syncFoeCrowd with full anim coverage (hurt/attack/walk/idle + one-shot DEATH collapse with corpse fade), facing flip, full colour, HP pips; updFoeAI is the single AI + damage authority, so the hitbox IS the body. foeShots ride animated bolt sprites (anim_firebolt / gold anim_hexbolt, lifted 70px); hound pounce pushes a visible bite arc; kill beat (hit-stop + slow-mo + flash) moved into killEnemy off the retired finisher path. Verified: node --check on the rewritten syncFoeCrowd + host-side tail check.
- [BUG-014] Boot moved to THE OLD ROAD + hands-off ability tutorial (hex/blink/portal/summon demo with captioned banners + the original narrator intro clip); ANY key/tap hands over control ("PRESS ANY KEY"), demo channel cut before the coven so the devil transform stays discoverable. FIXED 2026-07-09 by Fable; verified with a scripted node run of the full tutorial timeline.
- [BUG-015] Blink stun + portal ward were invisible. FIXED 2026-07-09 by Fable: 2 new black-bg FX sheets (2 xAI calls) — anim_blinkwave (radial hex-lightning discharge, expands to the real 230px stun radius from the DEPARTURE point) and anim_wardaura (violet-teal rune bubble looping on the warlock while P.wardT>0, pulses and dims as it expires). Luminance-keyed, ingested (6 frames each), wired via blink()/updZones wrappers with glow-ring fallbacks.

- [BUG-007] Phylactery — the bone dragon holds the lich's soul. FIXED 2026-07-13 by schedule (pit.js READ-ONLY reference, lines 635/1488/639-641). (a) life-freeze `!(P.lich&&d.type==='dragon')` was ALREADY in updDemons (landed unlogged with an earlier port) — verified this run, overrides the BUG-004 5s cap for the phylactery dragon only. THIS RUN landed (b) the dragon-alive guard in hurtWarlock (after ward/roll-fade, before hp moves: while P.lich with any dragon hp>0, 'PHYLACTERY' popup, flash/shake still read the hit, hp untouched — pit.js:1488) and (c) the last-dragon branch in updDemons' unsummon path (P.lich + dragon + no other live dragon → 'THE LAST DRAGON FALLS — the lich can BLEED now' banner, 'MORTAL AGAIN' popup, flashFx+vibrate — pit.js:639-641); mortality is implicit (guard (b) fails once no dragon lives) and his next death stays FINAL per BUG-005. Verified: standalone node harness on sed-extracted VERBATIM fragments — 20 asserts ALL PASS (shield on/off by lich·dragon-alive·dragon-dead·brute-only, ward outranks the guard, 7s life-freeze vs mortal decay, last-vs-first-of-two banner logic, no banner for brutes/mortal deaths, e2e shielded→bleeds→SLAIN). Read-tool confirm arena.html intact (ends </html> at line 3711; shell mount again served it tail-truncated at 2741 lines / 200473 bytes — not "repaired"; it even truncated the harness file in outputs/, so the harness ran from /tmp). (reported 2026-07-08 by Hiro)
- [BUG-006] Lich kit — port the lich SUMMON channel + shamblers + bone archers (pit.js READ-ONLY reference; split across runs). FIXED 2026-07-13 by schedule (final run, pieces 2+3 closed out). Piece 1 ZOMBIES: done 2026-07-08 (see PROJECT_LOG; summonZombies + zombie AI + infection-rise + draw, 18-assert harness). Piece 2 ARCHERS: found ALREADY LANDED in-file this run — a prior run ported summonArchers() (~L1319: 2 archers behind him, hp 15+3·kills, r10, life 24, base slots, SOLO gate + force escape) and the archer updDemons branch (~L1360: hold/return-home per Hiro spec, 240/130 careful-distance band, d10+bonus/2 arrow at <300px, flat side-on lane, _atkPulse sync), plus arrow handling in updFireballs, bonearcher preload/draw wiring (BASE_R.archer=10) — but never logged it; verified this run. Piece 3 CLOCK+FADE: the K=FADE reroute was already in doParry (~L1492); THIS RUN landed the missing LICH CLOCK in frame() before the living ladder (pit.js:2279 port — 6s summonZombies · 8s summonArchers · 12s exitLich; lichlord extension/extra-wave SKIPPED, evolutions unwired). JUDGMENT (per piece-1 note, decided this run): the clock calls summonZombies/summonArchers WITHOUT force — the standing decision keeps ?solo=1 claw-fiend-only with ONLY the phylactery dragon bypassing gates; the 12s resurrection still fires in solo. Verified: standalone node harness on sed-extracted VERBATIM fragments — 21 asserts ALL PASS (solo gate/force, spawn stats+home, hold-home, band approach/retreat, arrow fires flat down the lane + lands + is consumed, 24s expiry, clock at 6s/8s/12s, no double-summon, solo-lich gating + resurrect, living ladder untouched by the lich branch). Read-tool confirm arena.html intact (3595 lines, ends </html>; shell mount again served it tail-truncated at 2755 — not "repaired"). (reported 2026-07-08 by Hiro)
- [BUG-010] Duel HUD (bars + VS) overlapped the journal text. FIXED 2026-07-09 by Fable: #gghud hidden by default (display:none at boot), reversible via ?hud=1 — updateGGHud still writes the hidden nodes so re-enabling is zero-risk. Awaiting Hiro's better HUD design.
- [BUG-011] 12 sheets looked static (identical frames, dragons' idle wings frozen, half-body frames e.g. brute). FIXED 2026-07-09 by Fable (agent, 17 xAI calls incl. 5 retries): clawfiend, bonedragon, blackdragon, succubus, archsuccubus, gunner, hook, necro, stitch, brute, beast, lich regenerated with exaggerated per-frame poses, hover-wingbeat idles, full-body margins; sliced, ingested, stale higher-numbered frames swept. gunner gained NEW hurt/death sets (auto-wired via anims.json loader).
- [BUG-012] Summons gated behind level 3/5 + the coven orbited/clung to the warlock. FIXED 2026-07-09 by Fable: level gates removed (all three rungs summon at any level; ?solo=1 stays fiend-only); succubus branch rewritten — holds/returns to her SPAWN point when no enemies (mend stays ranged via the tracer), flies to a 150-260px firing band on the nearest foe when enemies exist. Verified: node harness — holds spawn while the warlock walks 900px away, seeks to band d=258, fires from range, fiend anchor intact (4/4 PASS).

- [BUG-008] Enemy animations were old-gen 3-frame keypose sets. FIXED 2026-07-09 by Fable: full labeled sheets (idle/walk/attack/hurt/death rows) generated, sliced and ingested for hound, hook, chain, pyre, grave, stitch, necro, brute, beast (+ gunner as the earlier pilot, + a 3-row lich sheet for the hero form); foeMotion now also selects _hurt (on flash) and _idle (at rest) sets so the still texture is the last resort. Remaining old-gen types tracked as BUG-009. Verified: per-enemy contact-sheet review + anims.json counts matched every slice.

- [BUG-005] Warlock can't die — replace the HP floor with the ORIGINAL death→LICH rise. FIXED 2026-07-08 by schedule: removed the hp-floor in hurtWarlock(); faithful pit.js:1506-1515 port (hp=1, lichRiseT=3, paralyzeT=3, clears channel/heavyWind/rollT/parryT, summonDemons('dragon', force) bypassing the ?solo=1 gate, 'DEATH SIGNS THE LEDGER' banner); kneel countdown in frame() → enterLich() at 0; 'THE PACT HOLDS' popup on all 4 verb gates + movement rooted while paralyzed (pit parity); lich death FINAL → 'SLAIN' banner + current-fight restart after 2s (no lose() flow exists yet, so minimal restart per the entry); stale TODO at ~L1101 deleted. NOTE (pit-faithful): a lethal hit DURING the 3s kneel is also final, same as the original. Verified: standalone node harness (11 asserts — rise, solo-gate bypass, kneel→lich, final death, restart reset) + node --check on the verbatim edited fragments + Read-tool confirm arena.html intact (3217 lines, ends </html>; shell mount again served it tail-truncated at ~2796 lines — not "repaired"). (reported 2026-07-08 by Hiro)
- [BUG-004] Summons last far too long — should live ~5 SECONDS each. FIXED 2026-07-08 by schedule: set life:5 for brute/dragon/succubus in summonDemons() and simplified the draw-block divisor to d.life/5 so the life ring/fade reads correctly. Verified: standalone node harness (all 3 types spawn life=5, lifeFrac 1 at spawn / 0.5 at half-life) + Read-tool confirm arena.html intact (3184 lines, ends </html>; shell mount again served it tail-truncated at ~2798 lines). (reported 2026-07-09 by Hiro)
- [BUG-003] Summons clung to the warlock (SIDEON assist-lane render override pinned d.x/d.y to slots at his left + a render strike-dart). FIXED 2026-07-08 by schedule: removed the ASSIST LANE / MIN_D slot + dart override — summons now render at SIM x (per-type vertical motion on the ground line, depth rule, and _atkPulse-synced attack anim/squash all KEPT); summonDemons() records homeX/homeY; updDemons() brute+dragon hang back at/return to the spawn point when no enemies exist and seek+attack the nearest when they do; facing follows the sim (dropped forced face-right). Verified: standalone node harness (7 tests — holds spawn with no enemies, returns home when displaced, seeks+shoves an enemy, render remap restores sim pos, not glued when the warlock walks away) + Read-tool confirm arena.html still ends `</html>` (shell mount was serving it tail-truncated; disk file intact). (reported 2026-07-09 by Hiro)
- [BUG-000] (example of a completed entry) Hex bolt skated along the floor and was dot-sized — sim flies projectiles at ground-plane y (pit remnant). FIXED 2026-07-09 by Fable: BOLT_LIFT render offset (105px, mid-section), size max(92, r·17), flat SIDEON trajectory (vy=0, facing-sign direction), animated sheet projectiles (hexbolt/firebolt sets). Verified: standalone node harness + Hiro's screenshots.
